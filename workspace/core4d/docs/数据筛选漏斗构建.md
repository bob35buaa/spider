总体上要分层过滤，搞成漏斗式的，三级漏斗，然后要搞成14gate的漏斗【增加ankle jerk p95和obj max speed】
- Tracking：Body tracking error还是20， object pos error 20，obj ori error 10
    - 窄：就是严格遵循阈值
    - 宽：阈值 + 1
- 物理接触/穿透：
  - 物理接触；窄：原始阈值0.5；宽：0.51
  - release，手和腿的穿透：
    - 窄： release(≤0.30)	hand_pen3mm(≤0.32)	leg_pen(≤0.20)
    - 宽： release(≤0.60)	hand_pen3mm(≤0.50)	leg_pen(≤0.40)
- 运动健康: 不区分宽窄
  - ankle jerk p95：1000
  - obj max speed：3
1. 第一层:  用宽口径粗筛：目的是过滤掉很不好的case，连宽口径都过不去，那就根本不行
2. 第二层：宽窄口径之间的case：需要Agent初审（只是提供参考，不具有效力） + 人工复审（最终拍板）
3. 第三层：窄口径通过的case，
  - 对于aug的case，需要对比其相关联的3个case（每个case有orig，trans0，trans1，trans2），如果aug的状态和其相关联的case都是成功的，视作成功case -> 进入下游RL，否则需要Agent复审 + 人工二审确认


## Agent（大模型）初审
使用/mnt/ali-sh-1/usr/xiayibo/ecodata_history/ecodata2_xiayb_rednote_deploy/ecodata2/ecodata/inference_engine/call_api_imitate_redaccel.py
系统已安装了ecodata2库，然后调用模型选"Qwen3-VL-235B-A22B-Instruct"

写一个prompt，要求模型来审核渲染的视频帧，需要先抽帧，然后按照顺序塞给大模型，抽帧就是每秒2帧，
