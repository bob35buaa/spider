# E168 Box021 20231020_022 p1/p2 独立评测

日期：2026-07-17

状态：双 case 回收、视频补渲染、量化评测和独立 xlsx 全部完成。

## 回收与产物

production watcher 与显式 pull 均确认：

- `box021_20231020_022_p1`：`run_complete_pending_eval`
- `box021_20231020_022_p2`：`run_complete_pending_eval`

两条的 root NPZ、outdir NPZ、config、scene/reference/contact mask 均齐。p1 已有本地视频；p2 使用 `render_a100_cem_videos.py` 补渲染 `210` 帧、`50fps`，失败 `0`。

## 评测

固定入口：

```bash
bash workspace/core4d/scripts/eval/wrappers/eval_E168_box021_022_pair.sh
```

结果为 evaluated `2/2`、not ready `0`、errors `0`、numeric pass `0/2`。

| case | numeric 失败 | z p95 | raw contact | penetration | lower body | foot slip max |
|---|---|---:|---:|---:|---:|---:|
| `box021_20231020_022_p1` | lower body | 0.197 | 0.677 | 0.154 | 0.173 | 0.197m |
| `box021_20231020_022_p2` | penetration | 0.141 | 0.814 | 0.333 | 0.048 | 0.951m |

两条的 tracking、fixed-reference z p95、raw contact、release 和 no-fall 均通过。p1 lower-body 超过 `0.10`；p2 penetration 超过 `0.30`。p2 `foot_slip_max_m=0.951` 很高，但 foot slip 当前只是诊断项，不参与 numeric hard gate。

两条均未做用户人工核验，保持 `PENDING_MANUAL_REVIEW`。

## Excel

```text
workspace/core4d/results/E168/s6_downstream/cem/eval/box021_20231020_022_pair/E168_box021_20231020_022_pair_case_metrics.xlsx
```

- 10 sheets。
- “完整指标”为 `2 rows x 192 columns`。
- “未就绪”和“评测错误”均为 `0`。
- LibreOffice 重算 `90` 个公式，扫描结果 `0` formula errors。
