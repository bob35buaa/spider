# E189 完整执行记录备份：box004/box024/box001 E167A no-PRG vs PRG 配对消融

> 本文件是 `progress.md` 中 E189 章节的完整备份（执行过程、跨机协调、环境坑、
> xlsx 迭代细节），实验已收口。精简摘要见 `progress.md` 的"最近完成"条目，
> 正式结论见 [log/265](../log/265_E189_box004_box024_box001_e167a_no_prg_vs_prg_results.md)。

## 进行中：E189 box004/box024/box001 E167A no-PRG vs PRG 配对消融（Phase 0-2 已完成，Full 待启动）

### 2026-08-06

- 用户观察到 E172(box004)/E173(box024/box023/box001) 四物体全部只用 PRG 跑过，未与纯 `E167A_zOnlyBody` 做过配对对比；要求对 box004/box023/box001/box024 补做 E167A 重定向。
- 核查发现 box023 已在 E179 完成 16/16 配对消融（结论 `PRG_BETTER`，box023 保留 PRG）；与用户确认后，E189 范围收窄为 box004(6)+box024(9)+box001(28)=43 条 `CEM_ELIGIBLE` case，不重跑 box023，直接引用 E179 结论。
- Case 范围严格复用 E172/E173 冻结的 manifest（不重新走 S0-S6），评测标准采用 E179 的十二门（六门physics + root/EEF/object tracking），均为用户确认的推荐选项。
- 执行环境为本机 8×L20Y（与 E173 相同机器）；当前 7/8 卡被已知的 GPU-filler `.cache/run.py` 占满（历史实验中记录为 user-authorized，可 pkill 释放），GPU3 空闲。
- 详细计划已写入 `plan/215_E189_box004_box024_box001_e167a_no_prg_full_cem_plan.md`：43 行按 `object_key,case_id` 排序后 `index%8` 分片到 8 卡（6,6,6,5,5,5,5,5）；9 条本地 canary 覆盖三物体各自的 v1-pass/v1-fail/v2 情形；十二门 E172/E173 baseline 待 Phase 0 只读重算（当前只知六门：box004 5/6、box024 3/9、box001 10/28）。
- 结论分级按物体独立判定（`NO_PRG_BETTER`/`NONINFERIOR`/`PRG_BETTER`/`MIXED`），不强行合并成单一跨物体结论，因为 E173 本身发现 PRG pass 率随物体尺寸单调退化，大箱(box024/box001)可能与小箱(box004/box023)方向不同。
- 用户批准计划后，Phase 0-2 已全部执行完成：
  - **Phase 0**：`scripts/experiments/E189/build_paired_authority.py` 一次通过，冻结 43 行 input authority（E172 manifest SHA `c53d0619...`、E173 manifest SHA `2128deb8...`，与 E179 记录一致）；只读重算十二门 baseline：box004 `2/6`(33%)、box024 `2/9`(22%)、box001 `5/28`(18%)——均比六门 baseline 更低，且降幅比 box023 的先例更大，说明大箱在十二门标准下的 PRG 可用率已经很低。
  - **Phase 1**：`build_e167a_no_prg_manifest.py --apply --snapshot` 一次通过，43/43 scene audit、43/43 config audit、43/43 no-PRG pair audit、43/43 与源实验 pre-PRG rubber scene 语义完全一致；`audit_e167a_no_prg.py --require-all` 43/43 pass。已用 `scripts/convert/snapshot_scenes.sh E189` 生成含 git HEAD + sha256 的 `results/E189/scene_snapshot/manifest.txt`（安全网2），并对 43 个新生成的 `scene_act_E189_rubberHull.xml` 执行 `git add -f`（安全网1）。
  - **环境修正（第一次，后被推翻）**：发现本机当时 `MUJOCO_GL=osmesa` 会导致 `import mujoco` 直接失败（libOSMesa 缺失）；一度改为 `MUJOCO_GL=egl` 并在 Claude Code 工具的 shell 里验证通过（含真实渲染）。
  - **环境修正（第二次，最终结论）**：用户在自己的交互式 root shell 里直接跑 `MODE=full bash run_E189_local_8gpu.sh`，在同一 `audit_e167a_no_prg.py` 的 `import mujoco` 上以 `AttributeError: 'NoneType' object has no attribute 'eglQueryString'` 崩溃——说明 `MUJOCO_GL=egl` 是否可用取决于调用它的 shell 环境，不是稳定的机器属性，之前"改成 egl"的结论不可靠。根因是 `mujoco/__init__.py` 只用 `try/except ImportError: pass` 保护 Renderer 相关 import，而 GL 库加载失败时抛出的是 `AttributeError`（不是 `ImportError`），不会被吞掉；`osmesa`/`egl` 两个显式取值都可能触发这种未被保护的崩溃。已验证 `MUJOCO_GL` 留空（unset）时 mujoco 会落到 `glfw` 分支，glfw 缺失/失败是干净的 `ImportError`，会被正常吞掉，`import mujoco` 因此稳定成功；canary/Full 全程 `save_video=false`、viewer disabled，本来就不需要真实 GL 上下文。已把 `run_E189_local_8gpu.sh` 和 `run_cem_queue.py` 都改成显式 `unset`/移除 `MUJOCO_GL`，不再有任何脚本强制某个 GL 后端。
  - **Phase 2**：`run_E189_local_8gpu.sh canary` 完整跑通：pkill GPU-filler 后 8 卡全部释放（之前 7/8 卡被 filler 占用 34-55GB），9 条 canary 分片到 6 张卡（GPU0/1/4/5/6/7，GPU4 跑 3 条、GPU5 跑 2 条），全部以 `run_complete_pending_eval` 收尾，`validate_runtime_outputs` 逐行复核 9/9 PASS（无 PRG 诊断字段泄漏、无 PRG 生效字段、E167A profile 完全匹配、qpos 全部有限）。9 条 final object tracking error（pos/quat）范围合理（pos 0.085-0.200，quat 0.058-0.371），无 NaN/爆炸迹象。运行结束后 8 卡全部归零（GPU-filler 未自动恢复，需要用户自行决定是否重启）。
  - **交付物**：`workspace/core4d/scripts/launch/active/run_E189_local_8gpu.sh full`（同一脚本，`MODE=full` 即可对 43 条正式跑 1024×32 CEM），已随 canary 路径验证过审计/分片/合并逻辑，尚未执行 full 模式。
- Tracker 已更新为 Phase 52 `⏳ Phase0-2完成；Full CEM待启动`。下一步（需用户决定何时触发）：跑 `MODE=full` 43 条 Full CEM（预计比 canary 长得多，seed 0/1024×32），随后统一评测、按物体配对对比、视频复核、completion audit。
- 用户在 `lshb-qs-jzsy-11` 上跑了 `MODE=full`（本 Claude Code 工具所在机器是 `lshb-qs-ditg-12`，两台机器共享 `/mnt/ali-sh-1/...` NFS 但 GPU/进程互不可见，只能靠共享盘上的日志/manifest/shard 文件判断跨机进度）。截至 2026-08-06 23:37，43 例中 29 完成、8 running、6 排队，进展正常；此前误判"任务已死"是因为在错误的机器（ditg-12）上查 `ps`/`nvidia-smi`，已向用户更正。
- **gpu7 队列真实中断**：`box024_20231011_028_p2` 的 CEM 本身算完了（`Total time: 2470s`，已写出 `trajectory_mjwp_act.npz`），但 `run_cem_queue.py` 把结果 copy 到 canonical `result_npz` 时撞上共享 NFS 的瞬时 `OSError: [Errno 5] Input/output error`，这一步没有 try/except，直接把整个 gpu7 shard 进程崩掉，导致其后排队的 3 例（`box001_20231020_010_p1`、`box001_20231003_1_042_p2`、`box001_20231023_108_p2`）从未被尝试。
- 已修复 `scripts/experiments/E189/run_cem_queue.py` 的健壮性：新增 `copy_with_retry`（3 次重试）包住 npz 拷贝，`run_one` 内部的拷贝+校验包进 try/except（失败记 `failed_postprocess`，不再让异常向上传播），`main()` 的逐行循环也包了一层兜底，单行未预期异常不再拖垮同一 GPU shard 里排在后面的 case；`failed_postprocess` 已加入可重跑的 `eligible_statuses`。
- 手动恢复 `box024_20231011_028_p2`：其 `outdir_npz`/`config_act` 已存在，只是缺 `result_npz`，用修复后的 `copy_with_retry` + `validate_runtime_outputs` 直接补齐并写回 `status=run_complete_pending_eval`，**没有重新计算**（避免浪费已完成的 41 分钟 GPU 时间）。
- 用户要求把 gpu7 卡住的另外 3 例在本机（ditg-12）3 张卡上并行补跑：`pkill` 本机 GPU-filler 释放 8 卡后，从共享 `gpu7.tsv` 里各抽出 1 行写成独立单行 shard（`results/E189/s6_downstream/cem/shards/full_recovery/gpu{0,1,2}.tsv`），`nohup` 启动 3 个 `run_cem_queue.py --gpu-id {0,1,2} --all`（PPID=1，已与终端分离，输出到 `logs/E189/cem/full_recovery/gpu{0,1,2}.log`），全部确认已进入真实 CEM（`run_mjwp.py` 进程存活，1024×32 预算）。完成后需要手动把这 3 行的最终状态合并回共享 `gpu7.tsv`/`cem_full_manifest.tsv`（尚未做，因为还没跑完）。
- 当前 43 例总状态：30 完成（jzsy-11 的 29 + 本机恢复的 1）、3 在 jzsy-11 上 running（gpu0/1/2 各卡自己队列里的当前 case，不是刚才说的那 3 条）、3 在 ditg-12 上刚启动、其余按各自队列排队。两台机器都在同时往同一份共享 manifest/shard 文件写状态，后续合并需要注意不要用旧内容覆盖新写入。

### 2026-08-07（本机 ditg-12 复查，Phase 4/5 收口）

- 本机 `full_recovery` 的 3 条（`box001_20231020_010_p1`/`box001_20231003_1_042_p2`/`box001_20231023_108_p2`）均已 `run_complete_pending_eval`，`Final object tracking error` 正常写出，无 NaN。gpu7 因 NFS `OSError` 中断的 `box024_20231011_028_p2` 复核为**实际已算完**（`Total time: 2470s` 且 outdir/result npz 大小一致 8671140 bytes），gpu7.log 里的 traceback 只是 copy 步骤的瞬时故障，不代表计算丢失。
- 43 条 Full manifest 中 **42/43 已有 `Final object tracking error` 终止行**；仅 `box001_20231003_2_037_p2` 仍在 jzsy-11 上运行（本机 `ps`/`nvidia-smi` 看不到该机进程，只能靠共享 NFS 日志 mtime 判断），进度正常（无 traceback），预计还需约 20 分钟。
- 用户要求：全部完成后 (a) 渲染成视频 (b) eval 并与 E172/E173 PRG 版本对比 (c) 输出 xlsx 表格 (d) 写实验 log (e) 注册进 viser 3D 可视化界面。已按 experiment-planning-zh 规范创建 TaskList 拆解为 5 步（eval runner+wrapper → xlsx/md/json 报表生成器 → 43 条 Full 渲染+paired 对照视频+关键帧复核 → viser 注册 → 结果 log/265 + 更新 tracker），计划 215 中 Phase 4/5 已预先定义好指标口径（十二门 gate、按物体独立结论、E172/E173 baseline 已在 Phase 0 算好），不需要重新规划口径，只需照做。
- **最后 1 条已完成**（`Final object tracking error: pos=0.1391, quat=0.1113`），43/43 `result_npz` 全部存在。已实现并跑通 `eval_E189_boxes_e167a_vs_prg.py`（+ wrapper + `gen_E189_boxes_comparison.py`），43/43 evaluated、516/516 gate cells、status=pass。**三物体结论明确分化**：box004 `NO_PRG_NONINFERIOR`(2/6→2/6)、box024 `PRG_BETTER`(2/9→0/9，lower_body 6/9→显著变差)、box001 `NO_PRG_NONINFERIOR`(5/28→6/28，略有改善)。三物体 lower_body gate 无一例外全部退化（-2/-6/-4），与 PRG 设计初衷（下肢防穿透）一致，物理上可信；box024 是三者中唯一净回归明显的，符合"大箱更依赖 PRG"的假设方向，但 box001（体积比 box024 更大）却是 NONINFERIOR，说明"随尺寸单调退化"假设在 no-PRG 侧不完全成立。box023(E179)=`PRG_BETTER` 引用不重跑。
- 已把 E189 加入 `scripts/eval/review/review_index.py` 的 `DEFAULT_EXPS`；额外补写了 review-player 兼容用的 `summary.json`（原始权威文件是 `e189_eval_summary.json`），`--check` 最终显示 `43 indexed / 43 evaluated / 8 numeric_pass / 43 playable`，无 mismatch。
- 渲染（Phase 5）委托后台 agent 实现 `scripts/experiments/E189/render_paired_results.py` + `scripts/launch/active/run_E189_render_all.sh`：复用 E168 的 `render_row` 底层渲染函数（fps/编码器与 E172/E173/E174/E179 一致）+ E179 的 ffmpeg hstack paired 拼接。**环境坑**：渲染阶段（需要真正出 RGB 帧）不能像 CEM 阶段一样 `unset MUJOCO_GL`，必须显式 `MUJOCO_GL=egl`（`unset` 会让 `mujoco.Renderer()` 因缺 `DISPLAY` 抛 `FatalError`；CEM 阶段 `save_video=false` 不经过 Renderer 所以 unset 没问题，两个阶段的 GL 需求不同，不要混淆）。最终 43/43 self + 43/43 paired 全部渲染成功，0 失败、0 近零字节文件，`render_manifest.tsv` 全部 rendered/existing，无 missing。总大小 self 80MB + paired 16MB，总耗时约 67 分钟（box001 是瓶颈，28 条）。
- **用户反馈重做 xlsx**：初版 xlsx（Summary + 3×paired 全字段 + gate_matrix + case_metrics 共6个sheet）用户认为不够清晰。已按用户明确规格重写 `gen_E189_boxes_comparison.py::write_xlsx`：砍掉 gate_matrix/case_metrics 两个原始 dump sheet，每个物体一个 sheet（`box004`/`box024`/`box001`，不再叫 `{obj}_paired`），列只保留 `case_id`+PRG/E189 是否通过+两版失败模式+12个门各自(PRG值/E189值/Delta)三列一组（表头按门名合并跨3列，Delta 列表头用橙色`EA580C`区分于蓝色`2563EB`的其余表头）。**Delta 定义按用户要求改为 `PRG − noPRG`**（与内部 `delta_{metric}`/`improvement_{metric}` 的 `E189−PRG` 方向相反，只在这个 xlsx 展示层做了符号翻转，`e189_vs_prg_paired.tsv`/`summary.json` 里的原始 delta 定义不变，避免影响其他已经引用旧符号的地方）。fall 门没有连续 metric（PAIR_METRICS 不含它），改为从 baseline tsv 和 `e189_case_metrics.tsv` 各自读 `fall_flag` 转 0/1 现场拼进去，不需要重跑 eval runner。
- 视觉复核：用 ffmpeg 对 11 条 pass-migration case 中的 6 条 paired 视频抽取 10/50/90% 关键帧人工核对。`box024_20231011_028_p2`（PASS→FAIL, lower_body）在 50% 蹲姿帧有非常清晰的腿部穿箱证据，强支持 box024 的 `PRG_BETTER` 数值结论；其余 case 差异更细微但方向一致；43 条视频均无渲染损坏/reward hacking 迹象。剩余 5 条 box001 migration case 仅做了文件完整性检查，未逐帧复核（记录为已知局限，见 log/265 §9）。
- 结果 log 已写入 [265_E189_box004_box024_box001_e167a_no_prg_vs_prg_results.md](../log/265_E189_box004_box024_box001_e167a_no_prg_vs_prg_results.md)，涵盖完整 Summary/Setup/Method/Metrics/Results/Interpretation/Conclusion/Claims verification(C0-C6全部PASS)/Limitations/Next steps/Reproducibility。Tracker 与 log INDEX 均已更新。E189 至此收口：box004/box001 维持 PRG default（non-inferior 不足以升级），box024 明确保留 PRG，三者均不进入 RL export。
