# CORE4D E182 v9 完整过程备份

> 完整备份：
> [E167–E179 初始恢复](progress_archive/E167_E179_20260724_full_backup.md) ·
> [E179 执行过程](progress_archive/E179_20260724_execution_full_backup.md) ·
> [E179 最终收尾](progress_archive/E179_20260725_closeout_full_backup.md) ·
> [E180–E181 完整执行](progress_archive/E180_E181_20260731_full_backup.md) ·
> [E182 Gate0–S1 canary](progress_archive/E182_gate0_s1_canary_20260801_full_backup.md) ·
> [E182 S1 continuation](progress_archive/E182_s1_continuation_20260801_full_backup.md) ·
> [E182 S2 v2–v8 完整过程](progress_archive/E182_s2_v2_v8_20260801_full_backup.md)
>
> 本文件只保留 E182 当前权威状态与下一执行入口。

## 2026-08-01：不可变执行口径

- 权威计划：`plan/200_E182_task_conditioned_coacd_full_cem_plan.md`。
- Full 与 E178 exact：27 cases、`seed0, 1024×32`；K只允许`8/16/32`，不测K4。
- heldout24 在 production SHA 冻结前 selection-forbidden，冻结后只作 evaluation；
  禁止按 heldout/Full 结果反选碰撞体。
- Full 资源只用本机单卡 + `spider-remote` RTX 6000 Ada GPU0/1；允许与现有任务
  叠加，禁止 kill、暂停、抢占或修改已有进程，不使用A100。
- bucket003 完整 P/R/G launch floor 未闭合前，grid-SDF、heldout和Full全部禁止。

## E182 已完成阶段

- Gate0 authority/preflight PASS；Gate1真实P/R/G query tape PASS，见log247。
- E181原global cavity `≤0.1%`已降为report-only；E182使用882-pose真实static P authority。
- v5 global threshold：`0/9 P PASS`。
- v6 per-segment threshold hybrid：K8/K16/K32各`0/6561 P PASS`。
- v7 segment3 Bell(4) partition：K16/K32各`0/15 P PASS`；只产生
  `TP18/phantom8/missed9`与`TP19/phantom10/missed8`，见log248。

## v8 task-aware local pre-segmentation：完整负结果

- plan200 amendment冻结family：
  `2 task-derived planes × threshold {5,10,20mm} × K {16,32}=12`；
  每row为9个nonempty segments，禁止跨presegment merge。
- dominant axis=`x`；planes=
  `-0.14311002844145554m/-0.11878629238288715m`；两split的volume delta=
  `4.77535e-11/-1.27153e-12m³`。
- pre-freeze closure：v8 contracts14、parent matrix60，共`74/74 PASS`；ruff/format/
  compileall/diff-check GREEN；freeze前v8 root为空。
- protocol SHA：`d1b19069c4be0b7f4c33884ca7aab6539ad9080ce0624a84d8eb78cf9e45fdd8`。
- 12/12 new CoACD children、6/6 composite bases、12/12 candidates BUILD_PASS；
  actual hulls=`[16,32,16,32,16,22]×2`，所有part vertices≤256。
- static P=`0/12 PASS`；aggregate SHA=
  `10666ec4d61668b528838cdab7cef9e8cb5748c2a884b2b10a827d7d694df60b`。
- 最佳near-miss：plane1/t010/K32(actual32)=
  `TP18/phantom7/missed9, precision=.720, recall=.667`。
- TP≥19的最低phantom点：plane1/t020/K32(actual22)=
  `TP19/phantom12/missed8, precision=.613, recall=.704`。
- frozen stop action：`STOP_V8_NO_PLANE_OR_FLOOR_CHANGES`；完整P/R/G未启动。
- 详细表、耗时、Claims与路径见
  `log/249_E182_bucket003_task_aware_preseg_v8_results.md`。

## 可视化实际观察

- post-result renderer固定best-balance/high-recall/TP19三代表，输出6张3D/2D图；
  visual manifest SHA=`1d1ce64ea6e97f44476dc4580c4511cf0eb6b9efe600e877c44fcf9e9558a5d3`。
- 已查看全部原分辨率图：最深phantom均为reference pose148，集中在同一顶部端部rim
  手部点；oracle clearance≈`+0.465mm`，candidate侵入`-4.01/-4.37/-4.77mm`。
- high-recall把TP提到22时phantom也升到22；best-balance降到7 phantom但漏9 TP；
  TP19仍需12 phantom。问题是局部rim convex外包trade-off，不是远处body/leg噪声。

## 当前状态与下一入口

```text
P_LAUNCH_FLOOR_NOT_CLOSED
FULL_NOT_AUTHORIZED
V8_STOPPED_COMPLETE_NEGATIVE_RESULT
```

- 禁止继续移动plane、加手工cut、降低0.70 floor或强送Full。
- v5–v8已超过三次失败协议；新的方法自由度需要用户确认并先写新plan/protocol。
- 可供决策的结构方向：
  1. 新协议同时使用两条自动plane，隔离三个transition x区间；
  2. rim-aware非平面分区/局部原mesh collision primitive；
  3. 改用MuJoCo SDF/plugin等更高保真P碰撞路线。
- 本轮没有访问heldout/grid、没有启动GPU/Full，也没有操作任何既有进程。

## 2026-08-01：v9 只读可行性审计启动

- 持续目标仍是完成 plan200，不把 v8 阶段闭合误写成 E182 完成。
- 按三次失败协议，先只读验证“同时使用两条已冻结自动plane”的 topology：
  watertight/winding/positive-volume、总体volume closure、最小hull数与候选规模。
- 本步骤不构建新CoACD、不计算新P、不访问heldout/grid、不启动GPU/Full；审计结果
  仅用于向用户提交新方法自由度的可执行证据，不能视为v9已获授权。
- Tracker恢复确认：E182权威状态仍为Gate0/Gate1 PASS、bucket003 v5–v8无候选、
  Full禁启；E178 authority仍是后续27-case paired baseline。
- plan200恢复确认：v8在static P `0/12`时必须停止；进入S3的必要条件仍是新候选
  先通过完整P/R/G launch floor、真实MuJoCo replay和3D/2D review。Full资源口径为
  本机单卡+Ada0/1叠加运行，当前审计不得越过这些gate。
- log248/249复核确认：v7只有`18/8`与`19/10`两种TP/phantom签名；v8单plane
  最佳平衡为`18/7`，而TP19最少仍需12 phantom。新审计必须同时使用两条冻结plane，
  不能继续移动cut、降低0.70门或重复threshold/merge sweep。
- v8 artifact/code只读检查确认：两条plane、source segment3与exact manifold boolean
  split函数均可直接复用；v9 feasibility只需把同一source按两个有序plane切成3个child，
  不需要运行CoACD或读取任何P score。
- 两plane严格有序且均位于同一source segment3内部：`p0=-0.14311002844145554 <
  p1=-0.11878629238288715`；共同source SHA=`96245bff...160b2`、volume=
  `0.014084007759406233m³`。下一步只对三个区间做exact intersection与closure复算。
- 双plane exact-manifold只读复算完成：3/3 children均watertight、winding-consistent、
  positive-volume；体积=`0.005048199531/0.001739451098/0.007296357166m³`，总体
  volume delta=`+3.58420e-11m³`，通过`1e-8m³`门。
- 独立用两份已冻结v8 split manifest做nested-volume差分交叉校验：source SHA一致，
  middle=`0.001739451061m³`，closure delta=`-1.27153e-12m³`，同样PASS。
- 因此可行的v9 topology为7 unchanged+3 children=`10 segments`，no-cross-segment
  merge下最小hulls=`10`；K8结构上不可能，预注册family应为3 thresholds×K16/K32=
  6 candidates。本轮CoACD/P/GPU/heldout访问仍为`0/0/0/0`。
- 复用审计attempt1误把目录写成`bases/`，实际冻结路径为`coacd_bases/`，只读命令
  立即No such file退出、未产生或修改artifact；后续固定使用实际manifest路径，不重复
  错误glob。
- transition isolation复核PASS：TP pose177落region0，两条phantom pose203/202分别落
  region1/2；两条plane确实把三个触发点一一隔离，而不是只增加无语义segment。
- v9两侧outer child与冻结v8几何在vertex/face/bounds/volume上float-roundtrip exact
  （max vertex diff=`6.94e-18m`）；每个threshold可复用7个v4 segment base和2个v8
  outer child base。若获批，仅middle child需`t005/t010/t020`共3次fresh CoACD，另有
  6份v8 child base复用；假设middle返回soft-cap4，base totals预计`40/36/26`。
- v9 feasibility audit至此闭合，repo `diff --check` PASS；仅progress产生39行记录。
  下一gate是用户确认“双冻结plane同时使用”这一新topology，之后才允许写plan
  amendment/protocol并构建6个candidate；当前仍为`FULL_NOT_AUTHORIZED`。

## 2026-08-01：等待v9确认期间的plan200实现缺口审计

- 按`experiment-planning-zh`恢复流程继续推进，但不把“持续推进”解释为越过三次
  失败协议。当前只核对S3–S7 planned artifacts、公共代码和launcher合同，区分可独立
  准备项与必须等P gate闭合的项。
- plan文件盘点：S0/S1 authority/query功能已由`build_authority.py`、`build_prg_query_tape.py`
  等拆分实现，S2 evaluator存在；S3–S7计划中的`bake_canonical_sdf.py`、production
  selector、scene sidecar、allocation/queue、paired render/completion audit、正式eval/report/
  watcher均尚不存在。
- 公共`spider/geometry/grid_sdf.py`尚不存在；query-tape plumbing已在config/mjwp/sampling
  落地。local/remote/pull launcher对未实现的task-audit/freeze/full均fail-closed，且Ada
  allowlist/叠加/no-delete合同存在；这说明当前Full无法被误启动，但下游实现工作量仍大。
- 远程规范复核：E182使用canonical `launch/active/`、独立run root、Ada0/1串行queue和
  scoped pull的方向正确；通用指南中的手动`tmux kill-session`不适用于本实验，用户的
  no-kill约束优先，当前E182 tests也显式禁止该命令。
- 现有`test_runtime_inputs.py`覆盖local/remote/query-worker/pull的Ada/no-kill合同，但
  尚无未来Full allocation/queue/watcher的contract tests。正式实现这些脚本时必须先补
  RED tests，不能用现有S1 tests外推Full安全性。
- repo-wide复用搜索未找到现成grid-SDF/trilinear backend；E181只留下计划合同，没有
  `grid_sdf.py`或E181 bake/backend tests可直接复用。plan199可作为S3详细规格来源：
  negative-inside、50mm margin、10→5→2.5mm、float32 authority、OOB explicit fail、
  `D_lower=D_C-epsilon_grid`及hull SHA三方一致。
- 因当前P gate未闭合，本轮不据此实现/bake grid；上述规格只登记为未来S3验收项，
  避免把“找到设计文本”误报为backend已完成。
- S5–S7可复用源审计：E179提供最新的resume-safe queue、paired ffmpeg renderer、
  completion audit与conservative double-absence watcher；E176提供multi-geom compiled pair
  contract。未来E182可复用其结构，但必须改成27-row、local+Ada0/1、PRG-on、canonical
  SHA和no-kill语义，不能直接复制E179的A100/16-row/no-PRG假设。
- E176 runner通过`importlib`复用旧queue；E182应抽取/显式import公共逻辑并为自身合同写
  direct-main tests，避免把历史实验的隐式常量带入Full。
- plan200下游缺口审计闭合，`diff --check` PASS；当前progress为166行、仍低于200行
  归档阈值。审计没有创建S3–S7文件或结果，也没有改变`FULL_NOT_AUTHORIZED`状态。

## 2026-08-01：持续目标 blocked audit

- 同一阻塞条件已连续出现3个goal回合：v5–v8超过三次失败协议，v9双plane属于新
  topology，而用户尚未明确批准三条候选路线中的任何一条。
- 已穷尽不扩大方法自由度的安全进展：v9几何/复用可行性审计与S3–S7实现缺口审计均
  已闭合。继续构建candidate、写amendment或进入grid/Full都会越过冻结stop action。
- 因此持续目标暂时标记blocked，解除条件是用户明确回复批准v9双冻结plane，或选择
  rim-aware / MuJoCo SDF/plugin路线；当前仍禁止CoACD、P、heldout、GPU、grid与Full。

## 2026-08-01：用户批准v9，持续目标恢复

- 用户明确回复“批准v9”；新topology授权已满足，不再受前述blocked条件约束。
- 授权范围严格为：同时使用两条既有冻结plane、10 segments、threshold
  `{5,10,20mm}`、K`{16,32}`、6 candidates、static P门`TP≥19 AND phantom≤8`。
- 下一步先写plan200 amendment，再以TDD冻结独立v9 protocol；在protocol freeze前不得
  产生新CoACD/P分数，v8 builder与既有artifact SHA继续只读。
- v9 pre-amendment preflight：结果根与v9源码均不存在；v8 builder/evaluator/v5 evaluator
  SHA=`1d16d4ed...777db/a31eb7e4...fd93/67783903...4560`，v8 protocol/build/static
  SHA仍为`d1b19069...45fdd8/3c1d8d80...e057f/10666ec4...df60b`。这些值将作为
  v9 parent bindings，后续禁止修改v8文件。
- plan200 v9 amendment已写入并通过`diff --check`：固定3-child exact topology、
  10 segments、6-row family、3次fresh middle CoACD、parent/reuse SHA、V9-C1–C6及
  static/完整launch两级stop-go。此时仍未创建v9源码/结果或运行新分数。
- v8接口/14项direct-main tests已复核：可复用transition atlas、static P数学、v5
  reducer与artifact validator模式；v9必须新增double-plane split、10-segment remap、outer
  base fingerprint/reuse、middle-only CoACD和独立resume/tamper合同，不能修改v8实现。
- v9 TDD RED已完成：新增8项direct-main contracts，覆盖double-plane topology、6-row
  family、10-segment remap、21+6 reuse、static P、parent/source binding与protocol
  contamination/tamper/resume；首次运行按预期在`ModuleNotFoundError:
  build_task_aware_preseg_v9`失败，证明测试先于实现且未创建结果root。
- v8 protocol/reuse实现复核完成：`protocol_payload()`纯计算、`freeze_protocol()`要求
  non-protocol root为空并exact resume；v4 reuse逐manifest/part SHA验证。v9将沿用该
  fail-closed模式，并额外直接绑定v8 protocol/build/static/visual与outer geometry fingerprint。
- v9 protocol foundation实现后8/8 GREEN：double-plane 3-child、transition isolation、
  6-row family、10-segment remap、21个v4+6个v8 outer reuse、static P floor、parent/code
  bindings与nonempty/tamper/resume全部PASS。正式v9 root仍未创建，尚未freeze protocol。
- pre-freeze静态检查attempt1发现2个`zip(strict=)`、3个public docstring和1个unused
  import；均为lint问题、未执行protocol/artifact。已机械修正，不改变topology/family/math。
- formatter attempt显示2文件需机械格式化；已执行ruff format。随后v9 tests`8/8`、ruff
  check/format与repo diff-check全部GREEN，正式root仍为空。
- pre-freeze parent matrix批跑已明确完成core/v1–v8主合同前10组共`49/49 PASS`；统一
  exec在进入`test_task_aware_preseg_v8_visual.py`时未返回该组输出且exit code未定义，
  因此不把visual/v9计入本次结论。下一步单独重跑剩余2组，避免用不完整runner结果
  宣称全矩阵GREEN。
- 剩余v8 visual=`2/2`与v9=`8/8`已单独自然完成、exit0；pre-freeze functional matrix
  合计`59/59 PASS`。前次undefined是统一runner会话输出问题，不是测试失败。

## 本次归档

- 原`progress.md`的1260+行完整过程已移动到
  `progress_archive/E182_s2_v2_v8_20260801_full_backup.md`；未删除过程证据。
- Tracker已链接log249，log INDEX已包含249且文档audit PASS。
- 最终closure已重跑PASS：v8=`14/14`、visual=`2/2`，4文件ruff/format/compileall、
  strict protocol/static/visual validator、log链接、Tracker/INDEX与repo diff-check全GREEN；
  三个最终SHA保持`d1b19069...45fdd8`/`10666ec...df60b`/`1d1ce64e...8a5d3`。
- v8阶段已完整结束为负结果；下一方法超出当前冻结family，必须先由用户确认结构方向，
  再新增plan amendment/protocol。当前不满足Full启动条件，持续目标尚未完成。
- 当前工作计划5/5步骤均已完成（包含0/12 stop分支）；这只代表v8阶段闭合，不代表
  plan200的C0–C9或持续目标完成。等待用户在“两plane同时使用 / rim-aware非平面 /
  MuJoCo SDF/plugin高保真P”中确认新的结构自由度。

## 2026-08-01：v9批准后的执行恢复

- 用户再次明确“批准v9”，确认继续执行已写入plan200的双冻结plane方案；此前blocked
  条件已解除，不创建新goal，也不扩大到rim-aware或MuJoCo SDF/plugin路线。
- 已完整恢复`experiment-planning-zh`、Tracker、plan200、最新log与progress；当前正式
  v9结果root仍不得在builder完成和pre-freeze验证前创建，protocol也仍未freeze。
- 当前实现边界复核：v9协议骨架8/8、pre-freeze parent matrix累计59/59 PASS；下一步
  必须先新增segment export、middle-only CoACD、composite/candidate、static-P及
  resume/tamper/no-op的RED tests，再补完整builder。
- 冻结约束保持：只构建3个middle bases和6候选；static P为`TP>=19 && phantom<=8`；
  若`0/6 PASS`立即`STOP_V9_NO_MORE_PLANES_OR_FLOOR_CHANGES`。在完整P/R/G launch
  floor前禁止grid/GPU/Full，heldout在production SHA freeze前禁止访问。
- 首轮实现盘点确认git仅含已知4个v9相关未提交文件，正式v9 root=`ABSENT`；v9 builder
  当前476行并止于protocol CLI，测试仅8项protocol-foundation合同。v8 static-P的
  9-segment scene grouping、882-pose confusion校验、aggregate严格resume模式可移植，
  但v9必须改为10 segments、6 candidates及v9 stop action，不能直接沿用v8常量。
- v8完整构建接口已定位：segment manifest/summary、isolated child CoACD subprocess、
  composite base、segment-preserving reducer、candidate/build summary、static-P result/
  aggregate均有严格SHA与resume validator。v9将保持同一artifact层次，但拓扑简化为
  单个3-child manifest、每threshold仅segment4 fresh、7个v4+2个v8 outer复用，且所有
  candidate必须覆盖segment `0..9`、hull数处于`10..K`。
- v9 TDD已扩为12项：新增exact 3-child export、middle-only 3-call、10-segment composite、
  6-candidate reducer/tamper、static-P `0/6` stop与validation-only resume合同。首次RED
  attempt未进入新增API缺失断言，原因是默认`python`环境缺少`trimesh`；这是解释器
  选择问题、未创建任何正式artifact。下一步恢复此前59/59所用的项目Python后重跑RED，
  不通过安装包或改变依赖来掩盖环境差异。
- 环境只读盘点：默认解释器为miniconda base Python3.13；已有`cari4d`、`hy3d`、`sugar`
  均可import trimesh，其中`cari4d`为trimesh4.7.0。先验证`cari4d`是否同时具备manifold/
  Open3D/CoACD及既有v9 8项合同所需依赖，再固定同一解释器执行后续RED/GREEN。
- `cari4d`进一步检查缺`mujoco/manifold3d/coacd`，测试导入链在`mujoco`处退出；其余现有
  conda env也没有完整E182依赖。该问题仍是解释器定位，不是v9代码失败；没有安装或
  修改环境。下一步检查repo内venv/uv及历史launch shebang，找到之前实际执行59/59的
  完整环境后再进行有效RED。
- 已确认权威环境为repo `.venv/bin/python`（trimesh4.11.5、MuJoCo3.7.0且CoACD/
  manifold可用），与历史E182 launcher一致。有效RED已完成：原8项继续PASS，第9项
  在预期的`AttributeError: v9.build_segments missing`失败；正式root仍ABSENT。现在进入
  GREEN实现，不能修改新增测试去绕过缺失API。
- v9 GREEN实现首版已落地：3-child segment export、segment4-only CoACD子进程、三份
  10-segment composite、K16/K32 reducer、6-row build summary、10-scene static-P与严格
  aggregate stop合同均已实现；CLI扩为6个显式stage。每层已有artifact均走SHA/identity/
  topology validator，不存在隐式fall-through。尚未跑GREEN，正式root仍未freeze/创建。
- 首次静态预检：两文件format与py_compile PASS；ruff仅报测试文件`Iterator`应从
  `collections.abc`导入，已机械修正。该轮未执行正式protocol/build，下一步运行12项
  GREEN以暴露真实pipeline合同问题。
- GREEN attempt1通过前9项，在fake CoACD导出的convex hull触发`max_vertices=32`合同；
  根因是测试替身可能生成超过32顶点，非production builder缺陷。已把假分解改为8顶点
  bounding-box convex part，仍保留“每次只返回1个合法凸体”的测试语义；正式root未创建。
- GREEN attempt2已完成3次fake middle并进入base-summary validator；发现本地
  `_require_artifact_reference`错误要求引用dict只能含path/SHA，拒绝了合法的threshold
  元数据。已按v8权威模式修为只严格核对path/SHA、允许调用层附加身份字段；这是实际
  resume validator修复，不放宽SHA约束。
- v9全链路GREEN完成：12/12 PASS。临时pipeline验证3次middle-only调用、三份10段
  composite、6个K16/K32 candidate、candidate manifest tamper拒绝、fake static-P `0/6`
  正确冻结`STOP_V9_NO_MORE_PLANES_OR_FLOOR_CHANGES`且二次调用validation-only no-op。
  所有临时root已自动清理，正式v9 protocol/result仍未创建。
- pre-freeze静态复核：v9两文件ruff check/format、repo diff-check全PASS；正式root仍
  ABSENT，git范围仍为plan/progress/v9 builder/test四文件。builder现1666行，虽沿用v8
  单文件artifact-authority模式但需要重点审计validator与stage边界；下一步重跑完整parent
  functional matrix，再做人工代码审查与source-SHA最终确认。
- pre-freeze parent matrix当前前9组自然完成`40/40 PASS`：exact-C real evaluator 2、
  CoACD v1–v5为3/3/3/4/4、v6=3、v7=4、v8=14。统一runner在进入v8 visual时
  返回后台session72767且尚无exit code；暂不把visual/v9计入，下一步只续接同一session。
- session72767已自然完成exit0：v8 visual=`2/2`、v9=`12/12`；本次完整parent+v9
  functional matrix合计`54/54 PASS`。无测试进程残留、正式v9 root仍ABSENT。下一步
  逐项核对plan命令、parent SHA、stage validator与diff，修完任何审查问题后必须重新跑
  54项和静态矩阵，才允许freeze protocol。
- parent SHA审计确认v8 builder/exact-C/protocol/build/static/visual均与冻结值exact；同时
  发现plan要求绑定“v5 reducer/evaluator源码”，当前v9仅绑定实际reducer builder
  `aa939806...c5095`，尚未显式绑定v5 query evaluator `67783903...304560`。这是
  pre-freeze protocol completeness缺口；下一步先加RED断言，再新增独立source role，不能
  用v8间接依赖替代plan明示绑定。
- v5 evaluator显式绑定的新增TDD RED已完成：旧前5项PASS，protocol dependency role
  列表按预期AssertionError，证明现有payload确实缺该authority。测试同时冻结期望SHA
  `67783903...304560`；下一步只补source import/role，不改变任何P/reducer数学。
- 已新增`V5_QUERY_EVALUATOR_AUTHORITY`独立source role，v9全链路重新`12/12 PASS`；
  reducer builder与query evaluator现在分别显式绑定，不再混称一个SHA。继续protocol审计
  又发现plan要求“两份v8 split manifest”与source segment manifest直接绑定，当前payload
  仅通过outer segment/base与v8 parent间接覆盖；下一步同样先补RED精确断言再完善topology，
  正式root仍ABSENT。
- source/split direct-binding TDD RED已完成：前5项PASS，protocol test在预期的
  `KeyError: topology.source_segment_manifest`失败；测试同时要求split manifest按plane0/1
  顺序和实时path/SHA exact。下一步只增加protocol metadata，不改变几何或family。
- 已在topology中直接加入v1 source segment manifest及v8 plane0/1 split manifest引用，
  v9全链路再次`12/12 PASS`。继续复核reuse fingerprint发现当前“face exact”只比较
  face_count，尚未比较三角面坐标集合；这弱于plan的vertex/face/bounds/volume exact口径。
  下一步补face-coordinate canonical比较的RED/GREEN，避免不同三角化被误认为outer exact。
- outer face fingerprint的TDD RED已完成：前三项PASS，reuse test按预期在缺失
  `sorted_face_vertex_max_abs_m`处KeyError。下一步实现对每个三角形内部顶点排序、再对
  triangle rows排序的坐标canonicalization，并把该delta纳入exact gate。
- 已实现canonical triangle-coordinate fingerprint并纳入outer exact gate，v9再次
  `12/12 PASS`；真实6份outer evidence均在`1e-15m` tolerance内face exact。继续对照v8
  isolated-CoACD合同发现v9 base-summary目前允许fresh middle manifest无native log，弱于
  v8的`require_native_log=True`。下一步补RED确保正式build-bases拒绝非隔离/无日志child，
  测试正路径则用明确的fake isolated runner生成日志引用。
- isolated-middle新增TDD RED已完成：原正路径用fake native.log保持PASS，新第11项用
  direct in-process runner构建3份无日志middle，当前builder错误接受并触发预期
  `AssertionError: nonisolated ... accepted`。下一步把base-summary对三份middle全部改为
  `require_native_log=True`；单独`build-middle-base` CLI仍只作为父进程子命令，不可独立
  满足complete base stage。
- base-summary已强制3/3 fresh middle native-log SHA，新增nonisolated拒绝合同通过；v9
  全链路现`13/13 PASS`。下一步补齐summary层最后两个审计点：完整candidate stage二次
  调用字节级no-op，以及static aggregate tamper拒绝；这两项应由现有validator直接满足，
  若不满足则继续RED/GREEN，不提前freeze。
- 补测patch attempt1因ruff后的单行格式与旧context不符而verification fail，未修改文件；
  已读取精确局部后改用窄context重试，不重复原patch。
- candidate build-summary字节级resume no-op与static aggregate tamper拒绝已纳入现有测试，
  v9仍为`13/13 PASS`。至此segment/base/candidate/static四层均有validation-only resume，
  protocol/segment/candidate/static均有tamper拒绝，base additionally强制native logs。
  下一步执行最终source审计、完整54→55项matrix与静态工具矩阵；通过后才freeze正式protocol。
- 最终静态矩阵已PASS：ruff check/format、compileall、diff-check；正式root ABSENT。v9
  builder/test SHA=`0022ef4e...f7a7e/2d8124e0...5444fc`；protocol dry-run固定6候选、
  10 segments、5 source roles、3 fresh/21+6 reuse，outer canonical face max delta=
  `6.94e-18m`。最终functional rerun前9组再次`40/40 PASS`，visual/v9转入session71239，
  尚未计入最终总数。
- session71239已自然完成exit0：v8 visual=`2/2`、v9=`13/13`，最终pre-freeze
  functional matrix=`55/55 PASS`。parent SHA、正式root空、source SHA、静态工具与完整
  runtime contracts全部闭合；现在满足V9-C1 freeze前置条件，下一动作只能写正式protocol，
  写后先validation-only重读并核对SHA，再开始segment/CoACD。
- 正式v9 protocol已freeze并立即二次validation-only重读PASS；结果root当前唯一文件为
  `attempt2_v9_protocol_manifest.json`，SHA=`5ea0245b...acde62`。manifest固定6候选/
  10 segments及builder SHA `0022ef4e...f7a7e`；V9-C1完成。下一步先export/验证3个exact
  children，再运行3个isolated middle CoACD，不修改任何源码。
- 正式3-child export及二次validation-only resume均PASS；segment manifest SHA=
  `96e1b393...051cc`，3段顶点/面=`809/1642,254/504,356/708`，体积合计closure delta=
  `3.584199e-11m³ <=1e-8`。V9-C2几何部分完成；下一步执行恰好3个middle CoACD子进程，
  预期每个有独立native.log，outer/v4只读。
- real middle CoACD当前完成t005/t010，各4 hull、wall5.549s/3.308s；CoACD按soft-cap4
  发出residual concavity warning（0.04292>0.01741、0.05147>0.03482），属于预注册
  soft-cap结果记录，不能据此改cap或threshold。t020仍在同一session79532，尚不宣称base
  stage完成。
- session79532自然完成exit0：t020 middle=2 hull、0.428s；base stage=`BUILD_PASS`，恰好
  3份composite。真实middle hull签名为`4/4/2`，因此预计composite totals应为`40/36/24`
  （替代先验“若middle=4则40/36/26”中的t020假设）。下一步严格resume并核对3份native
  log、21+6 reused references与manifest totals。
- base stage二次validation-only PASS；summary SHA=`e9ae98e0...f6049`，totals确认为
  `40/36/24`。三份middle均`request_satisfied=true`且native-log SHA有效；每个composite
  恰有10 segments、source计数`7 v4 + 2 v8 outer + 1 fresh middle`。V9-C3完成，下一步
  使用冻结v5 reducer构建6候选并核对actual<=K/10段覆盖，不读取P分数。
- 6/6真实candidate BUILD_PASS并二次validation-only PASS；build summary SHA=
  `2d900e7d...7623b3`。actual hulls按family顺序为`16/32/16/32/16/24`，全部覆盖segment
  `0..9`且max part vertices=`74/54/69/55/67/32 <=256`；V9-C4完成。下一步仅在冻结
  882-pose dev3 authority上执行static P，结果决定立即stop或进入完整P/R/G，不做任何调参。
- real static-P已完成`0/6 PASS`，aggregate明确`pass=0/6`：t005 K16/K32 precision=
  `0.4783/0.5116`、recall均`0.8148`；t010=`0.5278/0.5758`、recall均`0.7037`；
  t020=`0.6000/0.6818`、recall均`0.5556`。因此执行冻结stop action
  `STOP_V9_NO_MORE_PLANES_OR_FLOOR_CHANGES`；禁止完整P/R/G、grid、GPU、Full、heldout及
  任何plane/floor救援。下一步只做aggregate resume/SHA审计和负结果log/tracker归档。
- static aggregate二次validation-only PASS，SHA=`87c40118...5a642c`；exact confusion rows=
  `22/24/5,22/21/5,19/17/8,19/14/8,15/10/12,15/7/12`（TP/phantom/missed）。
  v8 parent 5个关键SHA保持exact，正式root共175文件，heldout/grid/GPU/Full访问均0。
  下一步写新log250、更新Tracker/INDEX；不创建v9 visual/replay，因为预注册明确只有至少
  1行static PASS才进入该完整launch-floor阶段。
- 已新增log250，完整记录protocol/topology/build/static表、v8对比、Claims、SHA、停止项
  与“为何不运行v9 visual/replay”的protocol解释；Tracker E182首行更新为v9双plane
  6/6构建、static P 0/6、v5–v9无候选、Full禁启，并链接log250。下一步重建log INDEX、
  文档/链接/Tracker描述审计，再决定git归档范围。
- log INDEX已重建为249个实际日志文件并包含250号条目；Tracker description=35 chars，
  满足≤80限制，repo diff-check PASS。results/E182位于repo symlink外，正式175个artifact
  不属于git worktree，沿用v8相同存储策略；git归档只包含plan/progress/log/index/tracker与
  v9 builder/tests。下一步按progress>200规则归档本轮详细过程并保留精简权威摘要。
