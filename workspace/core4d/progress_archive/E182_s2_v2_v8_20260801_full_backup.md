# CORE4D E182 S2 v2–v8 完整过程备份

> 完整备份：
> [E167–E179 初始恢复](progress_archive/E167_E179_20260724_full_backup.md) ·
> [E179 执行过程](progress_archive/E179_20260724_execution_full_backup.md) ·
> [E179 最终收尾](progress_archive/E179_20260725_closeout_full_backup.md) ·
> [E180–E181 完整执行](progress_archive/E180_E181_20260731_full_backup.md) ·
> [E182 Gate0–S1 canary](progress_archive/E182_gate0_s1_canary_20260801_full_backup.md) ·
> [E182 S1 continuation](progress_archive/E182_s1_continuation_20260801_full_backup.md)
>
> 本文件只保留 E182 当前可靠结论与下一执行入口。

## 2026-08-01：不可变口径

- 计划：`plan/200_E182_task_conditioned_coacd_full_cem_plan.md`；Tracker 为
  `✅ Gate0 PASS；🚧 S1 query tape`。
- Full authority 与 E178 exact：27 case、`1024×32 seed0`；K 只测
  `8/16/32`，不测 K4。
- heldout24 在 production SHA 冻结前 selection-forbidden，冻结后仅
  evaluation-only；不得按 heldout/Full 结果反选碰撞体。
- 资源只用本机 GPU0 + `spider-remote` RTX 6000 Ada GPU0/1；允许与已有任务
  叠加，禁止 kill、暂停、抢占或修改已有进程，不使用 A100。
- 2026-08-01 用户再次确认 Full CEM 调度口径：远程改用两张 RTX 6000
  Ada，不再使用 A100；本机与远程均可与正在运行的程序叠加，任何
  E182 launcher 都不得 kill/暂停/抢占/修改现有进程。
- 已完成资源口径一致性审计：plan、Gate1 log、Tracker、progress 和 E182
  launch/runtime contract 均指向本机 GPU0 + Ada GPU0/1；脚本无
  `kill/pkill/killall/tmux kill-session` 路径。文档中 `A100` 只以“不使用/不套用”
  的禁止性说明出现，不存在 A100 launch 命令。
- 本次上下文恢复后再次用全文检索复核上述口径，未发现 GPU3/6/7 或 A100
  launcher 残留；第一次追加 progress 的 patch 因标题实际为“CORE4D 当前进度”而
  context mismatch，未修改文件，随后改用现有段落记录。
- 调度合同复验：`test_runtime_inputs.py`=`4/4 PASS`，本地/远程 launcher
  `bash -n` PASS，`git diff --check` PASS；本次没有启动、停止或修改任何运行中任务。

## S0：Gate0 PASS

- authority=`27/3/24`，case snapshot files=`108`，E181 candidates=`54`；E178
  manifest SHA=`de9a3d165301318049da208aad52b1f5bc4d3ed671631f0097958734a0f022a8`。
- 当前 remote immutable root：
  `/home/xiayb/pHRI_workspace/e182_runs/e182_832e7fe0f61193a1/spider`；source
  `5093 files`、SHA=`832e7fe0...`，50-file runtime layer 与 CoACD dependency
  layer均 exact PASS，未修改 shared checkout/venv/已有进程。

## S1：query tape 进行中

- E178 历史 NPZ 不含原始 1024-sample tensor；dev3 用 exact E178 override/task/
  seed 做 `64×4 seed0` shadow replay。selection 唯一使用 frozen `on_a`；`on_b`
  仅诊断；production Full 强制 recorder-off。
- Gate1 硬门：deterministic mock exact、default-off no-op、same-run payload exact、
  chunk/manifest/source SHA 完整。真实 CUDA 跨进程 divergence 仅 report-only。
- bucket007：off/on_a/on_b 完成；on_a/on_b=`77/77` chunks，same-run mismatch/
  nonfinite=`0/0`，selection content SHA=`ab73e0ee...`。
- bucket004：远程 worker 已自然 PASS 并退出；on_a/on_b=`139/139` chunks。
- bucket003：远程 worker 已自然 PASS 并退出；on_a/on_b=`202/202` chunks，same-run
  integrity PASS。bucket003/004 两个 E182 tmux 都是自然结束，全程未停止、重启或
  修改已有任务。
- pull 入口 `pull_E182_remote_a6000_results.sh query-tape` 为 no-delete +
  `--ignore-existing`，远程完成后执行 dev3 same-run audit。
- no-delete pull 已完成：50-file runtime layer SHA exact，missing/mismatch=`0/0`；本地
  dev3 aggregate same-run audit=`PASS rows=3`，pull=`PASS`。下一步读取三 case manifest/
  wall/content SHA 细项并构建 bucket003/004 factored PRG tape。
- 三 case same-run 细项均 mismatch/nonfinite=`0/0`：on_a/on_b chunks 分别为
  bucket003=`202/202`、bucket004=`139/139`、bucket007=`77/77`；selection content SHA
  分别为 `561ea037.../68378948.../ab73e0ee...`。本地结果约 `804MiB`，磁盘尚余
  `734GiB`；wall time 位于 case manifest 的 `rows[0]`，下一次按正确层级聚合。
- bucket003/004 完整 factored P/R/G tape builder 已启动，限定两个 dev case且仅消费
  frozen `on_a`，最终 `PASS rows=2`。三 case逐 chunk verifier 均 PASS：bucket003
  `204` chunks/`925.20MiB`，bucket004 `141`/`636.73MiB`，bucket007 `79`/
  `352.83MiB`；总计约 `1.9GiB`，均为 `18 P geoms/7 consumers/1669 points`，压缩比
  `12.83×`，raw selection SHA exact。
- wall 汇总读取 attempt1 在 bucket007 旧 manifest 缺 `wall_seconds` 时触发 KeyError；
  003/004 已正确得到 off/on_a/on_b=`660.28/660.56/662.00s` 与
  `474.98/480.84/479.62s`。下一步按 optional field 处理 bucket007，不重复硬索引。

## Object-local P/R/G tape

- `build_prg_query_tape.py` 从 frozen config+`load_data` 恢复 reference，复用 E027b
  43→42 conversion；E178-final 来自 NPZ `(record,ctrl_step,42)` flatten。
- P 保存完整 qpos 作 full-scene replay，并从 explicit pairs + masks恢复 18 个 physics
  robot geoms；R/G=`25` unique query geoms、`1669` fixed points。
- tape 使用无损 factored geom-pose + fixed-offset 表示。bucket007 已完整构建：
  `79/79 PASS`，stored=`352.8MiB`，expanded=`4.42GiB`，缩小 `12.83×`。
- Gate1 只有在 bucket003/004 拉回、两条完整 tape 逐 chunk验证、三 case aggregate
  artifact PASS 后才可宣布闭合。

## S2：Gate1 等待期间的合成准备

- 新增 `evaluate_task_queries.py` 与 synthetic tests；exact-C 使用 manifold boolean
  union 后再做 Open3D negative-inside SDF，避免内部 overlap face 错误缩短距离。
- radius-aware paired metrics显式报告 coverage/nonfinite、sign disagreement、deep
  mismatch、false accept/reject 与 SDF p90；合成重叠 box `3/3 PASS`。
- 尚未读取真实 candidate score；Gate1 PASS 后才在 frozen dev3 tape 上评估 54 个
  K8/K16/K32 candidate。

## 当前验证

- query recorder `3/3`、pipeline `4/4`、runtime/launcher `4/4`、PRG tape `3/3`、
  preflight `5/5`、S2 synthetic `3/3` PASS。
- E182 ruff/format、四个 launcher `bash -n`、`git diff --check` PASS。
- 2026-08-01 commit preflight 已用 authoritative `uv run python` 重跑全部 E182
  direct-main groups：Gate1=`2/2`、preflight=`5/5`、PRG tape=`3/3`、query
  recorder=`3/3`、pipeline=`4/4`、runtime/launcher=`4/4`、fixture=`2/2`、task
  geometry=`5/5`，合计 `28/28 PASS`；`ruff check`、`ruff format --check`、四个
  E182 launcher `bash -n` 与 `git diff --check` 全部 GREEN。

## 已记录错误

- 远程 chunk count attempt1 使用错误目录层级返回 0；已改为 `raw_chunks/mode/case`。
- manifest摘要 attempt2 使用远程不存在的裸 `python`；未影响任务，不再重复，改用
  frozen remote Python或拉回后本地审计。
- commit preflight attempt1 使用当前 `python -m pytest`，但本地 base Python 未安装
  pytest，命令在收集前立即失败，未执行任何实验/进程操作。后续改用项目已固化的
  direct-main test 入口，不重复同一 pytest 命令。
- commit preflight attempt2 用 base Python 直接运行 direct-main，在首个需要
  `mujoco` 的 import 处立即失败；根因是绕过了项目固化的 `uv run python`/
  `.venv/bin/python`。已从 E182 launcher 确认 authoritative 本地入口为 `uv run
  python`，下一次使用该环境，不再用 base Python 运行依赖测试。

## 下一执行入口

1. 实现 S2 oracle cache：对 dev3 factored tape 流式计算 `D_M`，保存有符号
   distance + source/fixture SHA，不保存展开 points。
2. 实现单 candidate screen evaluator：exact-C、radius-adjusted P/R/G、R shadow、
   G mask/fallback/elite 和 P pose-contact，并先跑 bucket007 固定 one-chunk bounded
   real test。
3. bounded schema/finite/count/SHA PASS 后，再跑 dev3 的 54-candidate screen，然后按
   预冻结词典序产生每 object×K finalist；仍禁止读取 heldout24。
4. 完成 finalist mesh256 复核与 production full1669 canary 后才进入 S3 grid-SDF。

- 2026-08-01 持续目标恢复审计：HEAD=`1efe6bd`，Tracker 仍为
  `✅ Gate0/Gate1 PASS；🚧 S2 task audit`，工作树在恢复时为 clean。当前真实
  缺口是 S2 real streaming evaluator/实测结果，不是 Gate1 或资源调度。

## 2026-08-01 checkpoint 提交准备

- 用户明确要求先提交 Git；本次定位为 Gate1 + S2 evaluator foundation
  checkpoint，不声称 C3–C9 或 Full CEM 已完成。
- 待提交集合为 23 个 E182 范围文件，cached `diff --check` PASS；包含
  Gate1 log/auditor、factored P/R/G tape、S2 frozen fixture/evaluator 基础、Ada
  0/1 no-kill launch/pull/runtime contracts 与实验台账。

## Gate1 aggregate TDD

- 已先新增 aggregate audit direct-main tests：要求真实 dev3 artifact-only audit PASS，
  且篡改 PRG selection content SHA 必须精确 FAIL。RED 已实测为预期
  `ModuleNotFoundError: audit_gate1`。现已实现 runtime/same-run/三 case PRG verifier/
  selection identity/direct-tests 聚合器，真实 artifact 与 tamper tests=`2/2 PASS`；
  正式 Gate1（包含 direct-main tests）已 `PASS cases=3`，selection set SHA=
  `3781ad96505ee597222f145fbcaa943be198a9473fb4f0faf72b56fed247c8a5`，heldout
  仍为 `NOT_ACCESSED_DEV3_ONLY`。下一步静态复核、写阶段 log、更新 Tracker 至 S2。
- Gate1 artifact 最终 SHA=`a1177e14...f6f5`，并冻结 auditor/builder/replay/test source
  SHA；runtime=`50/50`、三 case verifier mismatch=`0`、五组 direct tests 均 exit0。
  首次静态检查的 unused import/format 已 mechanical 修复；Gate1 tests `2/2`、正式
  audit、E182 ruff/format、launcher bash syntax、diff-check 全 GREEN。
- 已创建阶段日志 `log/247_E182_gate1_real_query_tape_results.md`，记录三 case chunks/
  SHA、wall time、factored storage、Gate1 evidence、claims边界与 S2 下一入口；不把
  instrumentation PASS 扩大解释为 candidate/downstream 质量改善。
- Tracker 已推进为 `✅ Gate0/Gate1 PASS；🚧 S2 task audit` 并链接 log 247；log INDEX
  已重建。全目录 ruff/format、E182 launchers syntax、diff-check继续 PASS。

## S2 规模审计

- 每个 CEM chunk 为 `(64 samples,48 horizon,42 qpos)`；bucket003/004/007 分别有
  `202/139/77` chunks。每 pose 的1669点中 R_surface/G_hand=`1600`，其余 robot/leg
  为 `21/48`；直接对54个 candidate全展开会产生约386亿 point-candidate queries。
- S2 必须在读取 candidate score 前冻结 progressive fixture：reference/E178-final可
  全点；CEM first-pass需保留所有 tick/sample/horizon和全部 safety/leg points，对手 mesh
  做固定索引 reduction，并对每 K finalist做更高密度复核。下一步先写 sampling manifest/
  representativeness tests，再做固定 candidate timing-only benchmark。
- 已先写 S2 fixture direct-main tests：冻结 screen=`mesh64` 全54候选、finalist=
  `mesh256` 每 object×K 最优、production canary=`full1669`；三个 tier保留全部 CEM
  chunk/sample/horizon，primitive points全保留且索引嵌套。另要求 Gate1非PASS时拒绝
  freeze；builder尚未实现，下一步运行预期 RED。
- S2 fixture RED 已实测为预期 `ModuleNotFoundError`。现已实现 pre-score builder：
  从 Gate1/三条 PRG manifests冻结 nested point indices、all-pose query counts和54个
  BUILD_PASS candidate identity（只读 build manifest，不读 E181 fidelity score）；下一步
  执行 GREEN、静态检查并生成正式 frozen fixture。
- Fixture tests=`2/2 PASS`；首次静态检查 ruff/diff-check通过，仅两个新文件需要
  formatter。下一步 mechanical format、复跑并生成正式 fixture manifest。
- 正式 S2 fixture 已 `FROZEN cases=3 candidates=54`，manifest SHA=`b278062d...f0bd`、
  candidate identity SHA=`d43beee1...65d1`，Gate1 SHA exact。每 pose screen/finalist/full
  points=`197/581/1669`；三 object 单 candidate screen queries=`123.72M/85.17M/
  47.24M`，所有 CEM poses与全部 safety/leg points保留，heldout未访问。
- 已复核 authoritative R/G 公式：每 pose先对 consumer geom union取 min clearance；
  G 再沿48 horizon计算 sample min/violation fraction，并应用独立 body/hand/leg
  floor与max-violation阈值；R robot/leg为hinge，surface-band为带宽/指数 score。
  evaluator需复用 frozen config值和 radius-adjusted clearance，不能只报点级SDF误差。
- 固定 timing-only benchmark（bucket007 `t005_k08_v032`，不用于selection）在605,184
  screen points上测得 Open3D oracle/candidate=`7.78/13.77 Mquery/s`，boolean union
  `0.005s`；预计54-candidate screen可实际流式完成。raw chunk顺序与 active optimizer
  tick一一对应，含原始64-sample G summaries/valid masks，可用于逐 chunk shadow重算。
- 已扩展 S2 synthetic tests，冻结 consumer min-reduction、legacy/hard-floor G semantics、
  mask flip/false accept/reject，以及 R robot/leg hinge + symmetric surface-band公式；实现
  RED 已实测为缺少新 API。现已实现 consumer clearance reduction、48-step G sample
  summary/valid mask、gate comparison和 ungated geometry R components；功能 `5/5 PASS`。
  首次静态仅 import order/format，mechanical fix后测试、ruff/format、diff-check全 GREEN。

## S2 real evaluator 实现恢复审计

- `evaluate_task_queries.py` 当前只有 exact-union/Open3D SDF、point-pair、
  consumer reduction、G sample mask 和 R component 纯函数，尚无 oracle cache、
  candidate loader、streaming chunk evaluator、R/G elite shadow、P pose contact 或 CLI。
- frozen fixture 只有 manifest + 3 个 indices NPZ；下一个必须证据是 bounded
  real artifact，不能直接声称 54-candidate screen 可运行。
- progress update 的前两次 patch 因 context 空格/区块顺序不匹配而被拒绝，
  均未改动目标文件；已改为基于 EOF 准确 context 追加，不再重复原 patch。
- bounded authority 已确认：bucket007 的 reference/final/CEM poses=`216/166/3072`，
  one-chunk screen query 为 static full1669 + CEM screen197；CEM raw chunk 确为
  `qpos=(64,48,42)`、`rewards=(64,)`、`selected_indices=(6,)`，并含 body/
  hand/leg/combined gate summaries 与 posture valid mask。
- 固定 candidate `bucket007/t005_k08_v032` 为 8 个 ordered watertight convex parts，
  candidate asset SHA=`bd42632d...9c3d`，oracle cleaned mesh SHA=`e374dc5a...5049`。
  evaluator 必须逐 part 验 SHA、boolean union，且从 factored geom pose+offset 按 chunk
  materialize，不得展开并留存全部 query tensor。
- authoritative gate 组合已从 `sampling.py` 复核：body/hand/leg/posture masks 取
  AND，combined min/violation/depth 取逐 gate min/max/max；`min_valid_frac=max(0.02,
  0.02,0.05)=0.05`，64 samples 所以至少 4 valid 才不 fallback，elite 数从
  raw artifact 冻结为 6。由于 posture gate 开启，fallback ranking 为
  `shadow_reward - 5*posture_violation`，而不是 generic geometry least-violation；必须复制
  这一真实语义。
- config 已确认 body/hand/leg 阈值分别为 `(-0.005,0,NaN)`、
  `(-0.01,0.1,-0.02)`、`(0.005,0.02,-0.005)`；R 为 robot/leg scale=2,
  margin=0.02 和 symmetric surface-band。首次以 package import 读 config 失败是因
  E182 scripts 使用 sibling absolute imports；已通过将 E182 script dir 加入 `sys.path`
  成功读取，不修改 production import contract。
- S2 real evaluator 扩展已先写 direct-main RED，冻结 3 个新合同：只 materialize
  selected nested points、geometry+posture gate 组合及 normal/fallback elite、pose-level
  contact confusion。RED 已按预期在第一个缺失 API `combine_gate_summaries`
  处 ImportError，未执行真实 score 或读取 heldout。
- 上述 3 组 API 已实现：selected-point materialization 带 shape/range gate，
  gate summary 严格 AND/min/max，elite 支持 normal 与 posture-fallback，P contact 输出
  TP/TN/phantom/missed/precision/recall。功能测试从 `5/5` 扩展为 `8/8
  PASS`。首次静态复核仅剩 test import order `I001`，下一步使用 ruff
  mechanical fix 后复跑，不改变语义。
- `I001` 已用 ruff mechanical fix 消除，formatter 只重排了新 evaluator 格式；
  `8/8 PASS`、ruff 和 `diff --check` 重跑 GREEN。复核 E181 authority 后确认
  可直接复用 manifest 内 ordered part paths/SHA 和 oracle cleaned mesh path/SHA，
  但 candidate asset SHA 还必须用 E181 同一 inventory digest 重算，不能只信任字段。
- real evaluator 第一层已实现：重算 E181 candidate asset digest、逐 manifest/part/
  oracle SHA 校验、ordered parts boolean union、复用 Open3D scene 查询、atomic NPZ、
  0.1mm fixed-bin streaming SDF histogram 与 exact pair-count accumulator。histogram 报告
  conservative p90 upper + overflow + max，避免为亿级查询保留 error array。
- real evaluator 执行层已接通：dev3-only fixture/case/candidate resolver、tiered D_M
  oracle cache、factored pose batching、radius-aware P/R/G、R normalized error + per-chunk
  Spearman、body/hand/leg/geometry/posture-combined G、posture fallback elite/top6 overlap、
  launch-floor 与 CLI。bounded-real 固定 bucket007/K8/one chunk 并强制
  `selection_eligible=false`。合成 `8/8` 仍 PASS，ruff check/diff-check GREEN；当前
  仅 formatter 提示 evaluator 需 mechanical reformat，尚未运行真实 bounded score。
- formatter 已 mechanical 完成，ruff/format/diff-check 和 `8/8` 复跑 GREEN。首个
  bounded real 已完成：bucket007 `t005_k08_v032`、screen tier、one CEM
  chunk，`1,242,742` 个 D_M/exact-C paired queries，audit=`PASS`，且
  `selection_eligible=false`。这只证明 schema/count/finite/P-R-G 执行链成立，不用于
  candidate 选型或 launch-floor 结论。
- bounded 实测细项：point sign disagreement=`2.074%`、deep mismatch=`0.0367%`、
  p90 error=`36.1mm`；P contact precision/recall=`1.000/0.912`；R/G one-chunk 与
  D_M 无 rank/mask/elite 分歧。bounded launch floor 为 `11/12 FAIL`，唯一失败是
  p90 `36.1mm>30mm`；因为它 selection-forbidden，不得据此选/拒 K8。artifact
  SHA：audit=`9ede2e0d...b4e5f`、oracle=`43fe2600...6db8`、result=
  `d00ccd78...99c2`。
- 已新增 artifact-backed bounded direct tests，要求 10 项 audit checks、artifact SHA、
  query/P/R/G schema 与 selection-forbidden 精确；同时增加 streaming histogram
  conservative p90/overflow 合成测试，下一步执行并做静态复核。
- bounded artifact tests=`2/2`、geometry tests=`9/9` PASS，三个新/修改文件的
  ruff/format/diff-check 全 GREEN。
- 已实现 selection-eligible screen orchestrator：在任何 formal score 前冻结 P/R/G
  normalized-worst scoring contract、evaluator/runner/fixture SHA、bounded evidence exclusion和
  heldout prohibition；每 object 只建一次 D_M cache，18 candidates 串行、resume-safe、
  最后强制 54/54 聚合并按 launch floor→score→10% near-tie→actual hulls→
  runtime/params/id 选 9 个 object×K provisional finalists。固化入口为
  `scripts/launch/active/run_E182_task_query_audit.sh`，该脚本为 CPU/Open3D only，
  不调用 GPU 或任何进程处置命令。
- screen scoring/selection 已增 3 个合成测试：denominator 映射、10% near-tie
  优先 actual hulls、launch-floor 不可被 score 救回。首次运行在精确 dict
  equality 处失败，根因是 `0.3/0.30` 浮点表示导致 1 附近 ULP，不是
  scoring 语义错误。下一步将测试改为 `np.isclose`，不修改预冻结公式。
- 浮点断言已改为 `np.isclose`，screen=`3/3`、geometry=`9/9`、bounded
  real=`2/2` 全 PASS；5 个 evaluator/runner/test 文件 ruff+format、launcher
  `bash -n`、`diff --check` 全 GREEN。现在允许冻结 formal screen protocol，仍无
  selection-eligible score 被读取。
- formal screen protocol 已在任何 eligible result 前冻结，SHA=
  `41528914750bdca86df1280cf7366ad82b828adf1a8fc2f0581d273a0a0a5eba`，
  candidates=`54`、tier=`screen`、heldout=`NOT_ACCESSED_DEV3_ONLY`。已通过
  CPU-only launcher 启动 resume-safe 3-case screen，当前 exec session=`8614`；无 GPU
  任务或现有进程被修改。
- formal screen 首条 eligible row 已自然完成：bucket003
  `t005_k08_v032`，wall=`19.075s`、launch floor=`FAIL`。运行仍在同一
  resume-safe session 中继续；单条 FAIL 不触发停止或改公式，必须完成 54/54
  后才聚合。
- screen 进度 bucket003=`3/18`；新完成 `t005_k08_v064` wall=`23.475s`、
  `t005_k16_v032` wall=`19.860s`，两者 floor 均 FAIL。当前只记录运行证据，
  不在 incomplete group 上比较 K/阈值或生成 finalist。
- screen 进度 bucket003=`6/18`；t005 的 K16v64/K32v32/K32v64 分别
  `24.505/21.634/25.451s`，均 floor FAIL。至此 threshold=5mm 的 6 条已跑完，
  但 protocol 要求继续 t010/t020 全部候选，不作 early stop。
- screen 进度 bucket003=`8/18`；`t010_k08_v032/v064` wall=`21.546/24.466s`，
  floor 均 FAIL。session 无异常、nonfinite 或缓存 SHA 拒绝。
- screen 进度 bucket003=`10/18`；`t010_k16_v032/v064` wall=`21.423/24.777s`，
  floor 均 FAIL。当前 eligible 产物按 candidate 独立 JSON 落盘，中断时可按 SHA
  resume，无需重跑已完成条目。
- screen 进度 bucket003=`12/18`；`t010_k32_v032/v064` wall=`22.653/27.754s`，
  floor 均 FAIL；t010 六条已完成，继续 t020。
- screen 进度 bucket003=`14/18`；`t020_k08_v032/v064` wall=`18.806/22.677s`，
  floor 均 FAIL。暂时的 0 PASS 仍不做 CoACD 失败结论，必须在完整结果中定位
  是 SDF p90、P、R 还是 G 的具体 floor 项。
- screen 进度 bucket003=`16/18`；`t020_k16_v032/v064` wall=`22.797/24.118s`，
  floor 均 FAIL；该 object 仅剩 K32 两条。
- bucket003 screen=`18/18 COMPLETE`，最后 `t020_k32_v032/v064` wall=
  `23.637/24.989s`，floor 均 FAIL；该 object 暂时 `0/18 floor PASS`。runner
  已自动进入 bucket004 oracle cache/18-candidate queue，不修改 protocol。
- bucket004 screen=`4/18`；t005 K8v32/K8v64/K16v32/K16v64 全部 floor PASS，
  wall=`11.963/13.018/13.487/14.395s`。这证明 evaluator/floor 并非系统性
  将所有 CoACD 候选判 FAIL；bucket003 的 0/18 需在聚合后做 object-specific 归因。
- bucket004 screen=`7/18`；`t005_k32_v032` PASS (`13.585s`)、`v064` FAIL
  (`15.125s`)，`t010_k08_v032` PASS (`12.123s`)。同 K/阈值下 vertex cap 已出现
  floor 分歧，因此不能只用 max hulls 预判候选。
- bucket004 screen=`11/18`；t010 K8v64 FAIL、K16v32 PASS、K16v64 FAIL、
  K32v32 FAIL，wall=`13.084/12.214/14.071/13.179s`。当前 bucket004 的
  PASS 主要集中在 v032，但仍等待全 18 条后用冻结 score 选 finalist。
- bucket004 screen=`15/18`；t010 K32v64 FAIL；t020 K8v32 PASS/K8v64 FAIL/
  K16v32 PASS，wall=`15.602/11.604/12.931/12.860s`。继续最后 3 条。
- bucket004 screen=`18/18 COMPLETE`，floor PASS=`9/18`；最后 t020
  K16v64/K32v32/K32v64 均 FAIL，wall=`14.608/13.127/14.976s`。runner 已
  进入 bucket007 oracle cache/queue；当前全局 eligible progress=`36/54`。
- bucket007 screen=`4/18`；`t005_k08_v032` FAIL，K8v64/K16v32/K16v64 PASS，
  wall=`6.320/7.303/9.686/12.223s`。这与 bounded 的 K8v32 p90-only FAIL 方向
  一致，但 formal decision 仍只使用完整 eligible screen。
- bucket007 screen=`9/18`；t005 K32v32/v64 与 t010 K8v32/K8v64/K16v32 全 PASS，
  wall=`10.791/13.191/7.338/8.288/8.271s`。当前该 object 为 `8/9 PASS`，
  运行吞吐也明显快于 bucket003/004，但这仅是 CPU exact-SDF screen 耗时，
  不等价于 S4 physics/CEM throughput。
- bucket007 screen=`14/18`；t010 剩余 K16v64/K32v32/K32v64 与 t020
  K8v32/K8v64 全 PASS，wall=`9.574/11.017/13.723/7.621/9.084s`。该
  object 暂为 `13/14 PASS`，全局进度=`50/54`。
- formal screen 已 `54/54 COMPLETE`；bucket007 最后 t020 K16v32/K16v64/
  K32v32/K32v64 全 PASS，该 object=`17/18 PASS`。全局 floor PASS=
  `26/54`（bucket003/004/007=`0/9/17`），missing/error/nonfinite 运行中均未见。
  下一步必须运行冻结 aggregate，确认 9 个 object×K group 中实际能选出多少
  finalist，不手工挑行。
- frozen aggregate=`COMPLETE`，floor PASS=`26/54`，provisional finalists=`6/9`；
  aggregate/table SHA=`8b944476...6c50`/`83c268e7...4204`。bucket004 选出
  K8=`t005_k08_v064`、K16=`t020_k16_v032`、K32=`t005_k32_v032`；
  bucket007 选出 K8=`t010_k08_v064`、K16=`t010_k16_v064`、K32=
  `t020_k32_v032`。bucket003 K8/16/32 均 `NO_LAUNCH_FLOOR_CANDIDATE`。
- bucket003 归因非 D/R/G：SDF p90=`4.1–8.8mm`、R normalized p90=`0.1%`、
  R Spearman=`1.0`、G mask/fallback/top6 无分歧；18/18 均失败 P contact precision
  (`0.294–0.581<0.70`)，其中 4 条还失败 recall。因此当前证据是
  bucket003 CoACD hull union 在真实静态 P poses 上产生大量 phantom contact，而不是
  CoACD 算法全局失败。按冻结 protocol 不豁免、不启动 Full；下一步先做
  P 可视化+真实 MuJoCo 短接触复核，区分物理 phantom 与 point-proxy 保守性。
- 已复核现有可视化/侧车工具：E181 有 E178-style oracle/parts/phantom 3D +
  2D slice renderer 可复用；E175 有 XML multi-geom object replacement 基础，但尚无
  E182 CoACD production sidecar。因此诊断将先以 bucket003 每 K 冻结 score 最佳失败
  candidate 生成 task-pose phantom overlays，再构建仅用于 short contact replay 的实验
  sidecar，不直接进入 Full。
- 首次检查 bucket003 XML 误用了 bucket007 的 `omnirt_v2` 路径假设，导致
  read-only FileNotFound；未写文件。已改从 frozen dev3 authority row 解析，正确为
  `omnirt_v1/.../scene_act_E178_contactAlignedTop.xml`，scene SHA=`0880f6cf...ecd`。
  后续不再根据 object 手写 retarget variant 路径。
- 已实现 `diagnose_p_contact.py`：只从 frozen screen table 对 bucket003 每 K 选
  minimum-score failed candidate（诊断用，非 finalist），重算 reference+E178-final 的全 P
  point contact confusion，选最深 phantom pose，输出四视角 3D hull/oracle/task-point overlay
  与 XY/XZ/YZ+contact-timeline 2D 图。所有输入继承 fixture/candidate/part/oracle/
  screen SHA，输出强制 `selection_eligible=false`。下一步静态/合成验证后真实渲染。
- diagnostic renderer 已 format/ruff/diff-check GREEN 并完成真实渲染，
  `E182_P_CONTACT_DIAGNOSTIC=COMPLETE candidates=3`。产物位于
  `results/E182/s2_task_query_eval/p_contact_diagnostic/`，包含 K8/K16/K32 各一组
  3D+2D task-pose 图和 SHA manifest；下一步读取图像与数据做实际观察，
  不能只以“文件已生成”满足可视化硬门。
- diagnostic 数据复核：K8/K16/K32 最佳失败候选在 882 static poses 上
  phantom poses=`17/18/22`，precision=`0.485/0.581/0.542`；代表姿态的 C min=
  `-3.19/-3.63/-3.68mm`，而 M min=`+0.75/+0.75/+0.26mm`。这些不是大深度
  穿透，而是稳定跨过 0-contact 边界的 3–4mm hull 外扩。
- 已实际检查 K16 3D 四视角图；magenta phantom points 集中在 bucket 上缘/
  侧壁的手部查询点，不是远离物体的躯干/腿部点乱报。凸分解外形肉眼
  整体合理，但接触临界面的毫米级偏差足以将 non-contact pose 翻为 physics
  contact；下一步继续检查 2D timeline/投影并用 MuJoCo contact 复核。
- 已实际检查 K16 与 K8 的 2D XY/XZ/YZ+timeline；两者代表 phantom 均是
  手部在上缘接触临界处，投影上 C 边界比 M 外扩数毫米。timeline 显示
  phantom 主要出现在 E178-final 后段的多个独立接触窗，并非单一帧数值抖动；
  reference 中也有少量 phantom。K8/K16 失败的空间位置与时间结构高度一致，
  支持“hull 外扩改变临界物理接触”，不支持随机 point proxy 误报。
- 2026-08-01 持续执行恢复：工作树保留 S2 evaluator/screen/visual diagnostic
  的未提交修改；新 `replay_p_contact_mujoco.py` 尚未经过 format、静态检查或真实
  replay。下一步先验证 diagnostic sidecar 与 MuJoCo pair/contact 语义，当前仍禁止
  heldout24 与 Full CEM。
- 已将持续执行计划恢复为：context audit → replay 静态/合同修复 → 真实 MuJoCo
  replay → 按证据选择 P evaluator 修正或 decomposition attempt2 → production gates →
  三卡 Full → E178 paired evaluation；不把 diagnostic replay 当作 Full 的替代。
- 计划前 440 行已重新核对：S2 的 P authority 明确要求真实 pose/contact 与短
  rollout，且 launch floor precision/recall 均为 0.70；因此当前 replay 是闭合
  bucket003 P gate 的计划内诊断，不得通过放宽已冻结阈值绕过。
- 已完整复读 plan 余段与 log 247：C3/C4 仍是当前未闭合 claims，production
  freeze、grid、canary、Full 与 paired eval 均未开始；S2 replay 必须保持
  `selection_eligible=false`，只有诊断/修复 bucket003 P authority 的作用。
- Tracker 恢复核对仍将 E182 标为 `Gate0/Gate1 PASS；S2 task audit`，与当前
  artifact 状态一致；未将 54-candidate screen、P 可视化或未验证 replay 误记为
  production/Full 完成。
- progress 前半段已复读；其中早期“下一执行入口”是历史 checkpoint，当前执行
  authority 以文件尾部已完成的 54-screen/P diagnostic 和本轮 MuJoCo replay 入口为准。
- Tracker、plan、log 247 与 progress 已完成本轮恢复读取；现在进入 replay 源码与
  E178 scene/pair authority 审计，不再重复早期 screen 工作。
- 持续计划已推进到“静态检查、测试并修复 MuJoCo P-contact replay”；后续真实运行
  只消费 dev3 bucket003 的 frozen reference/E178-final poses，不接触 heldout24。
- replay 源码读取 attempt1 被 `rg --files -g AGENTS.md` 的正常空结果 exit1 提前终止；
  仓库内未发现额外 AGENTS.md，且未读取/执行 replay。下一次分开执行检查，不重复
  依赖 `&&` 的命令结构。
- 已完整读取 replay 源码：它会替换 E178 的全部 object proxy geoms，按原 explicit
  pair 的另一侧 geom 复制到每个 ordered CoACD part，并用 `mj_forward` 重放
  reference+E178-final 882 poses；输出保持 diagnostic-only。需通过真实 compile 验证
  mesh/pair 语义，且重点检查 pair attribute 是否在同一 other geom 上一致。
- replay 首轮 `ruff check` PASS；尚未据此声明 runtime 正确，下一步检查 format、
  原 scene pair 属性分组与真实 sidecar compile/replay。
- replay `ruff format --check` 首轮仅报需要 mechanical reformat，无语义错误；下一步
  执行 formatter 后复跑静态检查，不手工改逻辑。
- formatter 已完成；diagnostic manifest 重读确认 3 条均为 bucket003 dev case、
  `selection_eligible=false`、heldout 未访问，代表 pose 分别为 E178-final 201/201/23。
  replay 输入边界正确，下一步审计 base scene 的 explicit pair 属性。
- frozen fixture→PRG manifest SHA 路径已解析；base scene 为 bucket003 的
  `dcv3_omnirt_v1.../scene_act_E178_contactAlignedTop.xml`，P inventory 是 16 个
  lower-body geoms + `lh/rh` 共 18 个，未误混入 R-only torso/head geoms。
- base scene 有 5 个 E178 proxy boxes；18 个 robot–object pair 在 5 个 proxy 上的
  solref/friction/condim/margin/gap 逐类一致，另有 1 个 object–floor 模板。object body
  有显式 mass/diaginertia，因此替换 mesh geoms 不会重算质量惯量。builder 仍需加入
  pair-template 一致性断言，避免未来 scene 静默选错模板。
- 已复核 E182 direct-main test 结构；replay 将增加独立合同测试，覆盖 binary confusion、
  real sidecar geom/pair count、source SHA 与 `selection_eligible=false`，避免仅靠一次 CLI
  输出判断 builder 正确。
- replay builder 已加入 legacy pair physics-attribute 一致性硬断言；新增 direct-main
  tests 覆盖四格 confusion 与真实 bucket003 K8 sidecar compile、`19×actual_hulls`
  explicit pairs、base/prg SHA、object mass=2.0 和 geom name closure。尚未运行测试。
- replay test attempt1：binary confusion PASS，但真实 sidecar compile 在原 robot STL
  相对路径解析处 FAIL；根因是 XML 从 dataset scene 目录搬到 results/temp 后，原
  compiler/asset 相对路径基准改变，不是 CoACD mesh 或 contact failure。下一步复用
  现有 sidecar 路径重写合同，不能原样重跑。
- 现有 E175 仅解析 base scene `compiler.meshdir`，没有可直接复用的 sidecar relocator。
  replay 将显式把 base scene 的相对 `meshdir/texturedir` 解析成绝对目录后再写 sidecar，
  同时保留 candidate part 的绝对 file path；这是 relocation 修复，不改物理参数。
- bucket003 scene 证实 `compiler.meshdir` 指向 robot mesh 根，object visual OBJ 也通过
  相对该根的路径解析；因此只改 sidecar compiler directory 的基准即可同时保留 robot
  与 object visual assets，无需改每个 asset file 字段。
- 已实现 compiler `assetdir/meshdir/texturedir` relocation；replay direct-main tests
  从 attempt1 asset-path FAIL 转为 `2/2 PASS`。真实 K8 sidecar 可编译，pair count、
  object mass 和全部 CoACD geom 名均闭合；下一步复跑 ruff/format 后执行完整 3-candidate
  882-pose replay。
- 两个 replay 文件 `ruff check` PASS；format-check 仅要求 mechanical reformat，未发现
  新语义问题。下一步执行 formatter、复跑合同测试和 diff-check 后再运行真实 replay。
- mechanical format 已完成，随后两个 replay 文件 `ruff check` 再次 PASS；还需
  format-check、direct-main 和 diff-check 的最终 preflight。
- final replay preflight 当前为 format-check PASS、direct-main `2/2 PASS`；仅剩
  repository diff-check 后即可运行 3-candidate 真实 MuJoCo replay。
- repository `git diff --check` PASS；status 仅包含预期的 E182 S2 evaluator/screen/
  diagnostic/replay 与 progress 修改，无非 E182 文件。完整 replay preflight 已闭合。
- 真实 MuJoCo replay 首次完整运行成功：`COMPLETE candidates=3`，wall≈3.96s；未使用
  GPU、未访问 heldout、未启动 Full。下一步必须审计 manifest 的 point-C↔MuJoCo-C
  confusion、phantom confirmation、sidecar geom/pair 与 5-step contact persistence，
  不能仅凭 CLI COMPLETE 下结论。
- replay 结果：K8/K16/K32 的 point phantom=`17/18/22`，MuJoCo 确认=`17/18/22`
  （三者 100%）；point-C→MuJoCo-C precision 全为 1.0，recall=`0.846/0.566/0.558`；
  MuJoCo contact poses=`39/76/86`。这证明 frozen P gate 不是误杀，现有 bucket003
  CoACD family 确实制造真实 physics contact，且增大 K 未解决。代表 step0 三者均 contact；
  后续 zero-control 5-step 的 qpos L∞变化 0.37–0.49，persistence 仅作诊断、不可解释为
  production stability。
- replay artifact-backed tests 已扩为 `3/3 PASS`：冻结 diagnostic/fixture/sidecar SHA、
  3×882 poses、100% phantom confirmation、step0 contact、`19×hulls` pairs 和
  diagnostic-only/heldout contract。当前证据足以判定 evaluator 未误杀并进入不同
  decomposition 路线；不放宽 P precision floor。
- 执行计划已推进到 decomposition attempt2。下一步先重读 E181 build/rejection/visual
  authority 与当前 CoACD builder，选择 task-aware pre-segmentation 的最小可证方案；
  不原样重复 54-candidate family，也不重新利用已读 score 调旧参数。
- 重读 E181 log 245：原 family 固定 merge+decimate、threshold 5/10/20mm、K≤32、
  vertices≤64，44/54 触发 hull-cap warning；bucket003 全局 cavity 最低 19.07%。E182
  已证明真实 P 失败，因此 attempt2 必须改变 decomposition topology，而不是只提高
  原 family K 或重新放宽 cavity/P 阈值。
- 重读 E181 visual log 246：bucket003 的主要失败是开口/端段被 hull 跨接形成
  连通 phantom block，而不是均匀 surface offset。attempt2 应在 CoACD 前先按 bucket
  长轴/开口拓扑切成不跨接的局部 segment，再对子段分解并合并 ordered parts；这是
  task-aware topology change，针对当前真实 P 根因。
- E181 builder 源码确认输入是整个 cleaned watertight mesh，`pca=False`、merge=True，
  没有任何 segment/topology constraint；因此 CoACD 可以用单个 convex part 跨接长轴
  两侧 rim。attempt2 可复用 deterministic export/SHA，但需新建 E182 segmented builder，
  不能修改 E181 历史 artifact。
- 已定位 bucket003 K8/K16/K32 的现有 2D/3D task-pose 图；下一步重新查看 K16/K32
  截面，确定 pre-seg 轴与 segment 数，避免仅凭文件名假设长轴。
- 复看 K16 2D/3D：bucket003 bounds 约 x±0.25、y[-0.4,0.37]、z[-0.22,0.22]，
  手部 phantom 位于上缘侧壁；只沿 y 分段不足以阻止跨 x 空腔。attempt2 预注册布局
  应同时切 `x=2` 半区与 `y=4` 等长 slab，共 8 个 solid intersections；每 cell 的
  CoACD cap=`1/2/4` 对应总 K≤8/16/32，既保持 K family 又改变拓扑约束。
- evaluator identity 复核：candidate digest 只依赖 object、完整 parameters 与 ordered
  part SHA/count，manifest+parts+oracle SHA 均硬校验；attempt2 可生成独立 augmented
  dev-only fixture 后复用同一真实 P/R/G evaluator，无需降低现有校验或改公式。
- 原 screen runner 硬编码 54/18/6 group，不能安全复用到 attempt2；需独立 9-candidate
  runner/fixture，继续复用冻结 scoring function。oracle bounds exact 为
  x[-0.2924,0.2484]、y[-0.3828,0.3800]、z[-0.2330,0.2328]，支持 x2×y4 等空间切分。
- read-only manifold probe 对 x2×y4 首 cell intersection 成功：474 vertices/964 faces、
  watertight+winding-consistent、正体积 0.00913m³。证明当前依赖可做 exact solid
  pre-seg；下一步验证 8-cell volume closure 后冻结 attempt2 protocol。
- 全 8-cell manifold probe 通过：8/8 非空、watertight/winding-consistent，segment
  volume sum=`0.0997879339995` vs source=`0.0997879335577m³`，差 `4.42e-10m³`。
  x2×y4 exact partition 可作为 attempt2 frozen topology，不需近似 voxel 切割。
- 原 fixture candidate schema 已确认仅需 object/id/manifest SHA/asset SHA、max/actual
  hulls、threshold、max vertices；attempt2 将保留原 case/tier/index/gate1 authority，
  只生成 bucket003 的 9-row candidate identity 与新 candidate-identity SHA。
- 已实现 attempt2 builder（尚未 freeze/build）：预注册 x2×y4 exact manifold segments、
  t005/010/020 × K8/16/32、v32、每 segment cap=1/2/4；包含 protocol source SHA、
  volume closure、deterministic part export/digest、resume-safe 9 candidates 与 augmented
  fixture。首轮 `ruff check` PASS，仍需测试/format 后才能冻结。
- builder format-check 仅要求 mechanical reformat；protocol 尚未冻结，因此允许在
  source SHA 固定前完成格式化与合同测试。
- builder mechanical format 已完成；下一步抽取纯 partition helper 并用真实 oracle
  做 8-cell topology/volume direct-main test，再冻结 source SHA。
- 已抽取 `partition_mesh` 并新增 attempt2 direct-main tests（尚未运行），覆盖精确
  9-candidate family/K无4、真实 oracle 8-cell order/topology/volume closure，以及
  candidate digest 与 production evaluator exact 一致。
- attempt2 test attempt1 异常为 exit0 但无任何 PASS 输出；这不构成通过证据。下一步
  检查文件尾/main guard 与语法内容，修复后才可冻结 protocol，不原样视为 GREEN。
- 测试文件内容/main guard 完整；无输出不是文件截断。下一次改用 unbuffered Python
  并观察 native manifold/CoACD import 行为，仍要求显式 3/3 PASS 文本。
- attempt2 test attempt2 用 unbuffered 明确得到 RC=139、无 Python traceback。根因范围
  是同一进程顶层加载 CoACD native library 后再调用 manifold boolean；单独 manifold
  probe 已稳定。修复为 CoACD 仅在独立 `build-candidates` 路径懒加载，segment/test
  进程不加载 CoACD；不重复当前 import 顺序。
- lazy CoACD 修复消除了 native segfault；attempt3 进入 Python ImportError，发现
  `DEFAULT_FIXTURE` authority 实际定义在 `evaluate_task_queries.py` 而非 fixture builder。
  下一步修正 import source 后重跑，当前仍未 freeze protocol。
- 修正 fixture authority import 后 attempt2 tests=`3/3 PASS`，包括真实 manifold
  partition；native crash 根因已通过 command-stage process isolation消除。下一步完整
  ruff/format/diff-check，随后才冻结 attempt2 protocol/source SHA。
- 静态检查首轮仅报 test import order 与 unused numpy，两项均可 mechanical fix；
  builder 本身无 ruff error。下一步用 ruff `--fix`，不改变 protocol/geometry 语义。
- ruff mechanical fix 已完成两项，formatter 显示两个 attempt2 文件均无需改动；
  下一步复跑 ruff/test/diff-check，确认 builder SHA 可冻结。
- attempt2 builder/test final ruff PASS、direct-main `3/3 PASS`；下一步仅需
  repository diff-check，然后执行 `freeze-protocol`。冻结后 builder source 不再允许
  改动，若出现实现 bug 必须保留旧 protocol 并新建版本。
- repository diff-check PASS；attempt2 pre-freeze checks 已闭合。下一操作将首次写入
  pre-score protocol，当前仍无 attempt2 candidate manifest 或 score。
- attempt2 protocol 已在 build/score 前正式 `FROZEN_BEFORE_BUILD_OR_SCORE`；参数为
  x2×y4、t005/010/020、K8/16/32、v32，原因 SHA 绑定 100% MuJoCo phantom replay。
  下一步审计 protocol SHA/无既有 result 后构建 exact segments。
- protocol SHA=`70e3110a...e17c66`，builder SHA=`e2138038...7e19a`，replay SHA=
  `b432a538...d41b`；9-row family 与 unchanged selection/full-forbidden contract 重读
  exact。下一步 `build-segments`，任何 builder source 修改都会被 SHA gate 拒绝。
- frozen-source `build-segments` 首次运行 `PASS count=8`；下一步审计 segment manifest
  SHA、volume delta、每 cell topology/SHA 后才启动 CoACD candidate build。
- segment audit：8/8 watertight+winding-consistent，vertex/face=`204–1048/404–2144`，
  volume sum=`0.0997879341014m³`，vs oracle delta=`5.44e-10m³`；protocol/oracle SHA
  exact。segment 输入已可用于 9-candidate CoACD build。
- frozen 9-candidate build 已启动，exec cell=`975`；当前为 CPU-only sequential
  segmented CoACD，不访问 heldout、不使用 GPU。等待自然输出，不发送终止信号。
- 首个 30s wait 无 stdout，底层 process 仍在 exec session=`18408`；无 error/RC，继续
  read-only poll，不重启、不并发重复 build。
- build session 继续运行；出现 3 条 CoACD `No more convex hulls to merge` warning，
  尚无 candidate PASS/FAIL。该 warning 说明某些 segment 自然产生少于 cap 的 hull，
  不自动视为失败；最终以 actual hull count/manifest topology 为准。
- 后续 30s poll 无新输出，session=`18408` 仍自然运行；不作 early timeout，继续等待
  首个完整 candidate manifest。
- 再一轮 30s 无新 stdout，进程仍 active；CoACD 每 segment 的 native计算时间较长，
  当前没有 crash/OOM/traceback 证据，继续同一 session。
- 下一轮 poll 仍无新 stdout，session active；已向用户同步当前非致命 warning 与等待
  状态，不对运行中 process 发信号。
- 后续 30s session 仍 active 且无新输出；继续等待，尚未形成可审计 candidate row。
- process audit 显示 uv wrapper PID 1235557 / Python PID 1235560 均仍存在；下一轮 poll
  无新输出、无退出。继续同一 build，不启动第二实例。
- attempt2 build attempt1 在 5m48s、CPU100%/RSS1.44GiB 后自然 RC1：
  `segx2y4_t005_k08_v032: segment hull cap violated`。CoACD warning 表明至少一个 exact
  cell 无法 merge 到 per-cell cap1；这不是 evaluator/P failure，而是 frozen x2y4→K8
  build infeasible。旧 protocol/SHA 必须保留，不能编辑后覆盖。下一步检查 segment
  connected-component lower bound，再设计新 protocol 版本，不原样重跑。
- 8 个 segment 的 trimesh connected components 均为1，故 cap violation 不是输入断裂；
  是 CoACD merge graph/算法在 cap1 下的实现限制。下一步审计已写 partial part 数，并
  选择 x2y4 topology 下的 deterministic post-convex-hull 或更少 cells的新版本；不能
  假设单 connected component必能由 CoACD输出单 hull。
- partial output 只有 part_000..002，无 candidate manifest；segments 0–2 各成功1 hull，
  segment3（x0,y末端复杂区）在 export 前返回 >cap1。partial files 保留为失败证据，
  不进入 fixture。下一步只对该 frozen segment做 changed-cap2 bounded probe。
- segment3 t005/cap2 bounded probe 已启动（cell=`998`），仅内存运行、不写 candidate/
  score；这是改变 cap 的诊断，不重复失败 cap1。等待自然结果。
- cap2 probe 已运行约60s，无 stdout/error，底层 session=`52675` 仍 active；继续等待，
  不启动其他 CoACD probe。
- changed-cap2 probe 自然 PASS：segment3 t005 返回 exactly 2 hulls，wall=104.70s；因此
  x2y4 对 K16 可构建，失败只在 CoACD cap1 merge limitation。下一方案不改 segment
  topology：K8 用每 exact segment 的 deterministic convex hull（数学 cap1），K16/K32
  继续 CoACD cap2/4；独立 v2 protocol/root，旧 v1 protocol与partial failure保留。
- 已新增独立 v2 builder（尚未 freeze/build）：复用 v1 exact segment SHA，K8 为8个
  deterministic segment hull，K16/K32 为 t005/010/020 segmented CoACD；v1 failure
  prefix/SHA进入新 protocol。首轮 ruff 仅 import order，可 mechanical fix。
- v2 import order 已 mechanical fix；format-check 仅要求 reformat。下一步 formatter
  后新增 direct-main tests，仍不冻结 protocol。
- v2 formatter 已完成。K8 direct segment hull 仍需先检查每 part vertex≤32，否则当前
  `_export_part` 会正确拒绝；在 protocol freeze 前解决，不隐式放宽。
- 真实8个 segment convex hull vertex counts=`85–145`，所以 K8/v32 不可构建；这不是
  运行错误。v2 pre-freeze 修订为 K8 single candidate `v160`（覆盖 max145），K16/K32
  segmented CoACD 仍严格 v32，并在 runtime/manifest中显式比较 vertex成本；不做会
  缩入原 solid 的任意 decimation。
- v2 builder 已按 pre-freeze 结论实现 method-specific ceilings：K8 id=`...v160`，
  K16/K32仍`...v032`；新增本地 convex part exporter，candidate parameters/fixture
  显式记录对应 max vertices。尚未静态/合同验证。
- 新增 v2 direct-main tests（尚未运行），覆盖7-row family、v1 partial failure不可覆盖、
  8个真实 segment hull convex/topology 与 `max vertices 145≤160 且>32` 的显式依据。
- v2 首轮 ruff 仅 builder import block order（alias 自动分组）需 mechanical fix；test
  无静态错误。下一步 `--fix`+format 后运行真实 tests。
- v2 import fix/formatter 完成；仅 builder 被格式化，test unchanged。下一步执行
  direct-main，重点验证 v1 failure prefix与真实 K8 hull ceiling。
- v2 direct-main=`3/3 PASS`：7-row family、v1 partial evidence、真实 K8 v160 ceiling均
  闭合。下一步 final ruff/format/diff-check 后冻结 v2 protocol；仍无 v2 build/score。
- v2 final ruff/format-check PASS；仅剩 repository diff-check 即可 freeze v2 source SHA。
- repository diff-check PASS；v2 pre-freeze evidence complete。下一操作将冻结独立 v2
  protocol，旧 v1 SHA/partial prefix会作为父证据嵌入，绝不覆盖。
- v2 protocol 已正式 `FROZEN_BEFORE_V2_BUILD_OR_SCORE`；K8=segment hull/v160，
  K16/K32=segmented CoACD t005/010/020 v32，Full仍 forbidden。下一步审计 protocol
  SHA/source/parent evidence 后 build 7 candidates。
- v2 protocol SHA=`60b4ebd6...518d2`、builder SHA=`2f610fca...0f838`；7 rows、
  parent v1 SHA=`70e3110a...e17c66` 与3-part failure prefix重读 exact。现在允许
  build，仍不允许 score/Full。
- frozen v2 7-candidate build 已启动，exec cell=`1035`；CPU-only sequential，先 K8
  direct hull再6个 segmented CoACD。等待自然 manifests，不启动重复实例。
- v2 K8 candidate 已 `BUILD_PASS hulls=8 wall=0.028s`；首个 K16 CoACD仍运行，出现3条
  cap2 concavity warning（measured>threshold）。warning保留但不提前替代真实 P/R/G
  gate；继续等 candidate manifest。
- 下一轮30s无新输出，session=`68119` active；K8 manifest已落盘，K16尚未完成。
- 再一轮30s无新 stdout/error，首个 segmented K16继续 native计算；不重启。
- 后续30s仍 active无新输出；已向用户同步 K8 PASS/K16 warning状态。继续同一session。
- 再一轮30s无新结果；CoACD仍运行，暂无异常证据。
- 后续30s同样无stdout/error；继续自然等待首个K16完整manifest。
- v2 build attempt1 自然 RC1：K8 manifest PASS；K16 t005 在同一进程连续跑到 segment3
  时返回 >cap2。与 fresh-process segment3 cap2=`2` 冲突，说明 CoACD `seed=1`/native
  state 在多次 `run_coacd` 调用间并非独立，candidate内顺序污染结果。旧 v2 protocol、
  K8 manifest与K16 partial prefix保留。下一步改为每 segment fresh subprocess 的 v3
  execution contract，不原样重跑 sequential v2。
- v2 artifact audit：K8有完整 manifest+8 parts；K16 partial有6 parts（segments0–2各2）
  且无 manifest，符合 segment3前失败。v3 将复用冻结 K8 manifest，只为6个K16/K32
  candidate做 per-segment fresh child，并把每个 child native log/SHA持久化。
- 本机 CPU preflight：32 logical CPUs、114GiB available RAM；v3 可冻结4个 isolated
  segment child并行（预计<8GiB），不使用GPU。swap已满但当前available RAM充足；每个
  child仍 OMP/TBB/OpenBLAS=1，candidate之间串行。
- 已新增 v3 builder（尚未 freeze/build）：复用 v2 K8 manifest；6个 K16/K32 candidate
  的每个 segment 由 fresh Python child只调用一次 CoACD，4 child并行、candidate串行，
  child manifest/native log/SHA逐项持久化再组装 ordered asset。需先静态/合同测试。
- v3首轮ruff仅import order，可mechanical fix；未发现逻辑静态错误。下一步fix/format
  后增加family、v2 failure、fresh-child command与parallel contract tests。
- v3 import fix与formatter已完成；protocol仍未冻结。下一步新增direct-main tests。
- v3 direct-main tests已新增（尚未运行）：覆盖7-row family、v2 K8+6-part failure prefix、
  fresh child unique roots、4-worker与三类native thread=1合同。
- v3 tests=`3/3 PASS`；下一步 final ruff/format/diff-check 后才freeze v3 source SHA。
- v3 ruff PASS；format-check仅test需mechanical reformat，builder已formatted。下一步
  formatter并复跑，不改execution语义。
- v3 test已format并复跑`3/3 PASS`；下一步 final ruff/format/diff-check。
- v3 final ruff/format-check PASS；仅剩 repo diff-check即可freeze。
- repo diff-check PASS；v3 pre-freeze closure complete，下一操作freeze parent/child source
  与4-worker isolation contract。
- v3 protocol已`FROZEN_BEFORE_V3_BUILD_OR_SCORE`；fresh process/call=1、4 segment workers、
  candidate串行、thread env=1均冻结。下一步审计SHA后启动isolated build。
- v3 protocol SHA=`d0729a68...4bf89`、builder SHA=`e43e585c...32ed1`、parent v2 SHA=
  `60b4ebd6...518d2`；K8 manifest与6-part K16 prefix SHA exact。现在允许isolated
  build，仍禁止score/heldout/Full。
- v3 isolated build已启动，exec cell=`1084`；每candidate 8 fresh children、4并行，
  CPU-only。等待自然child logs/manifests，不启动第二实例。
- 首个 t005/K16 isolated candidate已有 segments 0/1/2/4/5/6=`6/8 PASS`，每个exactly2
  hull；wall约1.5–49.2s，warning仅记录cap concavity。segment3/7仍运行，fresh-process
  合同目前消除了same-process cap漂移。
- 下一轮30s无新stdout，segment3/7 complex children继续运行；无RC/error。
- 再一轮30s仍无新输出；同一两个complex children active，继续等待。
- 后续30s无新stdout；已向用户同步6/8 PASS与complex child等待状态。
- 再一轮30s无输出/错误；继续自然等待segment3/7。
- 后续30s仍active无新stdout；未触发timeout/kill。
- 2026-08-01 本轮恢复已完整重读 `experiment-planning-zh`、Tracker、E182 plan、
  当前 progress 与工作树；再次确认 Full 只允许本机 GPU0 + 远程 RTX 6000 Ada
  GPU0/1 叠加运行，禁止 A100、kill、暂停或抢占。当前仍只等待 v3 isolated CPU build，
  production SHA/P launch floor 未闭合前禁止 Full 与 heldout。
- v3 isolated build 已自然 RC1：首个 `t005/K16` 的 segment0/1/2/4/5/6/7 均
  `PASS hulls=2`，但 segment3 fresh child 返回 `actual=3 > cap=2`；segment7 约
  `292.88s` 后 PASS。8 个 child 的 native log 均保留，失败 candidate 无总 manifest。
  session 已结束，未 kill/重启，也未启动第二实例。
- 这表明 per-call fresh process 能消除同进程 native state 污染，但不足以使复杂
  segment3 稳定服从 CoACD 的硬 hull cap：此前同参数 fresh probe 曾返回2，本次返回3。
  不得原样重跑 v3 或把 partial candidate 纳入 screen；这是 x2×y4/cap2 路线的第三次
  build-level失败证据，下一步按三次失败协议重新审视“每段等额 cap”基本假设。
- source audit 确认 v3 child 确实是独立 Python subprocess、每 child 只调用一次
  `coacd.run_coacd`，参数仍含 frozen `seed=1`；当前 coacd Python API 没有额外全局
  RNG reset 接口。因此 segment3 的 2/3 hull 漂移不能再归因于同一进程连续调用。
- 为评估非均匀 global-K 预算，首次只读凸包 inflation 脚本误用了不存在的
  `segments/manifest.json` 并立即 exit2；实际 authority 文件为
  `segments/segment_manifest.json`。该错误未写 artifact、未运行 CoACD，下一步只用
  正确路径计算8段的 deterministic convex-hull代价，不重复错误路径。
- 正确只读审计显示8段若各自退化为单凸包，volume inflation ratio依次为
  `1.2388/1.0214/1.0186/1.2716/1.0797/1.0205/1.0177/1.2168`；segment2（其次
  segment6/1）是牺牲一个局部 hull 预算的最低几何代价，而复杂 segment3/7 应获得
  更多预算。这直接否定“8段等额 cap”假设，支持 task-aware/global budget allocation。
- 资源全文复核再次确认 E182 plan/Tracker/log/launcher 只允许 local GPU0 + remote
  RTX 6000 Ada GPU0/1；A100 仅出现在“不使用/不得套用”的禁止性表述，remote Full
  入口仍明确未实现且会拒绝手工启动。本次没有启动任何 GPU/Full，也没有操作已有进程。
- goal continuation 恢复审计：HEAD=`1efe6bd`，工作树仅含预期 E182 S2/progress
  修改与未跟踪脚本；Tracker仍为 Gate0/Gate1 PASS、S2进行中。已按 skill 重新读取
  当前 plan/log/progress；v3失败后真实下一入口是设计独立 v4 protocol，而非重跑
  v1/v2/v3、放宽 P floor 或提前启动 Full。
- v4设计收敛：每个 threshold 只构建一次 fresh-child CoACD base decomposition；不再
  将 CoACD `max_convex_hull` 当最终硬门。K8/16/32均从同一 base 派生，只允许同一
  x2×y4 exact segment 内确定性 merge，直至 global hull count≤K，禁止跨 segment
  merge，从结构上保留阻断 cavity bridge 的拓扑约束。
- 现有 `evaluate_task_queries.build_exact_union_mesh` 已固化 manifold exact union，
  E182 candidate digest/atomic manifest/export helpers可复用。v4 reducer可用 pair convex
  hull 相对 exact pair-union 的新增体积作为几何 merge cost，并用稳定 lineage/index
  tie-break；最终仍由 unchanged真实 P/R/G floor决定能否 launch，不以 merge cost替代。
- v4 TDD 已先新增 direct-main 合同测试（builder尚未实现）：冻结9-row family=
  `3 thresholds × K8/16/32`、每threshold单一 base cap4、v3 segment3 `3>2`失败证据、
  global-K硬门、每segment至少1 hull、禁止跨segment merge、保守凸包与稳定 lineage
  确定性。下一步运行预期 RED，再实现最小 builder/reducer；尚未 freeze/build/score。
- v4 RED 已按预期在缺少 `build_segmented_coacd_attempt2_v4` 时触发
  `ModuleNotFoundError`，未运行 native build。现已新增未冻结 builder：3个 threshold
  base各由8个 fresh child/4并行构建，CoACD cap4仅作软请求并保留超限返回；确定性
  exact-union added-volume reducer强制最终 global K≤8/16/32，生成9个候选与独立
  dev-only fixture。实现尚未 GREEN/静态检查，当前禁止 freeze/build/score。
- v4功能测试首次已 `4/4 PASS`，覆盖 family/父失败证据/global budget/拓扑隔离/
  保守性/确定性；静态首轮仅有一个 `sorted(list(...))` 的 C414 和两个文件待格式化。
  C414 已作等价 mechanical 修复；下一步只运行 formatter并复跑完整 pre-freeze checks，
  仍未写 protocol 或启动 CoACD。
- v4两文件已 mechanical format；`4/4 PASS`、ruff、format-check、repo diff-check均
  GREEN。pre-freeze source review发现必须补强两点后才可冻结：protocol需直接绑定
  v1/v2/evaluator依赖源码与完整 CoACD参数；final part允许至256 vertices时，后续
  MuJoCo sidecar必须显式 `maxhullvert>=actual` 并验证 compiled hull，否则 exact-C/
  D_C 与 physics C 可能因默认/64顶点简化而不一致。当前仍无 v4 artifact。
- pre-freeze补强已实现：protocol将完整冻结 `COACD_COMMON`，直接绑定 v1 export/digest、
  v2 CoACD/export、exact manifold union 三个依赖源码 SHA，并在 load 时逐项复核 v3
  7个成功child证据。physics合同固定 `maxhullvert>=256`、compile asset/geom/pair与
  真实 MuJoCo contact replay为 Full 前硬门。`4/4 PASS`与全部静态检查继续 GREEN；
  尚未 freeze/build/score。
- v4 final pre-freeze：v1/v2/v3/v4 decomposition direct-main合计 `13/13 PASS`，8文件
  ruff/format、v4 compileall、repo diff-check全 GREEN，且 v4 root 无任何既有文件。
- v4 protocol现已正式 `FROZEN_BEFORE_V4_BUILD_OR_SCORE`：SHA=
  `181e099b2be72239e12387214eb342fe8b19261dee2a402b0145f0b9d1a258ab`，builder SHA=
  `22037d649826402733671d481f24e20812d6217fc91fc48131fab686d1a3c71a`；9候选、
  K=`8/16/32`、threshold=`5/10/20mm`、heldout未访问。此后不得修改v4 builder；
  下一步只允许按冻结源码 build-bases。
- v4 build-bases preflight：load=`1.13`、available RAM=`114GiB`，无既有 v4/CoACD
  build进程；swap虽满但内存余量充足，builder/protocol SHA exact。CPU-only build已按
  frozen入口启动，exec cell=`1135`；3个threshold串行、每个8 child/4并行，不使用
  GPU/heldout。等待自然输出，不启动第二实例或发送终止信号。
- v4 t005 base首轮已有 segment1/2/4/5/6=`5/8 PASS`，均 exactly4 hull，wall约
  `1.46–10.73s`；cap-concavity warning只作为base质量证据，不是失败，因为v4已明确
  将 cap4视为软请求并由后续 reducer控制global K。segment0/3/7仍自然运行，session=
  `34733`，继续同一进程等待。
- t005 segment0随后 `PASS hulls=4 wall=49.20s`，当前 `6/8`；segment3/7仍是预期
  complex children且无 traceback/OOM。继续等待 cell=`1139`，不并发重启。
- 后续一轮30s无新stdout，t005 segment3/7仍active；这是历史上约100–293s的复杂端段，
  当前没有新失败信号。session=`34733`继续自然运行，下一wait cell=`1141`。
- 再一轮30s仍无新stdout/error；segment3/7继续 native计算，未触发任何 timeout，
  下一wait cell=`1143`。纯监控轮询后续归档时可删，当前保留执行入口。
- 后续30s状态不变，session仍active且无异常输出；继续同一 build，下一wait cell=`1145`。
- 再轮询30s仍只有complex child计算、无stdout/error；下一wait cell=`1147`，不作early stop。
- 后续30s仍active无新输出；下一wait cell=`1149`。未启动其他CPU/GPU任务。
- 又一轮30s无新stdout/error；complex children累计时长仍在历史量级内，下一wait
  cell=`1151`，继续等待自然完成。
- t005 base已完整 `PASS 32 hulls`：segment7/3分别 `4 hulls`、wall=`292.58/305.56s`，
  base wall=`307.97s`；这正好给K32零merge基线。t010随后 segment1/2/5/6各自然3 hull、
  segment4=4 hull（当前5/8），`No more hulls to merge`被正确保留为实际base count而非
  cap失败。session=`34733`已自然进入第二threshold，下一wait cell=`1153`。
- t010 segment0=`PASS hulls=4 wall=32.10s`，当前 `6/8`；segment3/7继续运行且无异常，
  下一wait cell=`1155`。
- t010 segment3返回 `5 hulls > requested4`、wall=`75.25s`，但按v4 frozen语义仍
  `BUILD_PASS`并保留全部base parts；这首次在真实构建中验证“软CoACD cap + 后置
  global reducer”确实消除了v1–v3的build blocker。当前 `7/8`，只等segment7，
  下一wait cell=`1157`。
- t010 segment7后续30s仍active无stdout/error；session=`34733`，下一wait cell=`1159`。
- t010 base已 `PASS total=29 hulls wall=142.25s`，segment7=`4/130.28s`；包含
  segment3的真实5-hull soft-cap violation。t020已自然开始，segment1/2/5/6均1 hull、
  segment4=2、segment0=4（当前6/8、已知10 hull），只等segment3/7。下一wait
  cell=`1161`，无GPU/heldout访问。
- v4三base已自然 `BUILD_PASS count=3` 并退出RC0：t005/t010/t020 total hulls=
  `32/29/18`，wall=`307.97/142.25/66.14s`；t020 segment3/7各4 hull、wall=
  `39.08/58.11s`。全过程无OOM/crash/重启，t010 segment3的5>4由soft-cap合同正确吸收。
  下一步先做manifest/part/SHA/topology审计，再运行CPU-only deterministic reducer；
  尚未生成candidate、fixture或score。
- base artifact审计 PASS：summary SHA=`9a28e2a3...4fc`；三base child/part SHA exact、
  segments均`0..7`、全部convex/watertight/winding-consistent、max vertices=32；per-segment
  counts分别 `[4,4,4,4,4,4,4,4]`、`[4,3,3,5,4,3,3,4]`、
  `[4,1,1,4,2,1,1,4]`，只有t010 segment3记录软请求超限。
- frozen v4 reducer首次build自然RC1：t005 K8/K16/K32均先成功（final=`8/16/32`，
  merge=`24/16/0`，wall<0.83s）；进入t010 K8时，某 pair 的 manifold exact-union
  volume略大于其combined convex-hull volume，超过冻结的 `1e-10*max(1,V)` 容差，
  触发 `convex merge volume is smaller than exact pair union`。三个t005 manifests保留；
  v4 protocol/source不得修改，也不原样重跑。下一步量化数值差并另起v5 tolerance合同。
- v4 tolerance诊断：全量模拟t010→K8的21次merge只出现1个负差，位于segment7
  lineage `(1,)/(2,)`：raw=`-6.765058886e-10m³`、pair union=`0.00553938667m³`、
  relative=`-1.2213e-7`；t020→K8的10次merge为0个负差。建议v5数值容差冻结为
  `max(abs=1e-9m³, rel=1e-6*union_volume)`，容差内clamp0并显式记录raw/clamp；这比
  实际误差约大8.2×（relative），仍远小于几何/查询mm级口径。
- v4 partial artifact核验：仅t005三候选有完整manifest/parts，SHA分别
  `99204c90...fe50/82dd9c84...6e11/72d6df4f...7dc`，max part vertices=`79/73/32`；
  t010无candidate manifest/parts，build_summary按预期缺失。v5将复用三base，不重跑
  CoACD，并生成独立候选root/identity。
- v5 TDD已先新增4项合同：独立9-row identity、完整绑定v4三候选prefix与精确负差、
  t010真实roundoff只clamp一次且trace可审计、三threshold的K32→K16→K8 merge trace
  必须为同一确定性hierarchy前缀。RED按预期为缺少v5 builder的
  `ModuleNotFoundError`；未修改冻结v4或生成v5 artifact。
- v5未冻结builder已实现：复用v4 immutable三base，不重跑CoACD；容差固定候选为
  `max(1e-9m³,1e-6×pair-union-volume)`，容差内added cost clamp0但manifest保存raw/
  tolerance/flag，外部仍hard fail。真实base功能 tests首次 `4/4 PASS`，包括t010只
  clamp一次和三threshold nested hierarchy；静态仅import order与builder待format，
  下一步mechanical fix后再做pre-freeze closure。
- v5 import/format已mechanical修复；自身 `4/4 PASS`、ruff/format/compileall与repo
  diff-check均GREEN。v1–v5全套pre-freeze复验正在cell=`1178`运行；v5 root在启动前
  尚无artifact，完成前不freeze。
- v1–v5 decomposition全套已 `17/17 PASS`，10文件ruff/format与repo diff-check均
  GREEN，v5 pre-freeze root为空；builder SHA=`aa939806...c5095`。
- v5 protocol已正式 `FROZEN_AFTER_V4_BASE_BEFORE_V5_CANDIDATE_OR_SCORE`：SHA=
  `e18505459d5da30cfd824554d2bb83e738e8240e3e8c29c6f0d442fe4949b303`，9候选、
  heldout未访问，冻结误差raw=`-6.7651e-10m³`/relative=`-1.2213e-7`与容差
  `max(1e-9m³,1e-6×V_union)`。下一步仅运行frozen v5 candidate derivation，不修改源码。
- frozen v5 candidate derivation已 `BUILD_PASS candidates=9`、RC0；实际hulls按threshold为
  t005=`8/16/32`、t010=`8/16/29`、t020=`8/16/18`，全部≤K且每个8 segment至少1 hull。
  t010 K8/K16各精确记录1次negative-roundoff clamp，其余0；全9 wall合计约3.10s，
  未重跑CoACD、未访问heldout/GPU。
- 9-candidate artifact审计PASS：build summary SHA=`cc6d2569...49d0`；全部manifest/
  ordered part SHA、evaluator candidate digest、part count/global K exact，max final vertex
  counts=`79/73/32, 73/68/32, 73/55/32`，均低于冻结256。K8 merge added volume分别
  `0.004881/0.004007/0.006322m³`；t010最低，但selection仍必须由真实P/R/G决定。
- v5 query fixture已在任何score前 `FROZEN candidates=9`：SHA=
  `7e8d229ba0b6c0e97430184b8ed71a575c49a7ae70c09c8a32df0d4b02984653`、candidate
  identity SHA=`331b5014...d246`，仅bucket003 dev case，heldout未访问，实际hulls=
  `[8,16,32,8,16,29,8,16,18]`。
- screen source audit发现原 `run_task_query_screen`与`evaluate_task_queries`分别硬编码
  `54/18/6`和`54 candidates+3 cases`，不能直接消费v5 fixture；且derived fixture位于
  新root，必须确认tier-index路径解析不依赖fixture parent。冻结v5 evaluator依赖源码
  不得修改；下一步以独立adapter/runner只放宽authority shape并复用原数学函数，先TDD
  验证indices/SHA路径，再冻结v5 screen protocol，不能直接启动score。
- index audit确认derived fixture的case仍指向relative `case_indices/...npz`，原evaluator
  会错误地相对v5 root查找；权威NPZ实际仍在原fixture root且SHA exact。已先新增v5
  adapter/screen TDD（实现尚缺）：要求只接受1 case/9 candidate frozen fixture、显式
  复用原fixture的197/581/1669 nested indices与SHA、每K严格3 threshold候选，并直接
  绑定原 `P_R_G_WORST_NORMALIZED...V1` scoring合同。下一步运行预期RED。
- v5 screen adapter RED已按预期为缺少 `evaluate_task_queries_v5`；未执行score。原runner
  aggregate字段/TSV与selection函数已完整复核，可直接复用normalized score和
  `_select_group`，独立runner只需把总数改为9、每K group改为3、group总数改为3，
  并冻结adapter/evaluator/scorer/runner四份source SHA。
- v5 authority adapter与9-row runner已实现（未冻结）：仅monkey-patch fixture count与
  indices原root解析，所有oracle/exact-C/P/R/G函数仍调用原evaluator；runner复用原score/
  selection并输出同schema TSV/aggregate。首测fixture authority PASS，但nested-index
  测试错误假设数组是prefix而失败；实际冻结合同只保证集合嵌套。静态另有Mapping import
  与3文件format机械项。下一步改为set inclusion断言并mechanical fix，尚未score/protocol。
- nested indices测试已修正为冻结的set-inclusion语义（197⊂581⊂1669），Mapping import/
  format机械完成；v5 adapter/runner tests=`3/3 PASS`，ruff/format/diff-check GREEN。
  下一步必须先跑max_cem_chunks=1的selection-forbidden real adapter canary，验证oracle
  cache与candidate链，再做完整pre-score source freeze；仍无selection-eligible result。
- adapter已增加固化的 `bounded-real` CLI：固定t010/K16、screen tier、1 CEM chunk，
  audit oracle/candidate status、fixture/candidate SHA、finite、chunk count、selection=false、
  heldout隔离；不读取score作选择。更新后adapter tests仍 `3/3 PASS`，三文件ruff/format/
  diff-check GREEN。下一步执行该canary；screen protocol仍未freeze，完整score禁止。
- v5 adapter bounded-real已 `PASS queries=2,077,242`，audit SHA=`d4edfb35...8ef9`、
  result SHA=`4b8f9231...55c5`；8项schema/SHA/finite/1-chunk/selection-forbidden/heldout
  checks全PASS，candidate wall=`0.254s`。这证明derived fixture→原indices→oracle→原P/R/G
  evaluator链可运行。
- bounded t010/K16结果按预注册仅作integration、禁止selection；其静态P指标确实为
  precision/recall=`0.636/0.259`并导致10/12 floor FAIL，point p90=`24.9mm`、R/G one-chunk
  无rank/mask分歧。不得据此提前挑/拒candidate；正式9-row screen将复算并统一裁决。
- v5 screen pre-freeze closure：core geometry=`9/9`、原score=`3/3`、v5 adapter/authority=
  `3/3`、v5 reducer真实hierarchy=`4/4`，合计`19/19 PASS`；9文件ruff/format、adapter/
  runner compileall、repo diff-check全部GREEN。screen root仍无任何文件，因此满足
  selection-eligible score前冻结条件；下一步写唯一screen protocol并审计source SHA。
- v5 screen protocol现已 `FROZEN_BEFORE_SELECTION_ELIGIBLE_V5_SCORES`：SHA=
  `aa454d408e4d3fca5105536108532ee5d318319c3e4021e6f8513cf7e6a36050`；fixture SHA=
  `7e8d229b...4653`、candidate identity=`331b5014...d246`，core/adapter/scorer/runner
  source SHA均冻结，1 case/9 candidates/每K 3 threshold，heldout未访问。
- 正式selection-eligible v5 screen已按frozen `run-all`启动，exec cell=`1209`；先流式
  建bucket003 D_M oracle cache，再串行9 candidates，CPU-only，不使用GPU/heldout，
  不启动第二实例或修改源码。
- screen首轮30s无stdout/error，session=`70907`仍active，当前预期在构建约123.7M
  query的共享D_M cache；继续等待cell=`1211`，不作early timeout。
- formal screen首个 t005/K8 已 `COMPLETE floor=FAIL wall=21.28s`；这是selection-eligible
  单row但尚不能形成K/group结论，继续串行剩余8个。session=`70907`，下一wait
  cell=`1213`。
- t005/K16与K32随后均 `COMPLETE floor=FAIL`，wall=`23.17/25.82s`；因此t005
  threshold的三个K都未过floor，但t010/t020尚未完成，暂不下object/K verdict。
  当前3/9，下一wait cell=`1215`。
- t010/K8=`COMPLETE floor=FAIL wall=22.75s`，当前4/9；继续同一session等待K16/K32，
  下一wait cell=`1217`。
- t010/K16/K32均 `COMPLETE floor=FAIL`，wall=`22.63/21.91s`；t010三个K也全未过。
  当前6/9，只剩t020 family，下一wait cell=`1219`；仍等待完整aggregate后判断具体
  failed checks与是否任一K有launchable candidate。
- t020/K8/K16=`COMPLETE floor=FAIL`，wall=`21.39/23.68s`；当前8/9，仅余t020/K32。
  到目前0 PASS，但不得在最后row与aggregate前宣布最终0/9，下一wait cell=`1221`。
- formal v5 screen已自然 `9/9 COMPLETE` 并RC0；最后t020/K32=`FAIL wall=23.46s`。
  aggregate=`COMPLETE pass=0/9 selected=0/3`，K8/K16/K32三个group均
  `NO_LAUNCH_FLOOR_CANDIDATE`；aggregate SHA=`544c2b49...a184`、TSV SHA=
  `52475b13...c7a9`。因此bucket003 P/R/G launch floor仍未闭合，按plan继续禁止
  S3 grid-SDF、production freeze、heldout与Full。下一步必须逐row分析失败门与空间
  pattern，不能放宽floor或按score强选。
- 9-row失败归因已明确为纯P trade-off：D/R/G全部极佳（sign=`3.86–6.62e-6`、deep≈0、
  SDF p90=`2.0–4.4mm`、R error=`0.1%`/Spearman=`1`、G mask/fallback/topk均无分歧）。
  t005 precision过但recall仅`0.407–0.481`；t010 precision/recall=`0.636/0.259`；
  t020 K8 recall=`0.704`过但precision=`0.655`，K16/32=`0.692/0.667`均略低门。
- K8端点对应oracle 27个contact poses：t005约TP13/phantom4/missed14，t020约
  TP19/phantom10/missed8，显示不同threshold/segment在true-contact recovery与phantom间
  可互补。下一路线不再改全局threshold或放宽0.70 floor，而是在冻结parent结果后做
  dev-only per-segment threshold attribution与3^8 hybrid枚举，寻找≤K且P双门同时通过的
  组合，再由完整P/R/G复核；仍禁止heldout/Full。
- 已复核P evaluator权威语义：仅reference+E178-final共882 static poses，对18个physics
  robot geom采样点做radius-adjusted exact mesh SDF，pose内min≤0为contact；candidate
  segment union的contact OR可与全candidate结果逐pose核对。v6 TDD已先新增（search尚缺）：
  绑定v5 0/9 aggregate与9 source candidates、枚举3^8 assignment、保持P precision/recall
  均≥0.70、global actual hulls≤K，并用synthetic互补/over-budget反例验证。
- v6 RED已按预期为缺少search模块；现已实现未冻结searcher：protocol绑定v5 screen/
  table/9 result+manifest/core source SHA；analyze阶段为每个K×threshold构建8个segment
  exact-union contact bitset，要求OR后与formal P confusion五个count逐项exact，再枚举
  6561 assignments、budget/P双门筛选并按冻结词典序保留每K top3。实现尚未GREEN，
  当前不得freeze/analyze或生成hybrid asset。
- v6 pure/source tests首次 `3/3 PASS`（parent authority、互补组合、over-budget拒绝），ruff
  GREEN；仅searcher待format。pre-freeze review将segment attribution NPZ改为现有atomic
  writer，避免中断留下partial arrays；下一步mechanical format、复跑与确认v6 root为空。
- v6 pre-freeze closure：search tests=`3/3` + core geometry=`9/9`，ruff/format/compileall/
  diff-check全GREEN，v6 root为空；searcher SHA=`b9f6a2ba...8316`。
- v6 search protocol已 `FROZEN_BEFORE_V6_SEGMENT_ATTRIBUTION_OR_SEARCH`：SHA=
  `ec9195ed9a660ad40038f079e96f21dc3a0550d2ee07137a14e87df5e20942f4`；固定882 pose、
  8-segment OR必须exact重现9个formal P counts、每K枚举6561、P双门0.70、budget≤K、
  每K保留top3，heldout/Full禁止。下一步运行frozen analyze-search。
- frozen v6 attribution/search已自然 `COMPLETE selected=0`、RC0：没有任何K产生同时满足
  global budget与P precision/recall≥0.70的per-segment threshold assignment。该结论尚需
  审计三K parent OR exact、passing counts/NPZ SHA与best near-miss；不得生成空hybrid
  asset或放宽floor。
- v6 artifact审计PASS：search SHA=`df1e4557...60e4`，K8/K16/K32 bitset SHA=
  `27af6db2...146b/d09f5ba0...b41/a9d775eb...162`；9/9 parent segment-OR均与formal
  P五个confusion counts exact，oracle contacts=27，三个K各`0/6561` P双门通过。
- exhaustive near-miss归因：K8最优仍为segment3=t020、其余近t005，P=`0.655/0.704`
  (TP19/phantom10/missed8)；K16/K32最优=`0.692/0.667`(TP18/phantom8/missed9)。882
  poses中只有segment3/7产生contact，segment3贡献绝大多数：K16 t020的4个未合并base
  parts给TP18/phantom7；把segment3四part全凸合并为K8会到TP19/phantom9。说明threshold
  组合不是自由度，真正下一杠杆是segment3四个base parts的部分merge partition：目标
  在保留TP19时只新增≤0个phantom（相对K16），不改floor。
- v7 TDD已先新增并RED（缺少search模块）：要求绑定v6三个K均0 passing、枚举segment3
  四base-part的全部15个canonical set partitions，并exact重现冻结K16/K32 best near-miss
  context assignments/counts。v7只搜索K16/32（K8在8 segment最少1 hull约束下无法给
  segment3部分merge自由度），协议与结果尚未生成。
- 2026-08-01 用户再次确认 Full 调度口径：远程只使用 `spider-remote` RTX 6000 Ada
  GPU `0/1`，不使用 A100；本机单卡与远程双卡均可和正在运行的程序叠加，launcher
  禁止 kill、暂停、抢占或修改既有进程。已复核 plan 200 的资源表、S6、命令、风险项
  和 checklist 均已按此口径写明；当前 bucket003 P launch floor 未闭合，仍不启动 Full。
- 持续目标恢复：已重新读取 `experiment-planning-zh`、Tracker、plan 200、log 247 与
  progress；当前权威入口仍是先验证 v7 segment3 四 base-part 的 15 个 partition 搜索，
  再 freeze/search/audit。Full 资源合同保持本机单卡 + Ada 0/1 叠加且不处置既有进程；
  bucket003 完整 P launch floor 通过前继续禁止 heldout、production freeze 与 Full。
- v7 首轮功能测试 `3/3 PASS`：parent authority、Bell(4)=15 canonical partitions、
  K16/K32 的真实 v6 near-miss context 均被覆盖；v7 artifact root 在 pre-freeze 时不存在。
  静态首轮发现两个实现卫生问题：import order `I001` 与未使用的 `counts` (`F841`)，另有
  formatter 机械改动；未 freeze、未 search、未访问 heldout/GPU。下一步只做等价清理并复验。
- v7 的 `I001/F841` 已作不改变搜索语义的机械清理：按模块顺序重排 import，并删除未使用
  的 `segment_hull_counts` 局部变量；尚未用修后源码 freeze 或运行正式搜索，下一步执行
  formatter、功能/静态/pre-freeze 全套复验。
- 修后首轮功能仍 `3/3 PASS`、format/compile/diff-check GREEN，但手工 import 重排仍被
  ruff 判 `I001`（同源模块 alias/group 的顺序）；已用 ruff safe fix 机械整理且 `0 remaining`。
  这是同一静态问题的第二次清理，不涉及算法或 artifact；下一步必须完整复验后才能 freeze。
- v7 源码审阅确认其搜索逻辑是：冻结 v6 near-miss context，保持其他7个 segment 的 contact
  OR，只把 t020 segment3 四个 base part 枚举 Bell(4)=15 分区；每 block 用 source vertices
  convex hull，并按 P precision/recall≥0.70 与 global K 选 top3。审计 v6 protocol 时首次
  手写猜错 result root 导致只读 `FileNotFoundError`，未写 artifact；下一步从脚本常量解析
  权威路径，并重点确认 v7 是否直接冻结其运行时依赖的 v6 source SHA。
- 权威 v6 protocol 确实绑定其自身 source SHA，但 v7 search 会直接 import 当前 v6 helpers，
  仅校验“v6 protocol 文件 SHA”不能防止运行时 v6 source 漂移。已在 v7 pre-freeze 源码中
  直接绑定 core evaluator、v6 helper module、v7 search 三份 SHA，并新增对应合同测试；
  尚未 freeze/search，下一步验证该补强与全套 parent contracts。
- v7 pre-freeze 功能 closure 已 `41/41 PASS`：core P geometry 9、real evaluator 2、
  screen 3、v5 authority/screen 3、decomposition v1–v5 共17、v6 search 3、v7 search 4。
  v7 root 明确 `exists=false/file_count=0`，因此没有 pre-freeze score/artifact 泄漏。
  下一步做全部相关源码静态/SHA/diff closure；全绿后才写唯一 v7 protocol。
- v7 final pre-freeze 静态 closure：12个相关文件 ruff/format、compileall、repo
  diff-check 全 GREEN；core/v6/v7 source SHA 分别=`a31eb7e4...fd93`/
  `b9f6a2ba...8316`/`fc2fdb61...609e`，v7 root 再次确认为空。源码 review 未发现
  heldout/GPU/Full 路径；现在允许执行一次 `freeze-protocol`，之后 v7 source 不得修改。
- v7 protocol 已正式 `FROZEN_BEFORE_V7_PARTITION_CONTACT_SEARCH`：SHA=
  `6e8cf951f793b9bc7a4ced02414b3ae7fe3c1258fff64555a94ff34e48feea37`；固定 K16/K32、
  每K 15 partitions、near-miss counts=`TP18/phantom8/missed9`，并直接绑定 core/v6/v7
  三份 source SHA。root 当前只有 protocol、search result 不存在，heldout 明确未访问；
  下一步只允许运行 frozen `search`，不得修改源码或按结果改 P=0.70。
- frozen v7 partition search 已自然 `COMPLETE selected=0`、RC0（约10.3s）：K16/K32
  的 segment3 四-part 15分区均未产生满足 P precision/recall≥0.70 的候选。因此不得构建
  空 candidate、不得放宽 floor 或启动 Full；下一步审计30 rows、result/protocol SHA、
  budget与 best near-miss，确定 decomposition 的下一真实自由度。
- v7 artifact 审计 `PASS rows=30/groups=2/passing=0`：result SHA=
  `3ee38ec25936beeda7aa8be6676e79e723d3b1a5da3527e0a853227b81a3002a`，protocol SHA
  exact；30/30 budget≤K、confusion总数=882、oracle contacts=27、source SHA未漂移。
  K16/K32 对同一 partition 的P行为完全相同，15分区只产生两种离散签名：10个为
  `TP18/phantom8/missed9`，5个为`TP19/phantom10/missed8`；不存在期望的TP19且phantom≤8。
- 决策前已重读 E181 logs 245/246、E182 log247、Tracker 与 plan 200：此前 E181 是
  cavity phantom；E182 已把 broader cavity 降为 report-only，但真实 P 仍证明 bucket003
  的 contact 临界面不可用。v5 global threshold、v6 per-segment threshold、v7 partial merge
  已构成三层不同自由度的负结果；按 plan §三次失败协议，停止同类 CoACD merge 试参，
  先完成 v7 可视化/阶段日志并向用户汇报，再转 task-aware pre-segmentation 或其他方法。
- v7 可视化设计已明确：固定 K16 frozen context，选两种唯一 P signature 的代表 partition
  （`TP18/ph8/miss9` 与 `TP19/ph10/miss8`），从 context 对应 parent segments 重建完整 hull
  union，分别生成四视角3D与XY/XZ/YZ+timeline 2D；输出只作 diagnostic、selection=false。
  复用现有 E182 renderer 时第二次手写猜错 v5 result 子目录触发只读 `rg` path error，未写
  文件；后续全部从 v6/v7 常量和 manifest 解析，禁止再猜 artifact 路径。
- 已新增独立 post-search v7 renderer 与 direct-main tests，不修改 frozen v7 searcher：
  renderer 从 protocol/search/parent manifest 重建 K16 两种代表 union，逐项复算882姿态P，
  输出3D/2D与SHA manifest；`selection_eligible=false/construction_eligible=false`。首轮测试
  `2/2 PASS`，确认两种 signature 全覆盖且重建 actual hull count/convex/watertight exact；
  尚未静态检查或真实渲染。
- renderer 静态首轮仅有 import order 与未使用 `TARGET_HULLS` 两项机械问题；ruff safe fix
  删除无用 import、整理顺序，formatter重排后 ruff/format/diff-check 全 GREEN。下一步复跑
  功能合同并执行真实渲染；输出必须逐项 exact 重现 frozen v7 counts，否则 hard fail。
- v7 visual 已 `COMPLETE signatures=2` 且 artifact audit PASS，manifest SHA=
  `bdfe8533...af2d5`；两组3D/2D均存在且SHA exact。precision-side partition=`[0]|[1,2,3]`
  重现`TP18/ph8/miss9`，代表 phantom 为 reference pose148、C=-4.69mm/M=+0.46mm；
  recall-side全合并重现`TP19/ph10/miss8`，代表 phantom为E178-final pose364、
  C=-4.88mm/M=+0.29mm。下一步必须实际查看四张图并记录空间/时间观察。
- 已实际查看四张原分辨率图：两种 partition 的洋红 phantom 都集中在 bucket 顶部 rim/
  端部唇缘附近的手部点，不是远处 body/leg 噪声；3D整体外形仍肉眼合理，但 hull 边界在
  rim 临界面向外跨约5mm。2D XY/XZ/YZ确认误接触位于同一上缘局部，timeline显示
  reference与E178-final均有离散、多窗口 phantom；全合并只是增加接触窗，未消除原 phantom。
  下一步精确列出从precision-side→recall-side新增的1个TP与2个phantom姿态，完成归因。
- precision-side→recall-side 是严格单调新增3个 E178-final contacts、无移除：pose177是
  oracle真接触（TP+1），pose202/203是oracle free（phantom+2），恰好解释18/8→19/10。
  三个触发点均在上缘同一局部（object-local约 x=`-0.167/-0.118/-0.120`, y=`0.201–0.206`,
  z=`0.197–0.202m`）；全合并把原 `+1.49–2.05mm` clearance 翻为`-0.11–0.88mm`，而两
  phantom的oracle仍有`+2.78/+3.50mm`。这证明需要局部 rim 几何自由度，不是继续全局合并。
- 实际查看时发现3D长标题左侧裁切；已只缩短 diagnostic pose label（partition仍完整保留在
  manifest），不改几何/指标。修后 ruff/format、visual tests=`2/2`、真实重渲染均 GREEN；
  下一步复看新版标题并冻结最终 image/manifest SHA，然后写阶段结果 log。
- 新版3D标题已实际复看，完整可读且几何/洋红点观察不变。最终 visual manifest SHA=
  `fd84a28f82bc3f4b34d6e0a4a19921e6781d620e89e7a753979c884f4ceb5218`，四张 image SHA
  与renderer/helper source SHA均 exact，最终静态检查 GREEN。v7 result+visual evidence
  已闭合；下一步创建阶段 log、更新 Tracker/INDEX，并按三次失败协议向用户汇报转向选择。
- 已新增 log248，完整记录 v5 0/9、v6各0/6561、v7各0/15、30-row审计、三点contact
  transition、四图实际观察、Claims与结果路径；结论为`P_LAUNCH_FLOOR_NOT_CLOSED /
  FULL_NOT_AUTHORIZED`，推荐 task-aware rim-preserving pre-segmentation。Tracker E182 已更新
  为 Gate0/Gate1通过但 bucket003 P v5–v7无候选，并链接247/248/plan；下一步生成 log INDEX
  并做文档/代码/artifact最终 closure 后向用户汇报。
- log INDEX 已重建并包含248。最终 closure PASS：v7 search tests=`4/4`、visual tests=`2/2`、
  4文件ruff/format/compileall、repo diff-check、Tracker/INDEX链接、protocol/result/visual
  heldout隔离与construction=false均 GREEN；三份 SHA 保持`6e8cf951...ea37`/
  `3ee38ec2...002a`/`fd84a28f...5218`。当前工作树还含此前完整 E182 v2–v7未提交证据，
  未做部分提交；Full、heldout、grid-SDF继续禁止，现按三次失败协议向用户汇报路线选择。
- 持续目标恢复：已重新读取 `experiment-planning-zh`、Tracker、plan200、最新log248与
  progress。用户的持续推进目标授权按 plan200 第三次失败分支继续；下一阶段采用推荐的
  task-aware rim-preserving pre-segmentation，但必须先冻结自动分区合同与dev-only evidence，
  不人工编辑 hull、不读取 heldout、不启动 grid-SDF/Full，且不得修改 frozen v5–v7源码或产物。
- v8 工作计划已切换为 risk-atlas→TDD builder→pre-freeze→protocol→dev P screen。源码审计
  确认旧 topology 是对 watertight oracle 做 object-local equal-bounds `x2×y4` exact manifold
  boolean intersection；segment3 是`x0/y3`整块、跨全z，后续 CoACD/merge始终禁止跨segment。
  因 v7 新TP与两phantom在y/z几乎重合而x相差约4.9cm，下一步先量化 segment3 bounds与完整
  transition atlas；若dominant-axis证据成立，冻结“由冲突点排序自动生成全部相邻中点”的
  局部split family，而不是手选一条plane。
- frozen transition atlas量化完成但未算新candidate P：3点 labels=`TP,PHANTOM,PHANTOM`，
  xyz range=`48.65/4.91/5.23mm`，dominant axis自动为x、相对次轴比=`9.30×`；排序坐标
  `[-0.166657,-0.119563,-0.118009]m`产生全部两个相邻中点plane=`-0.143110/-0.118786m`。
  两plane均位于segment3 exact cell内部，boolean二分均2/2 watertight+winding，volume closure
  误差`4.78e-11/-1.27e-12m³`。plan200目前只有原则性第三失败分支；下一步先补 v8 amendment，
  冻结上述自动family、K16/K32和成功标准后才写测试/代码或读取任何新P score。
- plan200 已新增并校验 v8 amendment：family固定为2条task-derived相邻中点plane×3 threshold×
  K16/K32=`12 candidates`；局部二分segment3形成9 segments，K8因拓扑最小9 hull不重跑且不测K4；
  7个未改segment复用frozen v4 bases，只构建两个新children；static P gate明确为`TP≥19且
  phantom≤8`，通过后才准完整P/R/G。0/12时禁止移动plane/手工切面/降floor/heldout。
  amendment链接log248且文档diff-check/5项合同校验PASS；现在才允许进入v8 TDD RED。
- v8 已先新增5项 direct-main 合同：frozen transition atlas/全部相邻中点、两plane exact
  local split、12-row family且无K8/K4、7×3 frozen v4 base SHA复用、static P边界与0.70双门
  等价。RED按预期为缺少 `build_task_aware_preseg_v8` 的 `ModuleNotFoundError`；未创建v8
  artifact、未计算任何新candidate P、未访问heldout/GPU。下一步实现只满足这些合同的
  protocol/geometry foundation，CoACD build与score仍禁止。
- 已复核 v4 base authority为`coacd_bases/t{005,010,020}/segment_000..007/manifest.json`，
  child manifest含BUILD_PASS、threshold、segment、ordered parts与逐part SHA。现已实现未冻结
  v8 foundation：从frozen v7动态重算3-contact transition/axis/全部midpoints；对segment3做
  exact boolean split；生成12-row family；逐项绑定21个未改v4 child manifest/part SHA；
  static P gate复用0.70公式。尚未GREEN/静态检查、无CLI/protocol/build/score artifact。
- v8 foundation功能首轮`5/5 PASS`，v8 root明确不存在。静态首轮仅有builder未使用Path、
  test import order与builder formatter三项机械卫生问题；compile/diff-check无语义错误。
  下一步只做ruff safe fix/format并复验；仍不得freeze或运行CoACD/P score。
- v8 foundation机械清理后 ruff/format/compile/diff-check与功能`5/5 PASS`；builder/test SHA=
  `b785f531...a9b5`/`43a7e36d...5654`。当前实现仍只有 family/geometry/authority函数，没有
  freeze/build/candidate/static-score CLI，因此不具备误启动路径。下一步源码review v4/v5
  child/reducer接口，补全完整builder与artifact原子合同后再扩大TDD；v8 root继续为空。
- 已完整审阅 v4 isolated child/subprocess/base与 v5 generic 9+ segment可用 reducer/export/
  candidate/fixture接口。第二层TDD已先扩展3项合同（实现尚缺）：old segment remap必须为
  `0,1,2,5,6,7,8`；protocol payload直接绑定7类运行时source+12 family+21 reuse manifests；
  static P selection绝不以score救回FAIL row。下一步运行预期RED，再实现protocol与pure
  selection；仍不build/score。
- 第二层RED已按预期在缺少`protocol_payload`处ImportError。现已实现未冻结pure contracts：
  7段old→new remap、只保留static-P PASS的确定性排序、完整protocol payload（atlas、2 split、
  12 family、21 reuse manifests、v4/v5 parent、fixture、physics/reducer、7类直接source SHA）。
  仍未实现freeze写盘/segment export/CoACD/candidate/static score CLI，未生成artifact；下一步
  运行8项功能与静态检查，失败只修foundation。
- pure protocol/family功能已`8/8 PASS`，v8 root仍不存在。静态首轮有 import order 及两个
  为后续build预导入但当前未使用的`_export_part_v2/atomic_json` F401；这是foundation尚未接
  write stages所致。下一步先不为静态绿删除即将使用接口，而是完成已预注册的freeze/build
  implementation后统一ruff；在此之前不freeze、不运行score。
- 2026-08-01 用户再次确认资源调整：E182 Full CEM 只使用本机单卡与
  `spider-remote` RTX 6000 Ada GPU `0/1`，不再使用 A100；三卡均获准与正在运行的
  程序叠加，任何 E182 操作不得 kill、暂停、抢占或修改既有进程。已全文检索确认
  plan200、Tracker 与当前 progress 已采用该合同；本次仅恢复/复核规划上下文，未启动
  GPU/Full、未访问 heldout，也未操作任何现有进程。
- 提交前资源审计补充：plan200 的可执行 Full 命令只指向本地 launcher 与未来的
  `run_E182_remote_a6000.sh`，远程 allowlist 为 Ada `0,1`。对整个 `launch/active`
  的宽范围检索命中了 E168–E179 的历史 A100 launcher，但它们不属于 E182、未修改、
  未执行；E182 范围内不存在 A100 或 GPU `3/6/7` launch 路径。
- 持续目标再次恢复：已按 `experiment-planning-zh` 复核 Tracker、plan200、log248、
  progress 与工作树。当前仍是 bucket003 P floor 未闭合，v8 是唯一获准的下一路线；
  v8 artifact root 明确不存在，因此尚未 freeze、CoACD build 或读取新 candidate P。
  下一步先验证最新扩展的 v8 builder/tests，再做源码与原子/resume合同审计；所有
  heldout、grid-SDF、GPU 和 Full 路径继续禁止。
- 最新扩展的 v8 builder 首轮功能回归已 `8/8 PASS`：transition atlas、exact local
  split、12-row family、21 frozen-base复用、static-P门、9-segment remap、完整protocol
  authority与FAIL不可按score救回均成立。该测试未调用任何写盘stage，v8 root仍需在
  静态审计后再次确认为空；下一步执行ruff/format/compile/diff并逐段review build/score路径。
- v8 最新扩展静态首轮：compileall与`git diff --check` GREEN；ruff仅报import `I001`
  和`parse_args/main`两个`D103`，formatter提示builder需机械格式化。源码首轮审阅确认
  CLI只含freeze/segments/base/candidates/static-P且没有heldout/GPU/Full命令；static-P按9个
  segment exact union后OR contact，FAIL row不会进入selected。仍需补读中段写盘/resume逻辑，
  特别审计child source binding、summary对完整12-row的SHA覆盖和partial artifact拒绝。
- v8 中段review发现必须在freeze前修复的真实resume/source合同缺口：`build_bases`重跑会
  再启动已有child，随后把原native.log覆盖为空并改写child manifest SHA，导致既有composite
  base在resume时漂移；segment/base/candidate/static-P现有resume验证也只覆盖部分字段，未
  全面绑定protocol/参数/manifest SHA。另发现v8运行时直接使用v4的ReducerPart/check/export，
  protocol source roles却只绑定v4 artifact和v5 reducer source，需新增v4 reducer/export源码
  SHA。下一步先为这些反例补TDD，再修实现，禁止在当前状态freeze。
- Parent实现复核确认：v1 `_export_mesh`会检查closed/positive/finite并做OBJ exact roundtrip；
  v2 CoACD export检查convex/watertight/winding与32 vertices；v5 reducer检查9个nonempty group且
  禁止跨segment merge。v5 protocol虽记录v4 source SHA，但v8仅绑定其protocol文件而不调用
  v5 `_load_protocol`，所以仍需在v8直接绑定实际v4 runtime source。atomic_json可用于所有
  manifest；native.log没有现成atomic helper，正确resume策略应是已有完整child直接验证返回、
  不重跑也不重写log。
- v8 pre-freeze TDD新增两项：protocol直接绑定v4 reducer/check/export source；complete child
  resume必须不调用subprocess且manifest/native.log byte-exact不变。RED已按预期首先停在缺少
  `V4_REDUCER_PART_CHECK_AND_EXPORT` role；第二项因direct-main fail-fast尚未执行，但现有
  `_run_base_subprocess`代码确定会进入subprocess。下一步实现两项最小修复并重新跑到GREEN，
  之后继续补严格manifest/summary绑定合同。
- 两项修复已GREEN，v8 tests=`9/9 PASS`：protocol新增直接v4 runtime source SHA；
  `_run_base_subprocess`遇到完整child时先复用child validator并核验native.log path/SHA，随后
  无子进程、无manifest/log改写返回。新执行的native.log也改为tmp+`os.replace`原子落盘。
  当前仍未freeze或写v8 root；下一步增强各stage manifest/summary的protocol、参数和child
  inventory绑定，并以synthetic tamper/resume测试覆盖。
- 又新增candidate/static-P两项tamper TDD。RED首先证实真实漏洞：当前`build_candidate`
  对`parts=[]`使用`all(...)`产生vacuous true，只要status/parameters匹配就会错误resume空
  manifest；static-P stale-result合同尚因fail-fast未执行，但源码只核candidate SHA，预期同样
  会被接受。下一步实现严格candidate inventory/refs validator与static-P protocol+row binding，
  再运行全部11项合同。
- candidate/static-P严格resume已实现并`11/11 PASS`：candidate现在重验E182 identity、
  frozen row/parameters、protocol/base/split/oracle refs、9个nonempty segment、连续part inventory、
  convex/closed/vertex ceiling、per-segment/aggregate统计及asset digest；空parts不再可能vacuous
  resume。static-P result新增protocol ref，并从882 confusion（oracle=27）重算precision/recall、
  hard gate、score与full-PRG eligibility；只绑candidate SHA的stale result会hard fail。
- base-child tamper TDD已RED：合成manifest保持segment SHA和part SHA正确但把threshold改为
  `10mm`，调用冻结`5mm`路径时当前实现仍错误resume，证实仅验segment/parts不足。下一步
  新增统一child validator，严格覆盖protocol、plane/threshold/segment、hull counts、连续part
  identity与convex/closed/vertex metadata；修复后同一validator供subprocess/composite读取。
- 统一base-child validator已接入direct resume、首次写入、subprocess resume/首次log绑定和
  composite child读取；它现在覆盖protocol、plane/threshold/segment、1–16 hull inventory、
  part identity/SHA/convex/closed/32-vertex及native log。首轮测试没有到算法断言，而是旧的
  no-op synthetic fixture未mock新增的`_load_plane_segments`依赖而FileNotFound；这是测试夹具
  缺口，不是artifact写入或实验失败。下一步只补fixture依赖后复验12项。
- no-op fixture已补齐新增validator依赖，v8 contracts=`12/12 PASS`；threshold漂移反例现在
  hard fail，完整child resume仍保持manifest/native.log byte-exact且不启动subprocess。下一步
  对split manifest、base/build/static aggregate summary实施同级引用与完整性验证，确保stage级
  resume也不重写证据或凭`status/count`接受陈旧summary。
- split/segment-summary严格validator已接入，12项现有合同与compileall仍GREEN：split会复核
  protocol/source、2个child exact paths/SHA/closed/positive、plane与volume closure；完成summary
  会在resume时验证2个manifest后直接返回。手工review发现新summary reference额外含
  `plane_index`，不能直接交给要求精确`{path,sha}`的generic helper；现有测试未执行写盘stage
  所以未覆盖。下一步先修这一字段投影并新增synthetic summary测试，再扩展其余summary。
- reference helper已改为严格检查path/SHA且允许同一entry携带排序metadata；`base_summary`现
  绑定protocol、segment summary、12个new child和6个composite base的冻结顺序/SHA/hull totals。
  完整summary resume会逐child执行strict validator、逐base重构期望payload后直接返回，不启动
  CoACD。现有12项、compileall、diff-check GREEN；仍需对build summary与static aggregate同样
  加固，并最终用真实pre-freeze dry artifact隔离测试覆盖stage路径。
- `build_summary`已同级加固：候选参数由单一helper生成；summary绑定protocol/base-summary与
  12个candidate manifests，并逐个重验9-segment inventory、asset digest和actual hulls。
  完成summary resume不再进入reducer或重写wall证据。12项、compileall、diff-check仍GREEN。
  手工review标记一处可读性差的chained candidate-count判断（当前family=12时语义正确），将在
  static aggregate补强时一并改为显式`len(family)==12 && count==12`。
- static-P aggregate严格validator已完成，且candidate-count判断已显式化。aggregate现在绑定
  protocol/build-summary/12 result SHAs，逐row重验frozen identity与confusion/gate/score后重算
  passing set；`pass_count/full_prg/zero-pass/selected IDs`任何漂移都会hard fail。完整aggregate
  resume为纯验证no-op。12项、compileall、diff-check GREEN；下一步补stage级synthetic
  no-op/tamper测试，并做ruff/formatter与v8 root为空的最终pre-freeze closure。
- stage级覆盖已扩为`14/14 PASS`：artifact reference允许plane等排序metadata但拒绝SHA tamper；
  已完成build/static summary调用分别证明只进validator，任何candidate reducer/static evaluator
  调用都会使测试失败。至此write/resume主合同已有直接覆盖。下一步做ruff safe-fix/formatter、
  完整相关测试矩阵、源码禁区检索与v8 root空目录审计；全绿后才允许freeze protocol。
- final static首轮仍只有机械项：builder import `I001`、`parse_args/main`两个`D103`和两文件
  formatter差异；未发现新增逻辑lint。两个public docstring已补齐，尚未运行ruff import fix或
  formatter。下一步执行safe import fix+format，再复跑14项与全相关父合同。
- ruff safe import fix与formatter已完成；修后v8 contracts=`14/14 PASS`，ruff、format-check、
  compileall、两文件diff-check全GREEN。现在进入pre-freeze扩大验证：必须复跑core P geometry、
  v5/v6/v7 parent contracts，检索builder无heldout/GPU/Full route，并再次确认v8 root为空；任何
  父合同或source SHA漂移都会阻止freeze。
- pre-freeze禁区检索只命中`NOT_ACCESSED_DEV3_ONLY`与Full-forbidden/eligibility元数据，未发现
  heldout数据加载、GPU/Full命令或kill路径；v8 root仍ABSENT。父矩阵当前已完成44项全PASS：
  core geometry9、real2、screen3、v5-screen3、decomposition v1–v5=`3/3/3/4/4`、MuJoCo replay3、
  v6=3、v7 search=4；同一CPU session尚余v7 visual与v8末尾输出，继续自然等待，不启动第二实例。
- 父矩阵自然完成RC0：再加v7 visual=`2/2`与v8=`14/14`，合计`60/60 PASS`。12个直接运行
  source文件的ruff/format/compileall、repo-wide diff-check全GREEN；v8 builder/test SHA分别为
  `1d16d4ed...e777db`/`bba25512...8a8d2`，v8 root再次ABSENT。pre-freeze技术closure已满足；
  下一步做最后的protocol payload人工/机器审计，确认12 family/21 reuse/8 source roles及禁区，
  然后才执行唯一`freeze-protocol`。
- in-memory protocol pre-freeze audit PASS：canonical预写SHA=`d1b19069...45fdd8`，固定两plane、
  12 candidates、K16/32、3 thresholds、9 segments、21 reused manifests、882/27 static-P authority
  与8个唯一runtime source roles（含v4 direct source）；root仍不存在。工作计划已将实现/review
  两步标记完成，唯一protocol freeze进入in-progress。下一操作才允许写protocol，写后builder
  source及绑定父证据均不得修改。
- v8 protocol已唯一冻结并post-freeze审计PASS：SHA=`d1b19069c4be0b7f4c33884ca7aab6539ad9080ce0624a84d8eb78cf9e45fdd8`；
  v8 root当前exact 1 file，只有`attempt2_v8_protocol_manifest.json`，status为
  `FROZEN_BEFORE_V8_SEGMENT_OR_COACD_OR_SCORE`且heldout未访问。从现在起禁止修改builder或
  任何8个bound runtime sources。下一步按冻结CLI只执行`build-segments`，再审计4 children。
- frozen `build-segments`完成且artifact audit PASS：2 planes/4 children均watertight、winding、
  positive，volume delta=`4.77535e-11/-1.27153e-12m³`，summary SHA=`5101cae5...de25e`；
  protocol SHA未漂移，root现8 files。下一步运行`build-bases`：仅12个新split children调用
  isolated CoACD，7×3 unchanged segments继续复用v4；CPU-only 4 child并行，不触碰GPU/heldout。
- frozen `build-bases`已启动为唯一CPU实例，4 fresh children并行；约30s后首个已报告PASS：
  plane0/t010/segment4=`4 hulls, 7.936s`。CoACD warning说明4-hull soft request下残余concavity
  超threshold，按冻结合同这是允许保留并由后续task P评估决定，不是build failure。session
  `41861`仍运行；未启动第二实例、未使用GPU/heldout、未发送任何信号。
- 同一base session新增5个PASS，累计6/12：plane0的t005/s4、t020/s4、t020/s3、t010/s3
  分别`29.84/2.06/30.00/43.99s`，plane1/t005/s4=`8.99s`，均返回4 hulls；soft-cap
  concavity warning继续按预注册口径保留。尚未出现RC/OOM/invalid mesh，继续等待余6 child，
  不做early selection或重跑。
- base session新增5个PASS至11/12：plane1 t010/s4=`3.11s`、t020/s4=`1.91s`、
  t010/s3=`47.60s`、t020/s3=`28.25s`，以及plane0 t005/s3=`68.07s`；全部4 hulls。
  仅余plane1/t005/s3，当前无失败；继续等待完整base-summary，不能在最后child前进入reducer。
- 最后plane1/t005/s3=`4 hulls,76.80s`，frozen base build自然RC0；artifact strict audit PASS：
  12/12 new children、6/6 composite bases，protocol SHA未漂移，base-summary SHA=
  `417de337...cfb16`。六base hull totals按plane×threshold为`[36,32,22,36,32,22]`；这意味着
  K16候选需同segment reducer合并，K32对t005也需合并、t010恰32、t020保留22。下一步运行
  frozen `build-candidates`构建12 rows，仍CPU-only且不计算P分数。
- frozen candidate reduction自然RC0，12/12 BUILD_PASS：两plane行为对称，t005 K16/K32=
  `36→16(20 merges)/36→32(4)`，t010=`32→16(16)/32→32(0)`，t020=`22→16(6)/22→22(0)`；
  单row wall约`0.03–1.03s`。尚未计算static P。下一步先用strict build-summary validator审计
  12 manifests、9-segment coverage、vertex ceiling、asset SHA与protocol/base refs，再运行唯一P screen。
- candidate artifact strict audit PASS：build-summary SHA=`3c1d8d80...e057f`，12 manifests均有
  9个nonempty segments、12个唯一asset SHA，actual hulls=`[16,32,16,32,16,22]×2`，最大part
  vertices逐row`[73,38,68,32,55,32,74,40,69,32,67,32]≤256`；protocol未漂移。现在允许运行
  frozen 882-pose static-P一次；必须完成12/12后才看pass_count，不做中途调plane/threshold/K。
- frozen static-P已自然RC0并完整`0/12 PASS`。plane0 rows precision/recall依次为
  t005 K16/32=`.478/.815,.500/.815`，t010=`.528/.704,.559/.704`，t020两K=`.577/.556`；
  plane1为t005=`.571/.296,.727/.296`，t010=`.643/.667,.720/.667`，t020=`.559/.704,.613/.704`。
  没有任何row同时达到0.70双门。按冻结zero-pass action必须停止v8，禁止移动plane、手工切面、
  降floor、完整P/R/G、grid/heldout/Full；下一步只做12-row artifact审计、归因可视化与阶段日志。
- static aggregate strict audit PASS：aggregate SHA=`10666ec...df60b`，protocol/build SHA=
  `d1b19069...45fdd8`/`3c1d8d80...e057f`，12 result SHA均闭合，file_count=381。最佳near-miss
  为plane1/t010/K32(actual32)：`TP18/phantom7/missed9, P=.720/R=.667`；它比v7 precision-side
  少1 phantom但没有恢复第19个TP。工作计划已转入0/12负结果闭合，不再允许full P/R/G。
- 负结果可视化按post-result diagnostic设计，不修改frozen builder：固定三代表为best balance、
  highest TP/high recall、以及TP≥19中最低phantom；每row输出四视角3D与三投影+timeline 2D，
  selection/construction/full-PRG均false。已先新增2项direct-main合同，renderer尚不存在；
  下一步运行预期RED，再实现只读renderer并绑定aggregate/protocol/build/helper SHA。
- visual TDD RED按预期为缺少renderer模块。现已新增独立post-result renderer（未修改frozen
  v8 builder）：严格加载0/12 aggregate与candidate manifests，确定性选择3 rows，逐项复算882
  pose confusion，选最深phantom pose并输出3D/2D；visual manifest绑定protocol/build/aggregate/
  renderer/helper SHA且三类eligibility均false。下一步先跑2项功能与静态检查，失败只修renderer。
- visual功能首轮`2/2 PASS`，确认代表IDs与frozen hull inventories exact；ruff、compileall、
  diff-check GREEN，仅renderer formatter有机械差异。下一步只format renderer并复跑；之后执行
  真实6图渲染，要求每row aggregate exact重现static P，否则hard fail且不写COMPLETE manifest。
- renderer已机械format，修后visual tests=`2/2 PASS`、ruff/format/compileall/repo diff-check全
  GREEN。现在允许执行真实post-result render；这不会修改protocol/build/static-P artifacts，
  仅在`visual_diagnostic/`写6 PNG与manifest，随后必须实际查看原图并记录具体观察。
- 真实v8 visual已`COMPLETE diagnostics=3`并自然RC0；renderer在写manifest前已逐row exact
  重现882-pose frozen confusion。下一步审计visual manifest、6图SHA、source依赖与三类
  eligibility=false，然后用原分辨率实际查看每个3D/2D代表，记录rim位置、phantom/missed时间窗
  及plane0/plane1 trade-off，不依据图反向改候选。
- visual artifact audit PASS：manifest SHA=`1d1ce64e...8a5d3`，6 PNG均存在且SHA exact，三类
  eligibility=false。已实际查看全部原分辨率图：三代表的最深phantom都是reference pose148，
  集中在同一bucket上缘/端部rim手部点，oracle clearance约`+0.46mm`，C侵入分别`-4.01/-4.37/
  -4.77mm`；不是远处body/leg噪声。整体外形肉眼合理，但局部rim convex外包仍跨过接触临界面。
- 2D timeline实际观察：best-balance仅7 phantom但漏9个真接触；high-recall把真接触提高到22的同时
  phantom增至22，尤其reference早段出现一串额外contact窗；TP19代表以12 phantom换回第19个TP。
  phantom同时分布在reference与E178-final的离散窗口，非单一pose异常。三图标题/图例完整可读；
  结论是task-derived单平面切分改变trade-off但未产生第三工作点，按0/12 stop而非继续调参。
- 阶段日志素材已量化：base并行stage wall=`128.42s`（child sum=`348.54s`），candidate reducer
  sum=`4.67s`，12-row static-P sum=`11.27s`；最终v8 root=388 files。progress当前1263行，已超过
  skill的200行归档阈值；下一步先写log249并更新Tracker/INDEX，再将已闭合E182早期过程做完整
  backup+精简归档，保留当前v8结论/错误/下一决策，避免直接丢失实验磁盘记忆。
- 已新增log249，完整记录v8协议、12-row表、耗时、6图实际观察、Claims、SHA、stop action与
  三个后续结构方向；Tracker E182已更新为`v8 12/12构建、static P 0/12、Full禁启`并链接249。
  下一步重建log INDEX，校验文档链接/描述长度，然后执行progress完整backup+归档精简。
- log INDEX已重建为248个日志并包含249；文档audit PASS：log249的8个本地链接均存在，Tracker
  描述35字符≤80且249链接有效，相关doc diff-check GREEN。下一步按skill §14归档progress：先
  保存当前1260+行完整backup，再把当前progress精简为E182状态/冻结SHA/关键负结果/下一决策，
  不删除任何原始实验artifact或log。
