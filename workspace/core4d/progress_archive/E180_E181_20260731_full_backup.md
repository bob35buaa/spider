# CORE4D 当前进度（完整归档）

> 完整备份：
> [E167–E179 初始恢复](progress_archive/E167_E179_20260724_full_backup.md) ·
> [E179 执行过程](progress_archive/E179_20260724_execution_full_backup.md) ·
> [E179 最终收尾](progress_archive/E179_20260725_closeout_full_backup.md)
>
> 本文件只保留最近完成工作的可靠结论与下一会话入口。

## 2026-07-26：E180 RL38 指标线性可分性审计（完成）

- 计划：`plan/198_E180_rl38_metric_linear_separability_audit_plan.md`。
- Authority：38 条 `32 success / 6 fail`；Box021/004/001/024/023 为
  `11/4/13/3/7`。两面板为 reference-input `38×67` 与同口径 metrics
  `36×111`；另有 42 条未进 RL proxy negative。
- 单指标无完美分离。训练内 sparse SVM 可完美分开 RL-only：
  reference 3 指标、standardized 2 指标，但不能当作泛化 gate。
- reference SVM：LOOCV failure recall=`4/6`，按对象留出=`5/6`，但误拦
  success=`10/32`；standardized SVM：LOOCV=`5/6`，按对象仅 Box024
  `028_p2` 命中（`1/6`）。
- 冻结 standardized RL separator 只拒绝 proxy `4/42=9.5%`；将 proxy
  合并拟合时，5 指标模型仍接受 `6/48` failure-like，故联合 hard gate
  当前不成立。
- 报告：`analysis/RL38_metric_linear_separability_audit_20260726.md`；
  日志：`log/243_E180_rl38_metric_linear_separability_results.md`；artifact：
  `results/E180/rl_metric_separability/`。
- 4 项直接单测、py_compile、链接、标题与 diff-check 通过；pytest 环境缺失。

## 2026-07-26：E168–E178 可用率与下游 RL 诊断（含 Box023）

### 最终状态

- 任务：`exp_analysis_0726.md`。
- 报告：
  `analysis/E168_E178_availability_and_RL_diagnosis_20260726.md`。
- 视觉证据：
  `analysis/assets/exp_analysis_0726/video_frames/`。
- 本轮只分析存量数据、人工标签与 rollout，未启动新训练；不更新 Tracker。
- Box023 新增 S6 后验：7 条 `RL_EXPORT_READY` 中 `040_p2`、`042_p2`
  训练失败，其余 5 条成功；两条失败均为人工 `MINOR_ACCEPTABLE`，
  且 E173 六门、十二门均通过。联合集合为 `32/38` RL 成功。

### 核心结论

- 低 yield 是 raw/action/contact、retarget 可解性、template/proxy、
  SPIDER 联合可行域和 gate/RL 接口的分层损失，不是单一算法故障。
- E170 PRG 将 Box021 人工可用从 `13/28→18/28`，但 10 条未恢复 case
  leg penetration `10/10` 改善、3mm hand contact `10/10` 下降。
- E170–E178 candidate gate-health 连续为 `0/N`；combined-valid 约束和
  `least_violation` fallback contract 是最高置信的 SPIDER 共性瓶颈。
- E174 非 box `5/39=13%`，修正 contact-aligned proxy 后 E178 physics
  `16/27=59%`、人工 `12/23=52%`；旧 proxy 是主要可修复损失源。
- 用户给出的下游集合为 `32/38` RL 成功。36 条可直接对齐六门：
  PASS 中 `20 success / 5 fail`，FAIL 中 `10 success / 1 fail`；
  旧六门既非 RL 成功充分条件，也非必要条件。
- Box004 `082_p1/p2` 和 Box024 `028_p2` 是六门漏检。`082` 对象轨迹的
  `94.236rad/s` 角速度峰值在 pre-Omni converted NPZ 已存在；
  `028` 的 pre-Omni 对象线速度也显著高于成功对照。
- converted→retargeted 的对象 quaternion 逐帧一致，故上述对象异常不能
  归因于 Omni solver；raw root 未挂载，仍需区分 raw 标注与 converter。
- Box023 两个失败从前段直立交互转为约中段多环境同步倒地；其 pre-Omni
  对象动态不能与成功对照分离，说明 `082/028` 式病态只解释失败子类，
  还需 phase-stratified 闭环 probe。
- E109 已确认历史 tracking 多为 self-ref/method-ref；没有 raw-GT 统一表前，
  不能宣称 OmniRetarget 整体质量已被证明差。

### 后续入口

1. P0：全库 pre-Omni 对象 SE(3) 连续性审计，先定位 raw/converter 边界。
2. P0：修复 combined-valid 选择；空交集必须定向重采样或拒绝。
3. P0：对失败与成功对照做早/中/末 phase PD、`partner_off/on` 短 probe。
4. P1：补 raw-GT OmniRetarget benchmark、full-3D/support/terminal gates，
   并按 object/date/sequence held-out 校准两级发布策略。

## E179：box023 / E167A no-PRG / Full CEM

### 最终状态与结果

- 同一 16 条 paired denominator；Full budget=`seed 0, 1024×32`；
  本地 4 条、A100 12 条，完成 `16/16`，terminal failure=`0`。
- E167A profile parity、no-PRG audit、视频与 completion audit 全部通过。

| 口径 | E173 PRG | E179 no-PRG |
|---|---:|---:|
| Physics 六门 | `13/16` | `9/16` |
| Physics + tracking 十二门 | `7/16` | `4/16` |

- 十二门迁移=`P→P 3 / P→F 4 / F→P 1 / F→F 8`，McNemar
  exact `p=0.375`。
- lower-body=`14→10/16`；leg penetration paired mean
  `Δ=+0.07363`，95% CI=`[+0.03704,+0.11427]`。
- 16 条 paired 视频复核支持 no-PRG 下肢退化；最终裁决
  `PRG_BETTER`，Box023 保留 E170 PRG。

### Canonical evidence

- 评测：`results/E179/s6_downstream/eval/full/E179_vs_E173_report.md`。
- Summary：`results/E179/s6_downstream/eval/full/e179_eval_summary.json`。

## 2026-07-30：E178 非凸物体 MuJoCo 碰撞表示调研（完成）

- 正式报告：
  `log/244_E178_nonconvex_mujoco_collision_review.md`。
- 结论分层：非凸刚体的引擎表示已经解决；自动获得“少组件、高保真、保任务
  空腔、1024-world 高吞吐”的 collider 尚未完全解决。
- MuJoCo 官方推荐 compound convex，并点名 CoACD。CoACD 是当前最佳开源
  production baseline；V-HACD 已 EOL。Navigation-driven ACD、RL-ACD 和
  CPD 是更前沿的任务/性能方向，但暂不能作为可复现 MuJoCo production 依赖。
- 普通 mesh geom 的碰撞仍是 convex hull。rigid flex 和 mesh-backed SDF
  能表达真实非凸面；E178 锁定的 MJWarp 3.7 rigid-flex 无 broadphase，且
  不支持与 rubber-hand mesh 的 geom-flex narrow phase，不宜进 Full。
  MJWarp v3.11 已增加 flex broadphase，但 flex 仍标为 experimental。
- 本地最小检查确认 `geom type="sdf" mesh="..."` 在 MuJoCo CPU 和 MJWarp
  3.7 均可编译；SDF 因 Halton 多起点与迭代成本，建议只作低批量 oracle。
- SPIDER 上游虽有 CoACD 入口，但 `coacd` 未进入项目依赖，入口未接 Core4D；
  当前 PRG/penalty/gate 又全部绑定 box-union SDF，不能只替换 XML。
- E178 推荐路线：先用 CoACD box mode 做兼容 bridge；目标架构为
  CoACD convex-union physics + 同源 GPU grid-SDF，并加入 cavity/
  navigability/contact-aware gates；原 mesh SDF 作为 fidelity oracle。
- 不把 collider 当作唯一瓶颈：E178 六门 `16/27`、十二门 `10/27`、人工
  `12/23 USE`，combined-valid `0/N` 与 tracking 仍需在 paired ablation 中
  同时报告。

## 2026-07-31：E178 CoACD / canonical SDF 路线审查

- 主路线可行：CoACD 生成 production collision set `C`，P 使用同一组 convex
  hull geoms；从 `C` 烘焙 object-local grid-SDF `D_C` 供 R/G 使用；原始
  mesh SDF `D_M` 只用于 fidelity 与 task-space 审计。
- `D_M→C` 不能只检查全局 Chamfer/Hausdorff；bucket 必须增加 cavity
  must-stay-free、inner/outer/rim must-cover、raw-contact/swept-volume 与
  contact-normal 等 task-aware gates，否则仍会放过“全局误差小但填腔”的
  collider。
- 当前实现缺口明确：`config.py` 的 union mode fail-closed 拒绝 non-box，
  `mjwp.py` 的 reward/penalty/CEM gate 全部绑定 box-union SDF；已有
  `preprocess/decompose.py` CoACD 原型参数固定、未接 Core4D，且 `coacd`
  尚未进入项目依赖。
- 推荐按 `oracle/asset gate → grid-SDF backend contract → convex physics
  canary → paired CEM` 分阶段推进；在 `D_C` 与 MuJoCo hull union 的
  occupancy/zero-surface/坐标系一致性验证通过前，不替换 E178 production。
- `D_M` 前置条件尚需实测：原始 mesh 必须有可靠的 scale、winding、闭合实体
  或显式 generalized-winding/sign 策略；系统 Python 的首轮 `trimesh`
  topology probe 因 `ModuleNotFoundError` 未启动，不能先验假定三个 bucket
  mesh 都是 watertight。
- 改用项目 `uv --frozen` 环境后拓扑 probe 通过：bucket003/004/007 主 mesh
  均为 watertight、winding-consistent，scene scale=`1 1 1`；但 bucket004
  含一个 10-face 零体积组件，bucket007 含 2-face/4-face 零体积组件。
  `D_M` 应来自带 manifest 的最小清洗 oracle mesh，而非未经审计的原 OBJ。

## 2026-07-31：E181 CoACD canonical geometry（计划完成）

- 用户批准以 `cleaned M* → D_M oracle → CoACD C → canonical D_C → P/R/G`
  为主路线；详细计划已写入
  `plan/199_E181_coacd_canonical_geometry_plan.md`。
- `C0–C10` Claims 与 `S0–S6` stop/go 阶段均已冻结；Gate 0–E 未通过前
  不允许启动 Full 27。
- 本次只完成规划文档：未修改实现代码、未构建 collision assets、未启动
  CEM/RL；分阶段 stop/go gates、脚本入口、scene snapshot、可视化和结果
  路径已在计划内冻结。
- 历史基线要求计划拆开四类判据：`C↔D_M` fidelity、`D_C↔C` consistency、
  convex physics/contact 与 `64×4/1024×32` throughput、paired CEM 的
  candidate health/downstream。E175/E176 的高 geom 慢速和
  E170–E178 `combined-valid=0/N` 均不得被合并成一个总分掩盖。
- 编号冻结为 `E181` / `plan/199_E181_coacd_canonical_geometry_plan.md`；
  E178 的 27 条只作为最终 paired authority，首轮必须先用三 bucket
  representative probes 通过离线资产、`D_C↔C`、接触和 `64×4` 吞吐门。
- 计划将 object `mass/inertia/friction/solref/condim` 设为 collider 外的冻结
  contract，避免 compound geoms 改变动力学后把收益误归因于几何。
- 吞吐采用每条 dev3 在其分配设备上同轮交错 A/B；canary 覆盖本地单卡、
  Ada GPU 0 与 Ada GPU 1。保留逐 case `≤3.0s` strict gate，同时报告同设备
  E181/E178 ratio；任何 `3.0s` 以上运行只能走既有显式 waiver，不能改写
  strict 结果。
- CoACD selection 采用 hard gates 后按最小 hull 数词典序选优，不用加权
  总分；当前 E178 canary 三条冻结为 dev3，其余 24 条 contact fidelity 为
  held-out，最终 Full 仍报告 27 与 heldout24 两种口径。
- E181 v1 只接入 canonical `solid_sdf` 并保持既有 R/G 公式、权重和阈值；
  inner/outer/rim 仅作离线审计分层，避免把 collider、backend 与 semantic
  reward 三个变量混成一次实验。
- 用户补充 Full 执行合同：正式 Full CEM 必须复用 E178 的同一 27 条 case，
  方便逐 case paired 对比；计算资源固定为本地单卡与远程 Ada 6000 两卡，
  共三卡并行。远程 profile 已确认是 `spider-remote` 的
  `2× NVIDIA RTX 6000 Ada 48GB`。E178 原始 `ordinal` 存在跳号，不能直接
  取模；计划现已按 canonical manifest 物理行序生成连续
  `authority_row_index=1..27`，再轮转得到确定性 `9/9/9` 分片。计划已同步
  补齐三 worker 的 GPU 归属、环境 SHA parity、远程回收与 27-row merge
  audit；正式 Full 只能由三卡 orchestrator 从冻结 allocation 启动。
- 更新后校验通过：E178 source manifest SHA=`de9a3d...f022a8`，rows/case
  uniqueness=`27/27`，物理行序分片=`9/9/9`；计划中无 A100/旧 E181
  launcher 残留，Markdown 标题、链接、空白和 `git diff --check` 均 PASS。
- E181 已进入持续执行阶段，下一步从 S0 authority/environment preflight
  开始。用户明确授权远程 RTX 6000 Ada 与机器上已有实验叠加运行：E181
  不 kill、不抢占现有进程，只记录显存/进程快照并验证剩余显存；正式启动
  前会把这一覆盖合同同步回计划和 launcher。
- 初始只读审计：当前没有 E181 代码、结果、日志或残留运行进程；本地 RTX
  5090 空闲显存约 `31.95GiB`。远程 Ada GPU0/1 已有用户实验，各占约
  `8.68/7.54GiB`、仍余约 `39.82/40.97GiB`，符合用户授权的叠加前提。
  本地/远程 Git HEAD 分别为 `bcfad012` / `6c0e9e78`，正式部署不能直接在
  共享远程 checkout 上 pull 覆盖现有实验，应使用 E181 独立 remote run root。
- 依赖缺口确认：`pyproject.toml` 尚无 CoACD 和 direct trimesh pin；
  `uv.lock` 只有 `trimesh 4.11.5` 的传递依赖记录，S0 必须先显式 pin 并验证
  CoACD real-metric API。
- 计划已同步用户的 Ada 共存覆盖合同：已有 process 不再构成 blocker，启动门
  改为基于 S5 实测峰值的剩余显存安全预算；launcher 禁止 kill/暂停/抢占。
  为避免影响远程正在使用的 `spider` checkout，E181 将部署到
  `/home/xiayb/pHRI_workspace/e181_runs/<execution_id>/spider/`，以 source
  snapshot SHA 而非共享目录状态作为三 worker 的代码一致性 authority。
- CoACD dependency authority 已核实：PyPI 最新版为 `1.0.11`，官方 README
  明确支持 `run_coacd(..., threshold=<meters>, real_metric=True)`；S0 将 pin
  `coacd==1.0.11` 和 `trimesh==4.11.5`。首次尝试的上游
  `python/package/coacd/coacd.py` raw URL 返回 404，已改由官方 README/PyPI
  JSON 确认 API，不重复使用错误路径。
- S0 dependency 实现已开始：`pyproject.toml` 已显式 pin
  `coacd==1.0.11`、`trimesh==4.11.5`，`uv lock` 成功解析 185 个包并将
  CoACD 写入 lock；下一步补独立 runtime signature/import probe。
- S0 dependency runtime probe PASS：实际导入版本为 CoACD `1.0.11`、
  trimesh `4.11.5`，`inspect.signature(coacd.run_coacd)` 确认包含
  `real_metric: bool=False`。E178 authority manifest 仍为 27 行、71 字段；
  E181 尚无脚本，因此下一步创建 direct-main 的 authority/environment
  preflight 与测试，不复用会改写历史结果的 E178 builder。
- CPU MuJoCo/MJWarp 最小 compile probe 已手工验证：同一闭合 cube 的
  `type=mesh` 与 `type=sdf` 均可由 MuJoCo 3.7 编译，并通过
  `mujoco_warp.put_model`。三个 raw visual mesh 已从 dev3 scene 确认：
  `bucket003/004/007_m.obj`，scene scale 均为 `1 1 1`；S0 builder 将以
  E178 manifest 和 XML 解析结果自动校验路径，不硬编码临时 scene。
- 已新增 S0 authority builder、environment probe、direct-main test 与
  `run_E181_local.sh preflight`。首轮静态检查发现 3 个文件需要 ruff format，
  且 `probe_environment.py` 触发 `UP022`（应使用 `capture_output=True`）；
  这是首次实现检查，尚未运行正式 preflight，下一步先修复再执行。
- `UP022` 已改为 `capture_output=True`，3 个脚本已 ruff format。严格重跑
  ruff check、py_compile、launcher `bash -n` 与 `git diff --check` 全部
  PASS；可以执行正式 S0 preflight。
- S0 authority 第一次正式运行 FAIL，尚未进入 environment probe：
  `bucket003_20231018_001_p1 target_scene` 当前 SHA=`fa14d5...31665`，而
  E178 `base_scene_sha256=cbb5af...f8523`。这表明 E178 manifest 的 baseline
  SHA 不是当前可变 `scene.xml` 的文件 SHA，或源 scene 已在后续实验变化；
  下一步从 E178 scene snapshot/manifest 解析正确 authority，不能放宽 SHA 门。
- 根因已确认：`cbb5af...f8523` 精确对应
  `scene_act_E174_rubberHull_PRG.xml`，E178 effective scene SHA
  `0880f6...11ecd` 对应 `scene_act_E178_contactAlignedTop.xml`；两者在
  E178 snapshot 中也有同 SHA 副本。builder 已改为验证 trajectory/contact、
  E174 base、E178 effective 及两份 snapshot 共 `6×27=162` 项；当前
  `target_scene` 只验证存在性，不再错误套用 base SHA。
- S0 authority 第二次运行在 raw mesh resolver 失败：脚本把 object mesh
  `file` 直接相对 XML 目录解析，产生重复
  `example_datasets/example_datasets/...`。XML 明确设置
  `compiler meshdir=../../../../../../spider/assets/robots/unitree_g1/meshes`；
  MuJoCo 实际语义是先应用 `meshdir` 再解析 `file`。下一步按 compiler
  语义修 resolver，不能硬编码 object asset 根。
- resolver 已按 XML `compiler meshdir` 语义修复；第三次 S0 authority
  正式运行 PASS：`rows=27, dev=3, heldout=24`，E178 source SHA 精确为
  `de9a3d...f022a8`，162 项 input/snapshot SHA 检查通过。launcher 本轮输出
  未显示后续 environment/test，需先检查日志与 shell 执行流再判 Gate 0。
- environment probe 单独运行确认返回 `RC=139`，所以空日志不是 PASS。
  当前实现先用 `mjwarp.put_model` 初始化 CUDA，随后再通过 subprocess/SSH
  抓 Git/GPU 状态；这是典型的 CUDA 初始化后 fork 风险。下一次尝试将所有
  Git/local/remote subprocess snapshot 前移到任何 Warp 初始化之前，再运行
  compile probes；不把 segfault 隐藏为 launcher 成功。
- subprocess snapshot 已全部前移，ruff 通过，但 environment probe 仍未输出
  PASS/JSON，说明 `RC=139` 根因不只在 CUDA 后 fork。下一步将 compile probe
  隔离到独立子进程并逐段定位 mesh/SDF/进程退出点；这是同一 blocker 的第二次
  针对性尝试，不重复当前单进程结构。
- `python -X faulthandler` 已给出确定根因：段错误发生在
  `probe_environment.py` 顶层 `import torch`，栈位于
  `torch._ops/torch.ao.quantization`，尚未进入任何 E181 compile 或 SSH
  逻辑。S0 不依赖 Torch API，已移除该导入；Torch 版本继续由 package
  metadata 记录，CUDA/GPU authority 改用 `nvidia-smi`。
- 移除非必要 Torch import 后，正式 launcher 完整跑通：authority
  `27/3/24`、environment local/remote=`1/2 GPUs`、mesh/SDF 的
  MuJoCo+MJWarp compile、2 项 direct-main tests 全 PASS；E178 source SHA
  仍未变化。最终 artifact audit 又发现 test 用临时 output 调
  `build_authority()` 时覆盖了 canonical `authority_manifest.json` 的
  `outputs` 字段，使其错误指向 `/tmp`。Gate 0 暂不收口，先隔离测试输出并
  重跑 formal builder。
- 首次隔离修复 patch 因 ruff 已改变目标行换行方式而 context mismatch，
  未改动文件；已用精确行号重新定位 `DEFAULT_OUTPUT_ROOT`、
  `build_authority()` 与 canonical write 三处，下一次 patch 使用现态上下文。
- test/canonical 输出隔离已实现，formal builder 重跑后 authority/environment
  再次输出 PASS，canonical manifest 不再由临时 builder 直接写入。严格命令
  没有打印预期的最终 `E181_S0_FORMAL_RERUN=PASS`，疑似
  `test_preflight.py` 在打印两项 PASS 后进程退出异常；需单独检查其真实 RC
  与 canonical `/tmp` 断言后才能收口。
- 单独检查确认 `test_preflight.py RC=0`。最终断言实际失败于 result symlink
  被 `.resolve()` 展开成 `/mnt/.../spider_workdirs/...`，不是测试进程异常；
  虽然文件正确，但不满足 repo-relative artifact contract。builder 已改为
  对 source/output/authority 保留 lexical absolute path，仅在真正需要时解析
  asset 文件 symlink。
- S0 / Gate 0 已严格 PASS：formal authority=`27/3/24`、object
  distribution=`9/4/14`、input/snapshot SHA checks=`162`、E178 source
  SHA=`de9a3d...f022a8`；CoACD real-metric、MuJoCo/MJWarp mesh+SDF compile、
  local/remote=`1/2 GPUs` 与 no-kill remote contract 均有 JSON evidence。
  direct-main tests=`2/2 PASS`，canonical artifact 全为 repo-relative path，
  ruff/py_compile/bash syntax/diff-check 通过。下一阶段进入 S1–S2。
- S1 raw topology 复核：bucket003=`5000 faces/1 component`；bucket004 主体
  `4990 faces` 加 10-face fragment，volume ratio≈`8.2e-7`；bucket007 主体
  `4994 faces` 加 4/2-face fragments，最大 volume ratio≈`7.0e-8`。三个主
  component 均 watertight/winding-consistent，signed-distance/contains
  smoke 一致。计划已把清洗门精确冻结为 `faces≤16` 且
  `abs(volume)/main_volume≤1e-6`，不使用含糊绝对“零体积”判据。
- S1 oracle builder 与 direct-main tests 已实现但尚未正式验收：
  `build_oracle_meshes.py` 将按冻结门删除数值零碎片、以 17 位精度导出
  `cleaned.obj`、验证 OBJ round-trip 与 `D_M<0` inside 的符号合同，并生成
  component overlay / 主轴截面证据；`run_E181_local.sh oracle` 已接入。
  下一步先执行 ruff、py_compile、shell syntax 与 diff-check，再跑正式
  oracle stage；Gate A 必须在 6 张 PNG 人工视觉核查后才可由 PENDING 转 PASS。
- S1 首轮静态检查只发现两个 import-order 问题，已由 ruff 自动修复；随后
  ruff format/check、全部 E181 Python 文件 py_compile、launcher `bash -n`
  与 `git diff --check` 均 PASS。现在可以执行正式 `oracle` stage。
- S1 第一次正式运行在 bucket003 的非视觉门失败并正确中止，未继续构建其余
  对象。清洗/round-trip/watertight/finite 与表面法线偏移检查均正常；
  唯一可疑指标是 signed-distance 与 `contains` 在 1 mm band 外的一致率
  `0.991371`。下一步核对该一致率的采样构成、阈值与 trimesh 两种查询的边界
  语义，先定位不一致点，不能直接放宽 Gate A。
- 初步不一致诊断：10k fixture 中 86 个点不一致，其中 77 个表现为
  signed-distance 判 inside 而 ray-based `contains` 判 outside；大量点恰好
  来自 ±2 mm 法线偏移样本，但也有少量离表面较远点，不能归结为 1 mm
  band。下一步需要区分 uniform 与 plus/minus 三类、检查 bucket 的薄壁/凹腔
  区域和 ray degeneracy，再决定 Gate 应使用何种独立符号 oracle。
- 计划原文要求的是“exact triangle distance + winding/contains sign”，硬门
  `≥99.99%`，因此当前实现把 trimesh proximity 与 ray `contains` 的差异直接
  当作 mesh sign failure 过于武断：二者内部都可能共享 ray/closest-face 的
  数值退化，且法线 ±2 mm 样本可能跨越 bucket 的薄壁。保持硬门不变，但需把
  查询分类并引入独立 robust winding-number/多方向 parity 证据后再重跑。
- 已核实当前 `mesh.contains` 使用 Embree，但 trimesh 默认仅固定一个方向的
  正反 ray parity；只有正反结果冲突且不含 freespace 时才用一个未固定 seed
  的随机方向递归补判。这既不完全 deterministic，也不是多方向 consensus。
  S1 应实现固定方向集合的双向 parity，多数投票并记录 direction agreement，
  以该 consensus 给 exact nearest-triangle distance 加符号。
- 环境中已有 Open3D 0.19 的 `RaycastingScene`，其
  `compute_occupancy/compute_signed_distance(..., nsamples=odd)` 原生支持
  多 ray inside 判定，并明确建议提高 `nsamples` 避免 edge/vertex sign flip；
  `igl/pysdf` 未安装。下一步用 Open3D `nsamples=5` 作为独立 robust sign
  backend，与 trimesh exact distance magnitude、contains 和已知样本交叉验证。
- bucket003 的 70k 正式 query 对比显示：Open3D multi-ray occupancy 与
  trimesh signed-distance sign 在 Open3D 1 mm band 外一致率
  `99.98993%`，与 trimesh ray `contains` 仅 `99.59444%`；距离幅值 p99 差
  `3.4e-8 m`。Open3D `nsamples=1/3/5/7/11/15` 的 occupancy 完全一致，
  说明其判定稳定；残余约 7–12 个跨 backend disagreement 需用 exact solid-angle
  winding number 仲裁，而非调整硬门。
- exact solid-angle winding 已仲裁 bucket003 在 trimesh 1 mm band 外的 9 个
  residual disagreement：`9/9` 均支持 Open3D occupancy，`0/9` 支持
  trimesh signed-distance sign；其中两个 uniform 点离表面达 2.4/7.5 cm，
  证明原失败来自 trimesh sign，而非 bucket topology。Open3D 0.19 已是项目
  direct dependency。S1 将以 trimesh exact unsigned distance + Open3D
  multi-ray sign 构造 `D_M`，并用 solid-angle winding audit 作为硬门。
- S1 计划和 builder 已按上述证据修订：`D_M` 距离幅值来自 exact nearest
  triangle，sign authority 为 Open3D `nsamples=5` occupancy；使用
  `nsamples=11` 做 100% stability gate，并对 2,000 个 stratified 样本加全部
  跨 backend disagreement 做 exact solid-angle winding audit，仍保持
  `≥99.99%` sign hard gate。下一步静态检查并重跑 direct-main tests/formal S1。
- 修订后的 builder 已通过 ruff format/check、py_compile、launcher syntax 和
  diff-check；正式 `oracle` launcher 已启动，当前仍在计算三个对象的 winding
  audit/direct-main tests，尚未返回最终状态。
- 正式三个对象已输出 `E181_GATE_A_NONVISUAL=PASS`，但 launcher 随后的首个
  synthetic cleanup test FAIL。根因是 fixture 文档称 tiny tetrahedron，代码却
  创建 20-face icosphere；其 face 数超过 16 且按 face-count 排序反而排在
  12-face 主 cube 前，使测试假设失效。实际 bucket 构建未受影响；下一步把
  fixture 改成 12-face tiny cube 后重跑全部 tests，不改生产清洗门。
- synthetic fixture 已改为 12-face tiny cube；ruff、py_compile 以及三个
  direct-main tests 全 PASS：cleanup、box `D_M<0` sign contract、三个实际
  bucket reduced build 均通过。需再跑一次完整 launcher，确认 formal artifacts
  和 tests 在同一执行中均 PASS，再进行 6 张视觉证据 review。
- 完整 `run_E181_local.sh oracle` 已在同一执行中严格 PASS：三个正式对象
  `E181_GATE_A_NONVISUAL=PASS`，三个 direct-main tests 全 PASS。Gate A 当前
  仅剩 6 张 component overlay / principal-axis cross-section 的人工视觉 review；
  在完成并持久化 3/3 approval 前仍保持 `visual_review=PENDING`。
- Gate A summary 审计确认 bucket003/004/007 的 winding sign agreement 均为
  `1.0`，finite 均为 `1.0`，删除组件数为 `0/1/2`。已查看 bucket003
  component overlay：raw 与 cleaned 在 XY/XZ/YZ 三视图逐点一致，无红色删除
  component，主体轮廓完整；该对象 overlay 视觉检查 PASS。由于系统无
  ImageMagick，后续直接逐张以原分辨率查看，不依赖 montage。
- bucket003 主轴中心截面三视图连续、闭合且无离群点，bucket003 视觉 review
  完整 PASS。bucket004 overlay 显示唯一 10-face 红色 fragment 位于主体左下
  外表面附近；cleaned 主体轮廓连续且 main component 自身已数值证明 watertight，
  删除后无可见缺口或其他形变。bucket004 overlay PASS，尚需其截面图确认。
- bucket004 主轴截面连续闭合，侧面小环与原有结构一致，无清洗伪影，因此
  bucket004 视觉 review 完整 PASS。bucket007 overlay 显示两个 4/2-face
  fragment 均投影在主体表面附近，cleaned 三视图保留完整桶体/上缘结构且无
  可见缺口或形变；bucket007 overlay PASS，尚需最后一张截面图确认。
- bucket007 主轴截面三视图连续闭合，圆形桶体、上缘/侧边细节均无离群或清洗
  伪影，因此 bucket007 视觉 review PASS；Gate A 已实际达到 3/3 approved。
  参考既有 E144/E168 做法，下一步新增显式 Codex visual-review builder，
  记录 reviewer、时间、每对象 findings 与 evidence SHA，并由脚本将 Gate A
  从 `PASS_NONVISUAL/PENDING` 原子晋级为 `PASS/APPROVED`，不手改 JSON。
- 已新增 `build_oracle_visual_review.py`：只接受当前 nonvisual PASS/PENDING
  artifacts，逐对象校验两张 evidence 存在并记录 SHA、findings、reviewer 与
  timestamp，生成独立 `visual_review.json`，随后原子更新三个 manifest 和
  Gate A summary 为 PASS/APPROVED；脚本支持已审批结果的幂等读取。下一步做
  static check、接入显式 launcher stage 并执行审批记录。
- launcher 已新增独立 `oracle-review` stage，并将输出写入
  `logs/E181/build/s1_oracle_visual_review.log`；它不会在 `oracle` 构建时自动
  审批，只有完成实际视觉检查后才显式调用。下一步运行 ruff/compile/shell
  检查与该 stage，再审计 review SHA 和最终 Gate A。
- visual-review builder 首轮 static check 仅有一项 ruff C420：constant-value
  dict comprehension 应改用 `dict.fromkeys`。首次 patch 因 ruff format 已把
  目标压成单行而 context mismatch，文件未改；下一步按现态单行修复后重跑
  全部静态检查。
- C420 已按 ruff 建议改为 `dict.fromkeys(APPROVED_FINDINGS, "PASS")`，未改变
  Gate 语义。下一步执行严格静态检查和 `oracle-review` 正式记录。
- visual-review builder/launcher 已通过 ruff、py_compile、bash syntax 与
  diff-check；正式 `oracle-review` 输出
  `E181_GATE_A=PASS approved=3/3 reviewer=codex_local_visual_review_20260731`。
  下一步审计 review record、manifest/summary SHA 指针和幂等重跑，再将 S1
  标记完成并进入 S2 CoACD 54-candidate sweep。
- Gate A review audit PASS：summary=`PASS`、三 object manifest=`PASS`、
  `approved=3/3`，所有证据和 review-record SHA 指针一致；review SHA
  `d111bd...78236`。`oracle-review` 幂等重跑后 review 与 summary SHA 均不变
  （summary SHA=`adb282...230a3`）。S1 / Gate A 完成，可进入 S2。
- 执行计划已把 S1 单独标为 completed，S2 标为 in_progress。S2 冻结网格为
  threshold `5/10/20mm` × max hull `8/16/32` × max vertices `32/64`，
  seed=1、real meters，共 54 candidates；先构建并验证 deterministic/convex/SHA/
  budget，再计算双向 surface、cavity、normal 与 contact-excess hard gates，
  最后按 hull 数等词典序选择 `C*`，不使用加权分数。
- CoACD 1.0.11 Python binding 合同已核对：`threshold` 在 `real_metric=True`
  时使用米，`max_convex_hull`、`max_ch_vertex`、`merge=True`、
  `decimate=False`、`seed` 均可直接冻结；输出为每 part 的 vertices/faces。
  已启动 bucket003 的 `20mm/8 hull/32 vertices` 单候选性能与 convexity smoke，
  当前仍在 native CoACD 计算中。
- 首个 smoke 完成：wall `26.63s`、peak RSS≈`2.07GB`、8/8 parts 均 convex
  且 watertight；CoACD 明确 warning max concavity 因 8-hull cap 超 threshold。
  但 `decimate=False` 时 part vertices 高达 545，说明 `max_ch_vertex=32` 不生效，
  会违反 S2 ≤64 hard gate。已用完全相同 config 启动 `decimate=True` 复核；
  是否冻结 decimation 必须由实测 vertex budget 决定。
- `decimate=True` smoke 完成：wall `26.59s`、peak RSS≈`2.17GB`，8/8 hull
  仍 convex/watertight，vertices=`31–32`，严格满足 32 budget；因此计划已冻结
  全部 S2 candidates 为 `merge=True, decimate=True`，并要求导出后再次实测
  vertex hard gate。单 candidate 约 27s，54 个顺序约 24min；builder 将按对象
  支持 resume，以三个对象进程并行构建。
- 已实现 `build_coacd_candidates.py`：完整参数/real-meter authority、isolated
  child per candidate 的 wall/RSS、deterministic 17-digit OBJ、convex/topology/
  vertex budget、part/candidate SHA、native concavity warning evidence、严格
  resume，以及三 object worker 后 54-candidate 汇总。首轮静态检查只发现未用
  `os` import 和 import-order，下一步机械修复后做 single-candidate formal smoke。
- import 问题已修且静态检查 PASS，但 formal single-candidate 立即 `RC=139`，
  与先前纯 `coacd+trimesh` smoke 的 27s 成功不同。最可能差异是 builder 为复用
  `atomic_json` 导入 `build_oracle_meshes`，连带加载 Open3D/matplotlib 后再进入
  CoACD native runtime，触发 native/OpenMP 冲突。下一步用 faulthandler 定位，
  并让 CoACD builder 自包含 JSON writer，避免无关 Open3D import。
- faulthandler 已精确确认 segfault 在 `build_oracle_meshes` 连带的
  `open3d/__init__.py` import，尚未进入 CoACD；builder 已移除该跨 stage import，
  内置同语义 atomic JSON writer。这个修复隔离 S1 Open3D 与 S2 CoACD native
  runtime，不隐藏失败。下一步重跑静态检查与 formal single candidate。
- 隔离修复后 static checks 全 PASS；bucket003 `t020_k08_v032` formal candidate
  已重新启动并正常进入约 27s 的 native 计算（不再 import-time crash），当前
  等待构建/导出/manifest 最终结果。
- native CoACD 已成功返回，但 exporter 在 part0 round-trip gate 失败。审计发现
  CoACD array 含 32 vertices，而 OBJ 实际只写/重载 31 个 referenced vertices
  和 58 faces；这是一个未引用顶点的 serialization 差异，不是几何漂移。下一步
  在导出前显式、可计数地删除 unreferenced vertices，并把 before/after 数写入
  part manifest，再保持 OBJ 的逐值 round-trip hard gate。
- exporter 已显式执行 `remove_unreferenced_vertices()`，manifest 同时记录
  `coacd_vertex_count`、删除数和最终 `vertex_count`；这不改变任何 face/表面。
  修改后 ruff/compile/diff-check 全 PASS。下一步再次运行 formal single
  candidate；成功后立即用 controller resume 路径验证 log/warning evidence。
- 第二次 formal run 仍仅在 vertex `np.array_equal` round-trip 门失败，face
  indexing 已精确一致，说明剩余差异是 OBJ decimal serialization 的浮点 bit
  pattern，而非重排。S2 part 不要求像 `M*` 那样保留输入 binary coordinates；
  authority 是导出 OBJ SHA。下一步改为记录 max serialization displacement，并
  以 `≤1e-15m` 硬门（17-digit 输出的数值精度量级），face 仍保持 exact。
- part OBJ gate 已更新为 face exact + max vertex serialization displacement
  `≤1e-15m`，实测值将进入 manifest；静态检查全 PASS。下一步第三次 formal
  run 应给出实际 displacement 或完整 PASS，之后不再围绕 bit-equality 重试。
- 第三次 formal `bucket003/t020_k08_v032` 完整 PASS：8 hulls、wall
  `26.330s`，native warning 被保留；所有 parts 通过 convex/watertight/vertex/
  face exact/`≤1e-15m` serialization gates。当前该 `--one` manifest 尚未由
  controller 附加 log SHA/warning parsed fields；下一步用 object controller
  resume/build 路径验证并开始正式 sweep。
- 多次同参数 smoke 的 native concavity warning 数值不同，暴露 CoACD/TBB
  parallel scheduling 可能破坏 seed-level determinism。builder 已在加载 native
  库前冻结 `OMP_NUM_THREADS=TBB_NUM_THREADS=OPENBLAS_NUM_THREADS=1` 并写入完整
  parameter contract；resume 现在也强制要求 native log 与 parsed warning
  evidence，因而当前裸 `--one` candidate 会由 controller 正确重建。下一步先做
  两个隔离 output roots 的 SHA determinism test，再启动 54 sweep。
- threading-contract 修改已通过静态检查，宿主没有预设上述 thread env。当前已
  在两个 `mktemp` 隔离 roots 顺序运行同一 bucket003 `t020_k08_v032`，将比较
  ordered part/candidate asset SHA；测试仍在 native CoACD 计算中，结果未判定。
- determinism 双跑已超过单次 27s 基线但进程仍正常运行、尚无 stdout；单线程
  contract 可能显著增加 native wall time。继续等待最终 SHA，不因耗时放宽
  reproducibility gate。
- determinism run A 已 PASS，单线程 wall=`88.760s`（约为并行 smoke 的 3.4×）；
  run B 仍在执行。若 SHA 一致，54 candidates 三对象并行的预计 wall 约 27min；
  若不一致则单线程仍不足以冻结 CoACD，需要将一次生成的 part SHA 作为明确
  authority 并重新界定“determinism test”，不能静默宣称重跑可复现。
- determinism run B wall=`88.567s`，两次 candidate asset SHA 和 ordered part
  SHA 完全一致：`7be5da...075f`，单线程 contract 使 CoACD 重跑可复现。已新增
  `test_coacd_assets.py`，可把这两个隔离 run 的完整参数/part SHA/wall/RSS 记录
  为 canonical evidence，并在 formal sweep 后审计 54 manifests、所有 part SHA、
  convex/topology/vertex/serialization/native-log gates。
- `test_coacd_assets.py` 首轮仅有 import-order，已自动修复；全 E181 ruff、
  py_compile 与 diff-check PASS。下一步把 `/tmp/e181_coacd_det_a.tgWeNa` 与
  `/tmp/e181_coacd_det_b.0DKWnk` 的两次成功结果提炼为 repo-relative canonical
  determinism evidence，然后接入三 object 并行 launcher。
- canonical determinism evidence 已写入
  `s2_coacd/determinism_probe_t020_k08_v032.json`，记录两次 wall
  `88.760/88.567s`、peak RSS≈`887MiB`、相同 8-part SHA 和 asset SHA
  `7be5da...075f`。launcher `coacd` 已接入三个 object background workers，
  全部 wait PASS 后才做 global 54 summary 和 direct-main tests；单-object
  controller 不再错误提前汇总全局。下一步静态检查后启动正式约 27min sweep。
- 全 E181 static checks 与 launcher syntax 均 PASS；正式 54-candidate CoACD
  sweep 已启动，bucket003/004/007 三个单线程 deterministic workers 并行，
  当前尚在各自首个 candidate native 计算。不会使用 GPU，也不会影响已授权的
  Ada 共存任务。
- sweep 启动后首个约 40s 尚无 candidate 完成输出，三个 worker 进程仍运行；
  这与单线程 88s/candidate 基线一致。继续按 30s 轮询，等待首批三个 manifests。
- 首批 formal candidates 正常 PASS：bucket004 `t005_k08_v032/v064` wall
  `69.89/69.64s`，bucket007 `t005_k08_v032` wall `118.38s`；均为 8 hulls 且
  native concavity warning 已写 evidence。bucket003 的 5mm 首候选仍在计算，
  没有 worker failure；继续并行 sweep。
- bucket004 第三个 `t005_k16_v032` 也 PASS（16 hulls, `68.11s`）；bucket003
  首候选和 bucket007 第二候选尚未完成。当前吞吐由 object 几何复杂度显著不同，
  但 resume granularity 为单 candidate，任何中断不会丢失已完成 manifests。
- bucket004 已完成 5mm grid 前四项至 `k16/v064`，bucket007 已完成
  `k08/v032/v064`；新 wall 约 `68.0s` 与 `118.3s`。bucket003 的 5mm
  decomposition 仍明显更慢，但进程未报错；继续等待，不提前降低 resolution。
- bucket004 的全部 6 个 5mm candidates 已完成（最后 k32 pair wall
  `66.77/66.42s`）；bucket007 已到 `t005_k16_v032`（`117.53s`）。
  所有 5mm outputs 都触发 hull-cap concavity warning，后续 fidelity Gate B
  会决定可用性，build stage 不据 warning 直接拒绝。
- bucket004 进入 10mm 后 wall 显著降至约 `24–26s`，已完成前三项至
  `t010_k16_v032`；bucket007 完成 `t005_k16_v064`。bucket003 首个 5mm
  candidate 仍未返回，可能其严格 threshold 搜索远慢于另两对象；launcher 仍
  活跃且另两 worker 持续产出。
- bucket004 已完成全部 10mm candidates，其中 k32 pair 无 hull-cap warning，
  并进入 20mm（k08 pair≈`15.4s`）；bucket007 到 `t005_k32_v032`
  (`116.0s`)。bucket003 5mm 仍是当前长尾；build 进度至少 17/54 candidates。
- bucket004 worker 已完成全部 18/18 candidates；20mm k16 pair 为 16 hulls，
  k32 pair自然停在 19 hulls，wall `13.5–13.9s`，无额外构建失败。另两个
  object workers 继续运行；全局 summary/test 会等它们结束后才启动。
- bucket007 已完成全部 6 个 5mm candidates（k32/v064 `116.4s`），并进入
  10mm，首项 wall 降至 `43.3s`。bucket003 严格 5mm 首项已运行约 12min，
  仍无 native error/output；其他 workers 的持续成功表明 launcher/资源正常。
- bucket007 10mm 已完成 k08 pair 和 k16/v032，wall 稳定 `42–43s`；总 build
  进度至少 27/54。bucket003 long-tail 继续运行，未设任意 wall timeout，因为
  native 仍可能合法完成且已有 resume 保护其他 36 candidates。
- bucket007 10mm 已推进至 k32/v032，wall `40–42.5s`，仍全部 BUILD_PASS；
  下一项后将进入更快的 20mm grid。bucket003 首个 5mm 仍未结束，当前是唯一
  未产生 manifest 的 worker。
- bucket007 已完成全部 10mm 并进入 20mm，k08 pair wall≈`23s`；当前至少
  32/54 candidates 完成。bucket003 的 5mm search 已约 16min；仍保留进程，
  因为 CoACD 在严格 threshold 下的 MCTS/preprocess 成本可远高于松阈值。
- bucket007 worker 已完成 18/18；20mm k16/k32 pairs wall `20–22s` 且均
  BUILD_PASS。现在 36/54 formal candidates（bucket004+007）已冻结，只剩
  bucket003 worker；其首个 5mm candidate 仍在 native 计算。
- bucket003 又运行约 60s 无输出；因为 launcher 尚未返回 shell，worker 仍存活。
  已完成的 36 candidates 和 determinism evidence 都已落盘；继续观察该合法
  long-tail，不终止/重启导致重复计算。
- bucket003 long-tail 再经过 60s 仍未结束，累计约 20min。当前没有 OOM、
  crash 或 launcher failure 信号；保持任务运行并继续短轮询。
- bucket003 继续运行、累计约 21min，无新增 stderr/stdout。其 5mm threshold
  对应最复杂 bucket003，可能在达到 hull cap 前进行大量分割搜索；当前不将慢
  运行误判为 blocker。
- bucket003 累计约 22min 仍在 native call，launcher session 保持 running。
  继续等待；如果最终 native 返回，后续同 threshold 的 v064 可能复用相同搜索
  复杂度但仍需独立生成，resume 将逐项保存。
- bucket003 首个 `t005_k08_v032` 已正式 PASS，wall=`1491.634s`（24.9min）。
  该结果已由 controller 写入完整 manifest/log evidence。继续 object-serial
  会使剩余 sweep 达数小时，因此已在下一 candidate 刚启动时向**本地 E181**
  launcher 发送 Ctrl-C（RC=130）；bucket004/007 的 36 项与 bucket003 首项均
  保留，未触碰任何远端既有任务。下一步实现 candidate-level bounded parallel
  resume，保持每 child 单线程与相同 SHA contract。
- builder 已新增 `--parallel-workers` bounded candidate executor：先严格 SHA
  resume，再为 pending configs 启动隔离 child，每 child 仍冻结单线程/seed/
  参数并独占 candidate directory；结果按 candidate_id 排序汇总。launcher 改为
  每对象顺序、对象内默认 6-way（约 6×0.9GB peak），从而避免 object×candidate
  乘法过并发。下一步静态检查并用现有 37/54 resume 启动剩余 17 项。
- bounded-parallel 修改通过全部 static checks；正式 resume launcher 已启动。
  bucket003 应跳过已完成 `t005_k08_v032`，并以 6 workers 构建其余 17 项；
  bucket004/007 随后应 18/18 全部 SHA-skip。当前等待 resume/pending banner 和
  首批松阈值 candidates。
- resume 行为符合预期：bucket003 首项严格 SHA-skip，banner 显示
  `pending=17 workers=6`；启动后首个 30s 暂无完成项。6 个 isolated children
  正在并行，预计 10/20mm 先返回、5mm 作为长尾。
- 并行启动约 90s 仍无 child 完成；首批队列实际包含剩余 5 个 5mm configs 加
  1 个 10mm config，因此严格分解占据大部分 workers。没有 failure 输出，继续
  轮询。
- 首个并行完成项为 bucket003 `t010_k08_v032`，8 hulls、wall=`192.686s`；
  相比独立 20mm probe 的 88s 更慢，说明 6-way 有 CPU/memory contention，但
  总吞吐仍优于 5mm 串行。该 worker 已接续下一个 pending config。
- 随后 60s 暂无新增完成项；5 个 5mm children 仍是长尾，第 6 个 worker 正在
  后续 10mm candidate。保持 6-way，不在运行中动态改变并发导致 reproducibility
  contract 混杂。
- bucket003 `t010_k08_v064` 也 PASS，wall=`192.060s`，与 v032 一致且 native
  warning 相同；10mm k08 pair已冻结。其 worker 继续 k16，另外 5 个 5mm
  candidates 仍并行运行。
- 继续运行 60s 无新增输出；目前总 formal build 39/54。所有 active children
  都在各自 isolated output 目录，暂无部分文件互相覆盖风险。
- bucket003 `t010_k16_v032` PASS，16 hulls、wall=`188.029s`；进度 40/54。
  10mm candidate wall 稳定约 3.1min，worker 持续向后推进，5mm 长任务尚无
  完成项。
- 再运行 60s 无新增项；并发 batch 稳定，无错误输出。继续等待 k16/v064 和
  后续松阈值结果。
- bucket003 `t010_k16_v064` PASS，wall=`188.549s`，与 v032 的 native
  concavity warning 完全一致；进度 41/54。10mm k32 pair 接续运行。
- 10mm k32 与五个 5mm children 继续运行 60s，无 failure。当前并发内存预算
  仍在预检约 6×0.9GiB 范围内（未出现 OOM/kill 信号）。
- bucket003 `t010_k32_v032` PASS，32 hulls、wall=`186.245s`，native warning
  表明即使 32 cap 仍高于内部 threshold；进度 42/54。v064 与后续 20mm 接续。
- 继续 60s 无新增项；5mm batch 已运行约 16min，接近此前独立 24.9min 基线，
  10mm k32/v064 也仍在计算。无错误输出。
- bucket003 全部 6 个 10mm candidates 已完成；最后 k32/v064 wall=`186.523s`。
  进度 43/54，动态 worker 现进入 20mm，预计这些会先于剩余 5mm long-tail 返回。
- 20mm 首项启动约 60s 尚未完成；5mm batch 约 18min。6-way session 正常运行，
  继续等待。
- bucket003 `t020_k08_v032` PASS，wall=`94.034s`，candidate asset 应与独立
  determinism probe同参数结果一致（最终 tests 会逐 SHA 检查）；进度 44/54。
  v064 与其余 20mm candidates 接续。
- bucket003 `t020_k08_v064` PASS，wall=`93.901s`，与 v032 native warning
  一致；进度 45/54。剩余 4 个 20mm + 5 个 5mm candidates。
- bucket003 `t020_k16_v032` PASS，wall=`93.263s`；进度 46/54。5mm batch
  已约 22min，接近首个独立 24.9min 但并发 contention 可能略延长。
- bucket003 剩余五个 5mm long-tail 已在 25.45–25.58min 几乎同时全部 PASS：
  k08/k16/k32 的 v032/v064 warning 成对一致；同时 `t020_k16_v064` PASS。
  当前正式进度 52/54，只剩 20mm k32 pair；candidate-level parallel 将原本
  预计约 2h 的 5mm 串行段压缩到约 25.6min。
- S2 CoACD build stage 已完整 PASS：最后 bucket003 20mm k32 pair
  `88.78/88.03s`，三对象均 18/18，global=`54/54`；bucket004/007 严格
  18/18 SHA-resume。direct-main grid/formal/determinism tests 全 PASS。
  artifact audit：全部 `BUILD_PASS`，hulls `8–32`，max part vertices=`64`，
  max OBJ displacement=`6.94e-18m`，44/54 有 hull-cap warning，max peak
  RSS≈`888MiB/child`；formal determinism SHA 精确匹配 probe
  `7be5da...075f`。下一步进入 Gate B fidelity 与 canonical selection。
- Gate B 数据合同初审：dev3 manifest 精确覆盖 bucket003/004/007 各一 case；
  trajectory 含 qpos/qvel/ctrl/contact/contact_pos，contact_pos 为 world-space
  `(T,2,3)`，不能直接当 object-local query；contact-mask NPZ 只存 hand/object
  距离与 mask，不存采样点坐标。必须结合 scene/object pose 把 contact_pos 转到
  object local，或从目标 hand geometry 重建点；在解析 qpos free-joint 与 XML
  body/mesh transform 前，contact-excess 不能用错误坐标近似。
- MuJoCo scene 已确认 object freejoint qpos address=`36`（pos 36:39, quat 39:43），
  visual mesh 另有 body-local geom pos/quat transform。首次手写逆变换后的 dev
  contact_pos 到 `M*` 距离中位数仍为 28–117mm、尾部达 0.4–0.69m，明显不像
  3cm contact 样本；下一步用 MuJoCo `mj_forward` 的 geom world transform复核，
  并确认 trajectory `contact_pos` 的语义，不能把它直接用于 Gate B。
- 用 MuJoCo `mj_forward`/`geom_xpos+xmat` 复核得到与手写变换相同结果，排除
  frame-transform bug。仓库源码确认 trajectory `contact_pos` 是 FK palm site
  world position，作为 reward/constraint target，并不保证每帧贴 object surface；
  因此 Gate B 正应使用计划定义的
  `max(0, dist(target,C)-dist(target,M*))`，远离表面的 target 不会被误算成
  collider error。dev/heldout 将各自按 E178 manifest 转成 mesh-local targets。
- 首个 naive bucket003 k8 probe 揭示 compound-union fidelity 不能直接把所有
  part triangles 当 `C` surface：内部切割面会把 C→M p90 虚增至 99mm，也会使
  nearest normal p90 达 92°。已改用每个 part surface 点的 ±normal occupancy
  测试，仅保留“外侧 free、内侧 occupied”的真实 union boundary，再以 dense
  boundary sample + KD-tree 计算 M→C/normal、exact M nearest 计算 C→M。当前正
  在三个较优候选上验证该测量合同及 cavity gate。
- 20µm boundary probe 仍保留大量相邻 hull 之间的数值缝/切割面，导致较优
  bucket004 的 C→M p90 仍 59mm；而 M→C 已仅 3.5/6.4mm，证明这不是整体几何
  失真。正在扫描 ±normal occupancy offset `0.02–10mm`，寻找能滤掉 inter-part
  seam、又显式计入误差预算的稳定 boundary 定义；不能用原 mesh sign 直接删
  under-approx surface 来美化指标。
- seam-offset sweep 显示 bucket004 C→M p99 从 20µm 的 111mm 降至
  2mm 的 18.5mm、5mm 的 6.5mm 并稳定，但这是 morphological boundary proxy，
  不应取代 exact union。计划明确要求 `C_i↔D_M`，因此下一步优先检查
  trimesh/manifold exact boolean union backend；只有 exact union 不可用时才把
  offset 作为带误差预算的 fallback。通用 convex-hull free-space probe 同时表明
  三个较优候选 broader false occupied 仍约 7.6–26%，可能触发计划规定的
  `ASSET_REJECTED`，需全 54 候选证据确认。
- manifold3d backend 可用且 32-part union 仅需 0.03s、输出 watertight；但
  CoACD parts 只在切割面零接触、不重叠，manifold 保留这些 touching components
  的内部面，exact boolean mesh 的 C→M p90 仍 82mm。对 compound collider，
  robot 无法穿过亚毫米 seam 接触深层切割面，因此 Gate B 将明确采用
  `2mm accessibility probe` 定义可达 union boundary，同时单独记录 seam
  sensitivity（0.02/0.2/0.5/1/2/5mm），避免把内部 partition area 当外形失真。
- 已实现 `evaluate_asset_fidelity.py`：每 object 冻结 20k M surface、1M
  convex-hull cavity sampling（最多 20k broad/10k core）、E178 full27
  FK-palm dev/heldout targets；每 candidate 生成 120k raw part surface 并用
  2mm accessibility 过滤，计算双向 surface/KD normal/must-cover、convex
  halfspace cavity occupancy、dev/heldout contact-excess 与全部硬门，支持
  SHA/config resume 和 54-row TSV。首轮 static 仅剩 import-order 待修。
- import-order 已修，全 E181 ruff/compile/diff-check PASS。已启动 bucket004
  单对象正式 fidelity smoke：当前正在构建 1M cavity 与 full27-derived target
  fixture/评估 18 candidates，尚未返回首项结果。
- bucket004 18/18 fidelity 已完成且全部 REJECT；broader cavity false occupied
  最低为 `5.95%`（t010/k32/v032），远高于冻结的 `0.1%`，v064 分解在同配置下
  反而系统性更差，证明需要实测而不能按 vertex budget 猜测。下一步审计最低
  cavity candidate 的其余 gates/fixture counts，确认 reject 不是 boundary/KD
  实现错误，再跑 bucket003/007。
- bucket004 fixture 规模有效：20k broad、4,934 core、290 dev targets、796
  heldout targets。`t010_k32_v064` 的双向 p90/p99、must-cover、normal、
  core、dev/heldout contact 均过门，**唯一失败是 broader cavity=7.57%**；
  v032 虽 cavity略低 5.95%，却另败 C→M/normal。这确认 ASSET_REJECTED 风险来自
  冻结的通用 cavity hard gate，而不是 contact transform 或单一测量 bug。
- 全量 fidelity 已启动；bucket003 18/18 全部 REJECT，best broader cavity
  仍为 `19.07%`（t020/k32/v032），远离 0.1% hard gate。bucket004 artifacts
  将 SHA/config resume，当前继续 bucket007 与 global summary。
- 首轮 54-candidate fidelity 输出 `ASSET_REJECTED`，三对象 pass counts 均 0；
  best cavity 为 bucket003 `19.07%`、bucket004 `5.95%`、bucket007 `4.625%`，
  且 core/normal/surface 等也有大量失败，结论不靠单一阈值边界。但审计发现
  evaluator 同时计算了 heldout contact-excess，违反“C* 冻结后才解封 heldout24”
  的流程合同。虽然 heldout 未参与 dev status/selection、且当前无 dev PASS
  candidate，这不会改变 reject 结论，但该 evidence 不能作为正式 Gate B。
  下一步移除所有 heldout trajectory 读取/字段，变更 config SHA，dev-only 重跑
  54 项后才收口 ASSET_REJECTED。
- evaluator 已移除 `full27.tsv` 与全部 heldout trajectory/target/distance/gate
  读取，只从 `dev3.tsv` 构建每对象一个 dev fixture；artifact 显式写
  `heldout_status=SEALED_UNTIL_C_STAR`。config SHA 随之变化，旧 fidelity 不会
  resume，将被 dev-only 结果覆盖。下一步静态检查并完整重跑。
- heldout negative grep、ruff、py_compile、diff-check 全 PASS；dev-only 54
  candidate 重跑已启动，当前正在重建无 heldout arrays 的 fixtures并覆盖旧
  fidelity，尚未输出最终 Gate B。
- dev-only 重跑中 bucket003 与 bucket004 各 18/18 已重新输出 REJECT，cavity
  数值与首轮一致（heldout 从未参与该指标/状态），证明移除 heldout 不改变 dev
  判定。bucket007 与 global summary 仍在运行。
- 正式 dev-only Gate B 已收口为 `ASSET_REJECTED`：54/54 evaluated、三个 object
  pass counts=`0/0/0`。heldout audit PASS：三个 fixture NPZ 仅含 M surface、
  cavity、dev targets/D_M；fidelity/TSV 只有
  `heldout_status=SEALED_UNTIL_C_STAR`，无任何 heldout metric/input。
  candidate_metrics SHA=`c8f781...75794`，evaluator config SHA=
  `86d610...57a6a`。按冻结计划，任一对象 K≤32 无 candidate 通过就禁止进入
  S3，不得临时放宽 cavity/contact gate；下一步持久化 no-selection terminal。
- 已新增 `select_canonical_set.py`：若存在 PASS 才执行冻结的词典序选择；当前
  0/0/0 情况会清空 `selected`、写 `SEALED_NO_C_STAR` 与
  `S3_S6_NOT_AUTHORIZED_BY_GATE_B`，同时生成 cavity、must-cover/contact-excess
  两张 54-candidate heatmap和逐对象 closest-rejected/failed-gate diagnostics。
  首轮 static 仅有 matplotlib 后置 import-order 待修。
- selection static checks 已全 PASS；正式运行按设计返回 terminal RC=3，并由
  wrapper 断言为 expected：
  `ASSET_REJECTED selected=0/3 heldout=SEALED_NO_C_STAR`。没有生成/冒充 `C*`，
  S3–S6 依法未授权。下一步查看两张 heatmap、审计 no-selection JSON 与 closest
  rejected diagnostics，并把 launcher/plan/progress 收口到这一负结果。
- 两张 Gate B heatmap 已逐图检查 PASS。cavity 图显示三个对象所有 54 cells
  都远高于 0.1%，bucket003 尤其为约 19–31%；task 图显示增加 hull 数通常改善
  must-cover/contact-excess，v64 也常改善 contact，但 cavity 往往更差，明确
  存在 trade-off，不能靠“选更细参数”解决全部硬门。视觉证据与数值
  ASSET_REJECTED 一致，无绘图错位/空图。
- no-selection JSON 审计 PASS：`selected={}`、三个 pass_count=0、
  `SEALED_NO_C_STAR`、`S3_S6_NOT_AUTHORIZED_BY_GATE_B`，record SHA=
  `bfae42...fe62`；两张 visual SHA 指针一致。主计划已写入 2026-07-31 实际终点
  和 2mm accessibility contract，明确 Full 三卡未启动。launcher 新增独立
  `asset-eval` stage：selection RC=3 会显式返回 planned ASSET_REJECTED，
  不会误入 grid/physics/canary/full。
- 执行计划已全部收口到 Gate B terminal（S0/S1、54 build、dev-only fidelity、
  no-selection 均 completed；后续步骤改为“遵守 gate、禁止 S3–S6/Full”并完成）。
  最终 ruff、py_compile、launcher `bash -n`、`git diff --check` 全 PASS。
  worktree 仅包含本次 E181 计划/依赖/新脚本与既有用户改动；未提交、未触碰
  E178 历史结果，也未启动或抢占远端 GPU。
- experiment tracker 仍错误写着“仅计划、未启动实现或实验”，必须更新。现有
  experiment logs 最新编号为 244；下一步创建
  `245_E181_coacd_gate_b_asset_rejected.md`，记录 S0/S1 PASS、54 build、
  dev-only Gate B、heldout sealed、Full 未启动及下一实验决策边界，并把 tracker
  E181 行链接到该结果日志。
- E181 结果日志已写入
  `log/245_E181_coacd_gate_b_asset_rejected.md`，并将 Tracker 首行从错误的
  “仅计划”更新为 `ASSET_REJECTED`。日志明确保留 E178 同 27 case 与
  local/Ada0/Ada1=`9/9/9` 的 Full 合同，但记录 Gate B 0/18/0/18/0/18 后
  `C*`、`D_C`、S3–S6 与三卡 Full 均未获授权、未启动。下一步生成自动 log
  索引并执行最终静态/链接校验。
- 已运行 `build_log_index.py`，自动生成的 `log/INDEX.md` 已纳入 E181
  `ASSET_REJECTED` 结果日志；索引报告共 244 个实际 log 文件。下一步只做
  final static、artifact SHA、Tracker/INDEX 链接与 Markdown 一致性检查。
- E181 最终收口检查 PASS：ruff check、ruff format-check、10 个 E181 Python
  文件 compileall、launcher `bash -n`、`git diff --check` 全通过；Tracker 与
  INDEX 均正确链接 log 245。canonical SHA 复核一致：
  candidate metrics=`c8f781...75794`、no-selection=`bfae42...fe62`；
  JSON 断言确认 `ASSET_REJECTED`、三个 pass count 均为 0、
  `SEALED_NO_C_STAR` 与 `S3_S6_NOT_AUTHORIZED_BY_GATE_B`。E181 至此正式终止
  于 Gate B，不启动 Full。
- Active-goal 完成性审计再次从当前 artifact 独立复核：S0 authority
  `27/3/24`、162 SHA 与 object `9/4/14` PASS；S1 oracle/visual `3/3`
  PASS；S2 build `54/54`、determinism SHA exact；正式 dev-only Gate B
  三对象均 `0/18`。三个 fixture NPZ 只有 M surface/cavity/dev target/D_M，
  54-row TSV 全部 `SEALED_UNTIL_C_STAR`，无 heldout metric；结果树不存在
  `C*`、`D_C`、视频或 Full result。当前本地无 E181 进程，远程无 E181 tmux
  或 isolated run root；Ada GPU 只读快照为 `1131/49140MiB` 与
  `19/49140MiB`，审计未 kill/暂停/启动任何远端进程。计划 384–385 明确规定
  任一 object 无 PASS 时以 `ASSET_REJECTED` 终止，故 S3–S6 未执行是 gate
  compliance，不是漏项。下一步重跑 direct-main/static checks，并按技能规则
  归档超过 200 行的 progress。
- 完成性审计已检查三个 direct-main test 入口：preflight authority build 与
  oracle reduced build 都写入 tempfile；CoACD test 只读 formal 54-candidate
  manifests 和已冻结 determinism evidence。因此可安全重跑验证而不会覆盖
  canonical Gate B 或远程环境快照。
- 完成性 direct-main tests 全部 PASS：preflight `2/2`、oracle `3/3`、
  CoACD grid/formal/determinism `3/3`（formal candidates=`54`）。随后 ruff、
  ruff format-check、compileall、launcher `bash -n` 与 `git diff --check`
  全部通过，且测试未改写 Gate B canonical artifacts。可以执行 progress
  无损归档和最终记录审计。
