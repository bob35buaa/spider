# Core4D data construction v3 seed data

本目录放可以进 git 的小型状态种子文件，不放运行结果。

- `existing_cases.tsv`：从可信历史结果整理出的 v3 `case_state_registry` seed。
- `existing_cases_summary.md`：seed 的来源和统计。

这些文件只保存状态、指标摘要和证据路径；大体积 CEM/视频/NPZ 仍留在 `workspace/core4d/results/` 或对应运行目录，不纳入 git。
