# R{XXX} (V{X.X}) 实验计划：{标题}

## Context

{前置实验结果摘要 + 根因分析}

### 根因分析

{当前问题的根因}

### 关键 insight

{本次实验的核心假设/思路}

## Claims

| Claim | 最低证据 |
|-------|---------|
| {要证明的命题} | {量化标准} |

## 改动

### 1. {改动描述}

**文件**: `{path/to/file}`

```python
# 代码片段或伪代码
```

{设计考量}

## 需要修改的文件

| # | 文件 | 改动 |
|---|------|------|
| 1 | `{file_path}` | {改动描述} |

## Reward 权重（如适用）

| 类别 | 旧权重 (R{prev}) | **新权重 (R{XXX})** | 占比 |
|------|------------------|---------------------|------|
| Body tracking | | | |
| Object pos+rot | | | |
| {其他} | | | |
| **Total** | | | |

## 训练命令

```bash
bash workspace/{exp_name}/scripts/train/train_{exp_name}_{version}.sh R{XXX} {GPU}
```

## 成功标准

| 指标 | 前次 (R{prev}) | 本次目标 (R{XXX}) |
|------|----------------|-------------------|
| GUI: {关键行为} | {描述} | **{期望行为}** |
| {量化指标} | {数值} | **{目标数值}** |
