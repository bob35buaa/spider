# @package _global_
# E063 Tier 1: re-enable stability_penalty + reduce task_obj weights on box023.
# Base: E062 (sphere geometry + auto X1 palm_normal [+1,0,0] for box023).
# Diagnosis: log 79 §3 — sim 完成 1.6m carry task (final 9.8cm) 但 carry→place
#   转换 (t=2.0-2.7s) 摔倒. 元凶 (a) stability_penalty=0 无摔倒 penalty,
#   (b) task_obj_rew=1.0 强 forward 牵引超过脚底支撑.
defaults:
  - core4d_e062_box023
  - _self_

stability_penalty_scale: 1.0    # was 0.0 (E034 default; E036 disabled; E041c never re-enabled)
task_obj_pos_rew_scale: 0.5     # was 1.0 (50% reduction, ease forward pull)
task_obj_rot_rew_scale: 0.5     # was 1.0 (50% reduction)
